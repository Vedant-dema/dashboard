# Feature Log — 2026-03-27

> Features added or modified on this date.

---

## FEATURE-046: Remove "S* Sonstiges" from Waschprogramm Dropdown
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The generic wash program option `S* Sonstiges` was removed from the `Waschprogramm` dropdown so users select only defined tariff/program entries.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — removed `S* Sonstiges` entry from `WASCH_PROGRAMME` options
- `docs/feature-logs/2026-03-27.md` — logged feature change entry
- `docs/FEATURE-LOG.md` — updated daily and master feature index

### What It Does (Technical)
1. Deletes the `S* Sonstiges` object from the structured wash program option list.
2. Leaves blank default option unchanged.
3. Keeps automatic Netto/Brutto price fill logic unchanged for remaining options.
4. Does not change persistence or payload structure.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `wasch_programm` | string | No | Selectable values now exclude `S* Sonstiges` |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kundenWash`
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
If existing records already store `S* Sonstiges`, they can still exist historically; the option is only removed from new dropdown selections.

---

## FEATURE-045: Waschprogramm Label Simplification and Blank Default Option
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The wash program selector title was simplified and its default visible option was changed to a blank entry. Users now see `Waschprogramm` (without `/ Tarif`) and the dropdown starts empty by default.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — changed label from `Waschprogramm / Tarif` to `Waschprogramm` and changed default select option to blank text
- `docs/feature-logs/2026-03-27.md` — logged feature change entry
- `docs/FEATURE-LOG.md` — updated daily and master feature index

### What It Does (Technical)
1. Renames the form label in the `Wasch-Infos` section.
2. Keeps select `value=""` as default on new form state.
3. Replaces placeholder option glyph (`—`) with an empty option display.
4. Preserves existing auto-price population behavior once a program is selected.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `wasch_programm` | string | No | Starts blank by default; selected option fills prices |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kundenWash`
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
This is a presentation adjustment only; persistence and price mapping remain unchanged.

---

## FEATURE-044: Auto-Fill Netto/Brutto Prices from Waschprogramm Selection
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The `Waschprogramm / Tarif` dropdown now carries price metadata. When a program is selected, `Netto preis` and `Brutto preis` are auto-filled based on the selected tariff, matching the legacy behavior shown in the reference screenshots.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — converted wash program list to structured options with prices and wired dropdown `onChange` to populate price inputs
- `docs/feature-logs/2026-03-27.md` — logged feature change entry
- `docs/FEATURE-LOG.md` — updated daily and master feature index

### What It Does (Technical)
1. Replaces `WASCH_PROGRAMME` plain string list with `{ label, netto, brutto }` option objects.
2. Adds `priceToInput()` formatter to normalize numeric prices to German-style input format (`21,85`).
3. Updates the wash program select handler to:
   - set selected program label,
   - lookup matching tariff object,
   - auto-fill `wash_netto_preis` and `wash_brutto_preis`,
   - clear both fields when selection is empty.
4. Keeps both price inputs editable after auto-fill for manual overrides.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `wasch_programm` | string | No | Selected tariff label |
| `wash_netto_preis` | string | No | Auto-filled net price text (editable) |
| `wash_brutto_preis` | string | No | Auto-filled gross price text (editable) |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kundenWash`
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Price values are currently configured as static frontend tariff metadata based on provided reference and can be refreshed later if official rates change.

---

## FEATURE-043: Add Netto/Brutto Price Inputs to Wasch-Infos
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
Two new price input fields were added directly under `Waschprogramm / Tarif` in the `Wasch-Infos` section: `Netto preis` and `Brutto preis`.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — added two new inputs under `Waschprogramm` and payload mapping logic
- `frontend/src/types/kunden.ts` — extended `KundenWashStamm` with `netto_preis` and `brutto_preis`
- `frontend/src/store/kundenStore.ts` — extended upsert field pick and create-path persistence for `netto_preis` and `brutto_preis`
- `docs/feature-logs/2026-03-27.md` — logged feature change entry
- `docs/FEATURE-LOG.md` — updated daily and master feature index

### What It Does (Technical)
1. Adds `wash_netto_preis` and `wash_brutto_preis` to modal form state.
2. Renders two decimal-friendly text inputs under the wash program selector.
3. Converts values (`","` accepted as decimal separator) to numbers via `moneyToUndef`.
4. Persists mapped values into `KundenWashUpsertFields` as `netto_preis` and `brutto_preis`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `wash_netto_preis` | string | No | Modal input value for net price |
| `wash_brutto_preis` | string | No | Modal input value for gross price |
| `netto_preis` | number | No | Persisted wash net price |
| `brutto_preis` | number | No | Persisted wash gross price |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kundenWash`
- **Schema Changes:** Added optional fields in model (`netto_preis`, `brutto_preis`)
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts`
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Values are optional and accept free-form numeric text; invalid numeric strings are ignored during payload mapping.

---

## FEATURE-042: Move Pricelist Reference Above Fuhrpark Column
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The `Preisliste Referenz` card was repositioned to the middle Waschanlage column so it appears directly above `Fuhrpark / Kennzeichen`, keeping pricing reference and fleet input in one visual workflow.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — moved existing `Preisliste Referenz` card from right column to middle column above `Fuhrpark / Kennzeichen`
- `docs/feature-logs/2026-03-27.md` — logged feature change entry
- `docs/FEATURE-LOG.md` — updated daily and master feature index

### What It Does (Technical)
1. Inserts the existing price-list link card before the Fuhrpark card in the middle column JSX block.
2. Removes the same card from the right `Wasch-Infos` column.
3. Preserves URL, styling, and icon usage unchanged.
4. Keeps form data/state behavior untouched.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `WASH_PRICE_LIST_PDF_URL` | string (URL) | Yes | External PDF reference URL, unchanged |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kundenWash`
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
This is a UI-position update only; behavior of pricing lookup and all input fields remains unchanged.

---

## FEATURE-041: Balanced Three-Column Waschanlage Layout + Pricelist PDF Reference
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The Waschanlage area in the new-customer modal was refined to a more balanced professional three-column desktop layout where column 1 and column 3 are equally weighted and the middle column remains tuned for Fuhrpark/Kennzeichen. A dedicated reference card with a direct link to the official DEMA Waschstrasse pricelist PDF was also added.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — updated desktop grid template and added a new `Preisliste Referenz` card with PDF link
- `docs/feature-logs/2026-03-27.md` — logged feature change entry
- `docs/FEATURE-LOG.md` — updated daily and master feature index

### What It Does (Technical)
1. Replaces the previous `xl:grid-cols-4` distribution with a custom 3-track grid: `1fr | minmax(340px, 1.15fr) | 1fr`.
2. Keeps left (`Bank & Zahlung`, `Konto`) and right (`Wasch-Infos`) columns symmetric in base weight.
3. Preserves middle column sizing emphasis for `Fuhrpark / Kennzeichen`.
4. Adds `WASH_PRICE_LIST_PDF_URL` constant and a styled external-link card above `Wasch-Infos`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `WASH_PRICE_LIST_PDF_URL` | string (URL) | Yes | Reference URL shown in Waschanlage card for pricing lookup |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kundenWash`
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
The grid weighting update is applied at `xl` breakpoint and above. The PDF is linked as an external reference and is not embedded inline.

---

## FEATURE-040: Three-Column Waschanlage Layout with Wide Wasch-Infos
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The Waschanlage section in the New Customer modal was rebalanced to a three-column desktop composition: a narrower left column for `Bank & Zahlung` and `Konto`, a middle column for `Fuhrpark / Kennzeichen`, and a wider right column for `Wasch-Infos`.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — changed desktop grid from 3-column to 4-track layout and reassigned column spans (`1 + 1 + 2`) for left/middle/right groups
- `docs/feature-logs/2026-03-27.md` — logged feature change entry
- `docs/FEATURE-LOG.md` — updated daily and master feature index

### What It Does (Technical)
1. Updates the Waschanlage grid wrapper to `xl:grid-cols-4`.
2. Shrinks the left block (`Bank & Zahlung`, `Konto`) to `xl:col-span-1`.
3. Keeps `Fuhrpark / Kennzeichen` isolated in the middle at `xl:col-span-1`.
4. Moves `Wasch-Infos` into its own right block at `xl:col-span-2` for wider input space.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Layout-only change; no data-contract change |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kundenWash`
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
This width optimization applies at `xl` breakpoint and above; smaller screens continue to stack sections vertically for readability.

---

## FEATURE-039: Reorder Waschanlage Cards (Bank First, Konto Second)
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The left-side card order in the New Customer Waschanlage section was updated so `Bank & Zahlung` appears first and `Konto` appears second, matching the requested business workflow.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — swapped visual order of `Bank & Zahlung` and `Konto` cards in the Waschanlage left column
- `docs/feature-logs/2026-03-27.md` — logged feature change entry
- `docs/FEATURE-LOG.md` — updated daily and master feature index

### What It Does (Technical)
1. Moves the `Bank & Zahlung` card block above the `Konto` card block in JSX order.
2. Keeps all state bindings and field names unchanged.
3. Preserves payload mapping and persistence behavior.
4. Changes only render order and user flow.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | UI order change only; no contract changes |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kundenWash`
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
This update affects only the visual sequence in the create modal; edit flows in other screens remain unchanged unless adjusted separately.

---

## FEATURE-038: Remove Rechnungsadresse Section from New Customer Waschanlage Tab
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The `Rechnungsadresse` section in the Waschanlage area of the new customer modal was removed so users no longer see or fill billing-address fields in `Kundendaten (Neu)`.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — removed the `Rechnungsadresse` card UI from the Waschanlage tab and updated helper text
- `docs/feature-logs/2026-03-27.md` — logged feature change entry
- `docs/FEATURE-LOG.md` — updated daily and master feature index

### What It Does (Technical)
1. Deletes the entire Waschanlage card block containing `wash_rechnung_*` inputs.
2. Keeps form state shape and payload function intact for backward compatibility.
3. Updates the inactive-profile helper message to remove `Rechnungsadresse` wording.
4. Leaves all other Waschanlage sections (`Konto`, `Bank & Zahlung`, `Fuhrpark`, `Wasch-Infos`) unchanged.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `wash_rechnung_*` | string | No | Billing address fields remain in form model but are no longer exposed in create UI |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kundenWash`
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
This change removes the create-modal UI only; existing records that already contain billing-address values are not migrated or deleted.

---

## FEATURE-037: Remove Rechnungsadresse Address Tab in New Customer Modal
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The address-type tab options inside the new customer flow were simplified by removing `Rechnungsadresse` from the selectable address tabs. This ensures the customer data area does not show or auto-seed that tab option when users add additional addresses.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — removed `Rechnungsadresse` from `ADRESSE_TYPEN` so the tab list no longer includes that type
- `docs/feature-logs/2026-03-27.md` — logged feature change entry
- `docs/FEATURE-LOG.md` — updated daily and master feature index

### What It Does (Technical)
1. Updates the `ADRESSE_TYPEN` constant by deleting the `Rechnungsadresse` label.
2. Keeps existing address tab rendering logic unchanged, now driven by the reduced type list.
3. Changes default type assignment for newly added extra addresses (second slot becomes `Lieferadresse`).
4. Preserves all payload and persistence behavior for address fields.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `adressen[].typ` | string | Yes | Address type label used for tab/pill rendering and selection |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kunden`
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Existing saved customers that already have `Rechnungsadresse` as a stored `typ` value may still display that value when loaded; this change removes it from new selection defaults/options in the new-customer modal.

---

## FEATURE-035: Unified Waschprogramm Dropdown with Expanded Tariff List
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Extended

### What Was Added
The wash program control was standardized across create and edit flows into a visible dropdown selection and expanded with a detailed tariff list (code + description format), matching the legacy-style selection behavior more closely.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — replaced generic wash program options with an expanded tariff option list
- `frontend/src/pages/CustomersPage.tsx` — changed wash program field from free text suggestions to dropdown select and reused the expanded tariff list

### What It Does (Technical)
1. Keeps a single selection field (`wasch_programm`) in the form model.
2. Replaces short placeholder options (`Standard`, `Premium`, etc.) with detailed coded options (`S*`, `1*`, `2*`, ...).
3. Uses `<select>` in both NewCustomerModal and CustomersPage so option visibility is explicit.
4. Leaves persistence model unchanged (`wasch_programm` stays a string field).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| wasch_programm | string | No | Selected tariff/program entry from dropdown |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kundenWash`
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Tariff labels currently live as static option text in the frontend and are not yet loaded from a backend tariff catalog.

---

## FEATURE-034: Single Waschanlage Selection Field (Program Only)
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The Waschanlage UI was simplified from two separate selection fields to one: users now select only `Waschprogramm / Tarif`. The separate `Fahrzeugtyp` field was removed to match the requested single-choice workflow.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — removed `Fahrzeugtyp` select from the Waschanlage card, kept only `Waschprogramm / Tarif`
- `frontend/src/pages/CustomersPage.tsx` — removed `Fahrzeugtyp` input from the customer detail Waschanlage editor and adjusted section label

### What It Does (Technical)
1. Removed UI rendering for `wasch_fahrzeug_typ` in create and edit flows.
2. Removed `wasch_fahrzeug_typ` from NewCustomerModal form state and payload mapping.
3. Removed `wasch_fahrzeug_typ` from detail-save upsert payload in CustomersPage.
4. Kept `wasch_programm` as the single active selection field.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| wasch_programm | string | No | Single selected wash program / tariff |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kundenWash`
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Existing historical records may still contain `wasch_fahrzeug_typ`; the UI no longer exposes this field for editing.

---

## FEATURE-033: Separate Konto and Rechnungsadresse Cards
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Extended

### What Was Added
The combined "Konto & Rechnung" section in the Waschanlage tab was split into two independent cards: one dedicated to account controls (`Konto`) and one dedicated to billing address data (`Rechnungsadresse`). This improves readability and makes the form structure clearer.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` - split the merged left-side card into two separate cards within the existing two-panel layout

### What It Does (Technical)
1. Renamed the original card heading from `Konto & Rechnung` to `Konto`.
2. Kept only `BUKto`, `Limit`, and `Kunde gesperrt` fields inside the `Konto` card.
3. Moved all `Rechnungsadresse` fields (`Rechnung-Zusatz`, `PLZ`, `Ort`, `Strasse`) into a new standalone card directly below.
4. Preserved all existing state bindings, suggestions, and payload behavior unchanged.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | UI grouping change only; no data contract changes |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** None
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
This is a visual and structural grouping update only; validation and save behavior remain identical.

---

## FEATURE-032: Professional Waschanlage Two-Panel Composition
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Extended

### What Was Added
The Waschanlage tab was redesigned into a cleaner professional composition: a broad left panel for finance and billing data, and a focused right panel for fleet and wash controls. This removes the awkward empty space from the previous layout and makes the screen feel balanced and enterprise-ready.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — replaced flat multi-card grid with a structured two-panel layout (`xl` 3-column split: left spans 2 columns, right spans 1 column)

### What It Does (Technical)
1. Wraps `Konto & Rechnung` and `Bank & Zahlung` in a left vertical group (`xl:col-span-2`).
2. Wraps `Fuhrpark / Kennzeichen` and `Wasch-Infos` in a right vertical group (`xl:col-span-1`).
3. Keeps `Fuhrpark / Kennzeichen` dynamic height (`self-start`) while maintaining stable spacing between all cards.
4. Preserves full responsive fallback on smaller screens (single-column stacking).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | UI composition change only; no data contract changes |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** None
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
The layout is optimized for desktop clarity at `xl` breakpoints; smaller breakpoints intentionally keep a linear stack for readability.

---

## FEATURE-031: Responsive Waschanlage Card Layout and Dynamic Kennzeichen Height
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Extended

### What Was Added
The Waschanlage tab layout was refined so the Fuhrpark / Kennzeichen card no longer stretches to the full height of neighboring cards. The section now behaves dynamically (grows only as needed) and uses a responsive 3-column layout on larger screens so Wasch-Infos appears earlier and remains more visible without excessive scrolling.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` - updated Waschanlage grid alignment and ordering classes for dynamic card height and improved responsive placement

### What It Does (Technical)
1. Changed the Waschanlage container grid to `items-start` so grid items do not stretch vertically to the tallest card.
2. Added `xl:grid-cols-3` to improve desktop information density.
3. Added `self-start` to the Fuhrpark / Kennzeichen card so it keeps intrinsic height.
4. Reordered card display so Wasch-Infos appears before Bank & Zahlung in layout flow on responsive breakpoints.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | UI layout-only adjustment; no data contract changes |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** None
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
The 3-column layout is active on larger breakpoints only; smaller screens remain stacked for readability.

---

## FEATURE-025: Solo IT Delivery Operating System
**Date:** 2026-03-27  
**Author/Agent:** Cursor AI  
**Status:** Added  

### What Was Added
A complete solo-delivery operating system was introduced to standardize daily planning, backlog control, learning cadence, and production-readiness gates across the project.

### Where It Was Added
- `.cursor/rules/time-management-ops.mdc` — operational execution rule for daily cadence and release gates
- `todo/README.md` — daily execution guidance and status model
- `todo/templates/daily-task-template.md` — repeatable daily planning template
- `todo/backlog.md` — source-of-truth backlog with workflow states
- `todo/2026-03-27.md` — seeded daily priorities and work blocks
- `learner/README.md` — linked learning workflow for delivery execution
- `learner/templates/daily-learning-template.md` — repeatable daily learning template
- `learner/learning-roadmap.md` — roadmap tied to backlog IDs
- `learner/2026-03-27.md` — seeded daily learning session
- `docs/solo-it-operating-model.md` — end-to-end operating model and governance details

### What It Does (Technical)
1. Enforces a daily startup protocol for priorities, risks, blockers, and backlog deltas.
2. Standardizes execution into time-boxed focus blocks with explicit WIP limits.
3. Keeps `todo/backlog.md` as the single source of truth for status transitions.
4. Adds release readiness checks (tests, lint, rollback, monitoring, risks).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| date | string (YYYY-MM-DD) | Yes | Daily todo/learning file identity |
| taskId | string | Yes | Unique task identifier |
| status | enum | Yes | `todo`, `in_progress`, `blocked`, `done`, `dropped` |
| owner | string | Yes | Responsible actor (`you` or `agent`) |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Execution quality still depends on consistent manual discipline and periodic backlog hygiene.

---

## FEATURE-026: Weekday-Only Office Cadence for Solo Operating System
**Date:** 2026-03-27  
**Author/Agent:** Cursor AI  
**Status:** Extended  

### What Was Added
The operating system was extended to run mandatory daily planning and learning only on Monday-Friday, with weekend activity defaulting to optional review unless explicitly requested.

### Where It Was Added
- `.cursor/rules/time-management-ops.mdc` — added weekday-only daily file creation and weekend mode behavior
- `todo/README.md` — aligned execution guidance to office-day cadence
- `learner/README.md` — aligned learning cadence to weekdays
- `docs/solo-it-operating-model.md` — documented weekday cadence and weekend exception policy

### What It Does (Technical)
1. Checks whether the current day is a workday before requiring new daily files.
2. Skips mandatory sprint-load planning on weekends by default.
3. Preserves explicit urgent overrides when weekend work is necessary.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| workdayPolicy | string | Yes | Office cadence policy (`Monday-Friday`) |
| currentDay | string | Yes | Day-of-week used for behavior branching |
| urgentFlag | boolean | No | Explicit override for weekend execution |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Weekend exception handling remains process-driven and requires manual logging discipline.

---

## FEATURE-027: Hide VAT Backend Raw JSON Panel in Customer Modal
**Date:** 2026-03-27  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
Removed the backend debug response panel from the VAT tab in the new-customer modal, including the text block that described raw `vies_raw` payload visibility.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — removed rendered backend JSON panel and associated temporary state/write paths

### What It Does (Technical)
1. Deletes the conditional UI block that rendered "Server-Antwort (komplettes JSON vom Backend)".
2. Removes the explanatory note about `vies_raw` in the same panel.
3. Cleans up no-longer-needed local state mutations used only for that panel.
4. Preserves VAT validation behavior and success/error messaging for normal user flow.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `country_code` | string | Yes | VAT country code sent to backend |
| `vat_number` | string | Yes | VAT number sent to backend |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| POST | `/api/v1/vat/check` | None | `{ country_code, vat_number }` | Existing VAT check response schema |

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Raw backend payload visibility is no longer available in the modal UI; troubleshooting now relies on network/devtools logs.

---

## FEATURE-028: Daily Report Folder and Auto Daily Reporting Rule
**Date:** 2026-03-27  
**Author/Agent:** Cursor AI  
**Status:** Added  

### What Was Added
A new `Daily report` workspace folder and an always-applied Cursor rule were added to standardize professional end-of-day reporting. The workflow creates one dated file per workday and tracks completed work, concrete changes, issues faced, mitigations, blockers, and carry-forward actions.

### Where It Was Added
- `Daily report/2026-03-27.md` — first daily report file with professional reporting structure
- `.cursor/rules/daily-report-ops.mdc` — always-on automation rule for per-day report creation and reporting standards

### What It Does (Technical)
1. Defines a fixed report path format: `Daily report/YYYY-MM-DD.md`.
2. Enforces weekday creation behavior (Monday-Friday) with weekend creation only on explicit request.
3. Provides mandatory reporting sections for outcomes, changes, issues, mitigations, blockers, and next actions.
4. Standardizes status vocabulary using `todo`, `in_progress`, `blocked`, `done`, and `dropped`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| date | string (YYYY-MM-DD) | Yes | Report file identity |
| status | enum | Yes | Daily progress state |
| priorityId | string | No | Link to task or priority item |
| issue | string | No | Risk/problem encountered during execution |
| escalationOwner | string | No | Responsible person when blocked |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Report content quality still depends on disciplined daily updates; creation is rule-driven but narrative accuracy is manual.

---

## FEATURE-029: Consolidate Todo, Learner, and Daily Report Under `vedant/`
**Date:** 2026-03-27  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The work-management structure was consolidated into a single parent folder named `vedant` so all operational files live in one place. Existing `todo`, `learner`, and `Daily report` content was moved under `vedant/`, and active Cursor rules were updated to use the new paths for daily automation.

### Where It Was Added
- `vedant/todo/2026-03-27.md` — moved daily todo file
- `vedant/todo/backlog.md` — moved backlog source of truth
- `vedant/todo/README.md` — moved todo operating guide
- `vedant/todo/templates/daily-task-template.md` — moved todo template
- `vedant/learner/2026-03-27.md` — moved daily learning file and updated backlog path reference
- `vedant/learner/learning-roadmap.md` — moved learning roadmap
- `vedant/learner/README.md` — moved learner operating guide
- `vedant/learner/templates/daily-learning-template.md` — moved learning template
- `vedant/Daily report/2026-03-27.md` — moved daily report file
- `.cursor/rules/time-management-ops.mdc` — updated paths to `vedant/todo` and `vedant/learner`
- `.cursor/rules/daily-report-ops.mdc` — updated report path to `vedant/Daily report`

### What It Does (Technical)
1. Re-homes three operational domains (`todo`, `learner`, `Daily report`) under one parent directory.
2. Preserves all file contents while changing file locations.
3. Updates rule-based automation references so new daily files are created and read from `vedant/` paths.
4. Removes old duplicate files from legacy top-level folders to avoid drift.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| baseFolder | string | Yes | Parent container for operational files (`vedant`) |
| todoPath | string | Yes | Daily planning and backlog location |
| learnerPath | string | Yes | Daily learning and roadmap location |
| reportPath | string | Yes | Daily professional report location |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Path migration is complete for these operational folders; any external scripts or bookmarks still pointing to old top-level paths should be updated manually.

---

## FEATURE-029: Remove VIES Hint and Customer Table-Name Labels from UI
**Date:** 2026-03-27  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
Removed additional technical/internal UI text from customer-facing screens, including the VAT hint sentence requested and table-name references (`kunden`, `kunden_wash`) in customer form copy.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — removed VAT hint note and replaced table-name specific helper text with user-facing wording
- `frontend/src/contexts/LanguageContext.tsx` — updated `customersDeptHint` translations to remove table-name references

### What It Does (Technical)
1. Deletes the VAT hint paragraph in the VAT tab that referenced temporary VIES load behavior and manual adjustment guidance.
2. Simplifies modal subtitle text by removing inline table-name labels.
3. Rewrites wash-profile helper text to avoid exposing backend table names in UI copy.
4. Updates localized `customersDeptHint` strings in DE/EN/FR/IT language maps to keep wording consistent.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `customersDeptHint` | string | Yes | Localized UI helper text shown in Customers page header |
| `includeWashProfile` | boolean | No | Existing form toggle; behavior unchanged, only explanatory text updated |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
This change updates display text only; customer save behavior and backend contracts remain unchanged.

---

## FEATURE-030: Professional Fuhrpark / Kennzeichen List UI
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The single-text Kennzeichen input in both the NewCustomerModal (Waschanlage tab) and the CustomersPage detail view was replaced with a professional "Fuhrpark / Kennzeichen" card. Users can now type a plate number, press Enter or click "+", and it appears in a clean vertical list. Each plate has a red trash icon for removal. The Waschanlage tab was also reorganized into three clearly separated cards: "Konto & Rechnung", "Fuhrpark / Kennzeichen", "Bank & Zahlung", and "Wasch-Infos" for a cleaner, more professional layout.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — replaced single Kennzeichen input with multi-plate list UI; rearranged Waschanlage tab into 4 logical card sections; changed form state from single string to array
- `frontend/src/pages/CustomersPage.tsx` — replaced single Kennzeichen input in detail drawer with same professional Fuhrpark / Kennzeichen list card; added `newPlateInput` local state

### What It Does (Technical)
1. `initialForm()` now stores `wash_kennzeichen_list: string[]` and `wash_kennzeichen_new: string` instead of a single `wash_kennzeichen` string.
2. The "+" button and Enter key handler add the trimmed uppercase plate to the array if not already present.
3. Each plate in the list renders with a Trash2 icon button that removes it by index.
4. `washFormToPayload()` joins the array with `, ` back into a single comma-separated string for the backend `kennzeichen` field (backward compatible).
5. In CustomersPage, the existing `draftWash.kennzeichen` string is split on commas for display and re-joined on mutation.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| wash_kennzeichen_list | string[] | No | Array of plate numbers in form state |
| wash_kennzeichen_new | string | No | Current text in the new-plate input |
| kennzeichen | string | No | Comma-separated plates sent to backend |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kundenWash` (same `kennzeichen` string field)
- **Schema Changes:** None (backward compatible comma-separated format)
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes (via existing localStorage persistence)

### i18n Keys Added
None (placeholder strings are German-contextual UI hints, not user-facing translatable text yet).

### Dependencies Added
None.

### Notes / Known Limitations
- Plate deduplication is case-sensitive after uppercase normalization.
- The backend `kennzeichen` field is still a single string; if the API changes to an array, the join logic in `washFormToPayload` must be updated.

---

## FEATURE-036: VIES Request Date Applied to New Customer Aufnahme
**Date:** 2026-03-27
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The VIES result flow now carries the VIES request date together with trader name and address into the new-customer form. When a valid VIES response is applied, the customer `Aufnahme` field is updated from `request_date` so the captured timestamp reflects the VIES check event.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — added request-date normalization, wired `request_date` into form state/payload, and rendered `requestDate` in the VIES result panel

### What It Does (Technical)
1. Adds `aufnahme` to modal form state and includes it in `formToPayload`.
2. Initializes `aufnahme` with current timestamp on modal open and keeps preview in sync.
3. Normalizes VIES `request_date` into local display format when possible.
4. Applies normalized VIES date to `aufnahme` when user clicks "Daten ins Formular übernehmen".
5. Shows `requestDate` in the VIES result details beside other response metadata.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| request_date | string \| null | No | VIES response timestamp used to seed customer `aufnahme` |
| aufnahme | string | No | Customer intake datetime persisted in `kunden` row |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** `kunden`
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| POST | `/api/v1/vat/check` | None | `{ country_code, vat_number }` | Uses existing `request_date` in response payload |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (existing `aufnahme` handling reused)
- **Actions/Selectors added:** None
- **Persisted:** Yes

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
If VIES returns a non-parseable date string, the raw trimmed value is kept as-is to avoid dropping source data.

---
