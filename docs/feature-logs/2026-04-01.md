# Feature Log — 2026-04-01

> Features added or modified on this date.

---

## FEATURE-134: Bulgarian VIES address — `Gr.` / `гр.` city suffix after comma
**Date:** 2026-04-01
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
VIES addresses such as `… Obl.Plovdiv, Gr.Plovdiv` (Bulgarian *Grad* = city) now split into street and city: the part after the last comma is recognised when it starts with `Gr.` or Cyrillic `гр.`, and the city name is stored without that prefix. Multiline addresses whose last line is only `Gr.City` are handled the same way.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — `tryGradAbbrevTailCity`, `parseViesSingleLineAddress`, `parseViesMultilineAddressBody`

### What It Does (Technical)
1. After a comma/semicolon split, if `tryPostalAndCity` fails on the right segment, `tryGradAbbrevTailCity` tests `^Gr\.?\s*(.+)$` and `^гр\.?\s*(.+)$` (Unicode flag).
2. Multiline: if the last non-country line matches that pattern, preceding lines become `strasse` and the captured token becomes `ort`.
3. Numeric PLZ is unchanged: if the registry string contains no `NNNN City` pattern, `plz` can remain empty (member-state dependent).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| address (VIES) | string | No | May contain `, Gr.City` or `, гр. …` tail |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
`Obl.Plovdiv` (oblast) stays in the street line; only the `Gr.*` tail is promoted to city. If Bulgaria returns a 4-digit post code elsewhere in the string, existing numeric patterns still apply; this sample had no separate PLZ token.

---

## FEATURE-135: Address transliteration (BG Cyrillic) — no bogus “Jk” / title-case mangling
**Date:** 2026-04-01
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
Address fields use a dedicated pipeline: Bulgarian Cyrillic abbreviations (`жк`, `обл.`, `гр.`, `бл.`, `вх.`, `ет.`, `ап.`, etc.) map to stable Latin (`Zh.K.`, `obl.`, `gr.`, …) before generic transliteration, then scientific BG letter mapping so `ЖК` is not rendered as `Jk`. VIES-derived street/city/plz no longer run through title-case (which broke `bl.70`, `Zh.K.`, etc.). VAT name/address blocks use `translate="no"` / `notranslate` to reduce browser auto-translate turning `obl.`/`gr.` into words like “Region”/“Stadt”. Backend skips unidecode on `BG` VIES display text like `DE`, preserving registry Cyrillic until the frontend normalizes.

### Where It Was Added
- `frontend/src/common/utils/addressTransliterate.ts` — new
- `frontend/src/common/utils/addressLatinNormalize.ts` — uses `transliterateAddressToAscii`
- `frontend/src/components/NewCustomerModal.tsx` — VIES UI + parsing merge (restored multiline/PLZ/nested raw + Gr. handling), `looksAdministrativeAreaText` uses address transliteration
- `backend/main.py` — `_normalize_vies_display_text` preserves `BG` like `DE`

### What It Does (Technical)
1. `expandCyrillicAddressAbbreviations` then `mapBulgarianCyrillicToLatin`, then existing `transliterateToAscii` for any remaining scripts.
2. `normalizeLatinAddressLine` calls the address-specific pipeline.
3. VIES parse outputs use `normalizeLatinAddressLine` only (no `toTitleCaseLatin`).
4. `normalizeVatAddressForUi` uses `transliterateAddressToAsciiMultiline` without per-line title case.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| Address strings | string | No | Cyrillic or Latin EU register text → consistent ASCII/Latin for form storage |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Toponym romanisation follows the scientific BG table (`я`→`ya`, etc.); anglicised spellings (e.g. “Trakia” vs “Trakiya”) are not special-cased. If VIES returns a different address than the trader’s real doorplate, that remains a registry limitation.

---

## FEATURE-136: Customer acquisition source (dropdown + source detail)
**Date:** 2026-04-01
**Author/Agent:** Cursor AI
**Status:** Added

### What Was Added
New/Edit customer modal now includes an **acquisition source** dropdown (referral, website, email, call) directly under the company name, plus an optional **source detail** text field with placeholder text explaining that it will later hold the specific entity name for the selected channel (e.g. referrer or site). Values persist on the customer record and appear in change history.

### Where It Was Added
- `frontend/src/types/kunden.ts` — `acquisition_source`, `acquisition_source_entity` on `KundenStamm`
- `frontend/src/components/NewCustomerModal.tsx` — form state, payload, history display labels, UI row
- `frontend/src/store/kundenStore.ts` — `TRACKED_FIELDS` for history diffs
- `frontend/src/contexts/LanguageContext.tsx` — `MESSAGES.de` / `.en` / `.fr` keys

### What It Does (Technical)
1. `acquisition_source` stores one of `referral` | `website` | `email` | `call`, or is omitted when unset.
2. `acquisition_source_entity` is optional free text; transliteration on blur matches other single-line fields.
3. `formatHistoryValueDisplay` maps stored source codes to translated labels in the History tab.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| acquisition_source | string | No | `referral`, `website`, `email`, or `call` |
| acquisition_source_entity | string | No | Future: specific referrer/site/campaign name |

### Database
- **Engine:** Browser `localStorage` (`dema-kunden-db`)
- **Tables Affected:** `kunden` rows in `KundenDbState`
- **Schema Changes:** Two new optional fields on customer objects in persisted JSON
- **Key Queries:** None

### API Endpoints (if applicable)
N/A.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts`
- **Actions/Selectors added:** None — fields flow through `createKunde` / `updateKunde` via spread
- **Persisted:** Yes

### i18n Keys Added
`newCustomerLabelAcquisitionSource`, `newCustomerAcquisitionSourceNone`, `newCustomerAcquisitionReferral`, `newCustomerAcquisitionWebsite`, `newCustomerAcquisitionEmail`, `newCustomerAcquisitionCall`, `newCustomerLabelAcquisitionSourceEntity`, `newCustomerAcquisitionSourceEntityPh`, `historyFieldAcquisitionSource`, `historyFieldAcquisitionSourceEntity`

### Dependencies Added
None.

### Notes / Known Limitations
Source values are fixed English tokens for stable storage and future DB alignment (`acquisition_source` in Customer 360 schema). The detail field does not auto-fill from the dropdown yet.

---

## FEATURE-137: New customer modal — layout + DB-aligned fields
**Date:** 2026-04-01
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
Customer & Address tab layout is mobile-first (single column then two columns from `sm:`), with **customer type** and **account status** on the company card, **first/last name**, **profile notes** vs **operational notes**, **website** on the company card (removed from the address card). **Type / account** tab no longer duplicates type/status (hint points to Customer & Address). **Marketing score** moved under the marketing block; finance BIC/IBAN use i18n labels. **Contacts** gain optional **website**; **Car wash** tab adds **vehicle type** (`wasch_fahrzeug_typ`). Footer stacks on small screens. Data fix: `profile_notes` and `first_name`/`last_name` are no longer incorrectly derived from `bemerkungen` / split company name.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — form state, payload, UI sections
- `frontend/src/types/kunden.ts` — optional `website` on `KundenKontakt`
- `frontend/src/contexts/LanguageContext.tsx` — `MESSAGES.de` / `.en` / `.fr` keys

### What It Does (Technical)
1. `formToPayload` uses `form.first_name`, `form.last_name`, `form.profile_notes` explicitly.
2. `wash_vehicle_type` maps to `wasch_fahrzeug_typ` in wash upsert.
3. Contact rows persist `website` via `normalizeKontaktEntriesForPayload`.

### Data It Accepts / Emits
| Field | Type | Description |
|----|---|----|
| first_name, last_name, profile_notes | string | Customer details / profile notes |
| internet_adr | string | Company website |
| kontakte[].website | string | Per-contact website |
| wasch_fahrzeug_typ | string | Wash vehicle type |

### Database
- **Engine:** Browser `localStorage`
- **Schema Changes:** Optional fields on JSON customer/kontakt/wash shapes

### API Endpoints (if applicable)
N/A.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts`
- **Persisted:** Yes (existing persist paths)

### i18n Keys Added
Multiple `newCustomer*` and `historyField*` keys for customer type, status, names, profile/operational notes, contact website, BIC/IBAN, vehicle type, art-tab hint.

### Dependencies Added
None.

### Notes / Known Limitations
Contact role dropdown options remain partially hardcoded German labels in JSX (pre-existing); can be migrated to `t()` in a follow-up.

**Extended:** Address card again shows **VAT ID** full width, **Tax no.** + **Website** in one row (matches legacy layout / DBML address + `customers.website`), plus **Industry reg. no.**; company column no longer duplicates website.

---

## FEATURE-138: Remove duplicate VAT & tax from Type / account tab
**Date:** 2026-04-01
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **Type / account** tab no longer shows a second **VAT & tax** panel (VAT ID, tax no., industry reg. no., tax country/type code). Those identifiers are edited only on **Customer & Address**, where VAT ID and tax no. already sit with the address. **Tax country/type code** (`tax_country_type_code`, stamm-level) is now edited on the Customer & Address address column after industry reg. no., so the field is not lost when the duplicate block is removed.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — removed right-column VAT section from `tab === "art"`; added `tax_country_type_code` input in Customer & Address column

### What It Does (Technical)
1. `tab === "art"` renders billing + marketing only in the grey card; no `patchAdresse` IIFE for VAT fields.
2. `form.tax_country_type_code` uses existing `set("tax_country_type_code", …)` next to per-address `branchen_nr`, `ust_id_nr`, `steuer_nr`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| ust_id_nr, steuer_nr, branchen_nr | string | No | Per-address; UI only on Customer & Address |
| tax_country_type_code | string | No | Customer stamm field; UI on Customer & Address |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
N/A.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** Yes (existing customer payload paths)

### i18n Keys Added
None (reuses `newCustomerLabelTaxCountryTypeCode`, `newCustomerTaxCountryTypePh`).

### Dependencies Added
None.

### Notes / Known Limitations
`newCustomerLegalVatTitle` remains in `LanguageContext` for possible reuse; it is no longer referenced by this modal tab.

---

## FEATURE-139: Phase 1B — Customer 360 modal (summary, grouping, timeline, dirty state)
**Date:** 2026-04-01
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The customer create/edit modal behaves more like a **Customer 360** in edit mode: a compact **summary strip** (customer number, status, VAT check snapshot, profile completion %, risk hints, last saved), clearer **section hierarchy** on Customer & Address (master data, addresses & tax, contacts, related & operational column), a more readable **history timeline** (before/after grid), and clearer **unsaved / validation** messaging in the footer. Create flow and tab purposes are unchanged; styling stays business-appropriate.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — summary strip, section headings, history layout, `dirtyBaselineRef` + serialized dirty compare, footer rows for unsaved/errors, inline validation for required company name
- `frontend/src/contexts/LanguageContext.tsx` — `customer360*`, `historyTimeline*`, `historyColField` / `historyColBefore` / `historyColAfter` (de, en, fr)
- `docs/product/customer-flow.md` — Phase 1B flow and screen semantics
- `docs/architecture/overview.md` — “Customer UI (Phase 1B)” under current runtime shape
- `docs/progress/weekly-progress.md` — Phase 1B completion notes

### What It Does (Technical)
1. On open in edit mode, form state is serialized to a string baseline; `isFormDirty` compares the current serialized state to that baseline.
2. Summary chips read from `editInitial`, `form`, `vatCheckResult`, and `computeCustomerProfileCompletionPct`; risk uses status flags and a simple billing completeness check (e.g. empty IBAN+BIC).
3. History tab adds intro copy and a responsive CSS grid for field / before / after columns.
4. Modal close clears dirty baseline ref; validation errors use `footerMessage` above primary actions.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | No new API payloads; UI-only summaries and client dirty detection |

### Database
- **Engine:** Browser `localStorage` (existing customer persistence paths)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No (baseline is in-memory per modal open)

### i18n Keys Added
`customer360SummaryStripTitle`, `customer360ChipKuNr`, `customer360ChipStatus`, `customer360ChipVat`, `customer360VatValid`, `customer360VatInvalid`, `customer360VatUnknown`, `customer360ChipCompletion`, `customer360ChipRisk`, `customer360RiskBlocked`, `customer360RiskPayment`, `customer360RiskBilling`, `customer360RiskLow`, `customer360ChipLastEdited`, `customer360UnsavedChanges`, `customer360SectionMaster`, `customer360SectionMasterHint`, `customer360SectionAddressTax`, `customer360SectionAddressTaxHint`, `customer360SectionContacts`, `customer360SectionContactsHint`, `customer360OperationalContext`, `customer360OperationalContextHint`, `historyTimelineTitle`, `historyTimelineHint`, `historyColField`, `historyColBefore`, `historyColAfter`

### Dependencies Added
None.

### Notes / Known Limitations
Risk and completion percentages are **heuristic** and for orientation only. VAT chip reflects the in-session VIES result, not a persisted server flag unless already mapped elsewhere.

---

## FEATURE-140: Phase 2 — customer frontend repository layer & feature module
**Date:** 2026-04-01
**Author/Agent:** Cursor AI
**Status:** Added

### What Was Added
Customer-facing UI is routed through a **frontend repository facade** (`customerRepository`) so pages and the modal no longer need to import `kundenStore` for the main data path. A **`frontend/src/features/customers/`** module holds the repository, mappers (form ↔ persist / VAT DTO), validators, `useCustomerForm`, explicit type aliases for persisted vs draft vs API shapes, and a **reusable history timeline** component. Phase 1B behavior (tabs, summary strip, dirty detection, VAT flow) is preserved; storage remains `kundenStore` under the repository.

### Where It Was Added
- `frontend/src/features/customers/repository/customerRepository.ts` — facade over load/save, CRUD, history, wash/risk helpers; re-exports input/history types and boolean history field sets
- `frontend/src/features/customers/mappers/customerFormMapper.ts` — form draft, initial state, dirty serialization, completion %, payloads
- `frontend/src/features/customers/mappers/customerFormConstants.ts` — shared country/EU constants for forms
- `frontend/src/features/customers/mappers/vatCheckCoercion.ts`, `mappers/vatCheckSnapshotMapper.ts` — VAT check normalization
- `frontend/src/features/customers/types/persistedCustomer.ts` — aliases for persisted aggregates
- `frontend/src/features/customers/types/customerApiDto.ts` — `VatCheckResponseDto`
- `frontend/src/features/customers/validators/customerValidation.ts` — shared validation helpers
- `frontend/src/features/customers/hooks/useCustomerForm.ts` — form hook for incremental adoption
- `frontend/src/features/customers/utils/historyFieldDisplay.ts` — history value display (used by timeline)
- `frontend/src/features/customers/components/CustomerHistoryTimeline.tsx` — history tab UI
- `frontend/src/features/customers/components/CustomerModalFrame.tsx`, `CustomerModalHeader.tsx` — optional shell pieces for future splits
- `frontend/src/features/customers/index.ts` — barrel exports
- `frontend/src/pages/CustomersPage.tsx` — uses `customerRepository` instead of direct store calls
- `frontend/src/components/NewCustomerModal.tsx` — repository types; history tab uses `CustomerHistoryTimeline`
- `docs/architecture/overview.md` — Phase 2 subsection
- `docs/progress/weekly-progress.md` — Phase 2 milestone notes

### What It Does (Technical)
1. UI calls `customerRepository.listRows`, `loadLocalDb`, `saveLocalDb`, `createKunde`, `updateKunde`, etc.; the repository forwards to existing `kundenStore` implementations.
2. Feature mappers and validators centralize transformations that were scattered; the modal can adopt them gradually (`useCustomerForm` available).
3. History rendering and field-value formatting live in the feature folder; the modal composes `<CustomerHistoryTimeline />` instead of inlined JSX.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| Same as Phase 1 | `KundenDbState`, `NewKundeInput`, wash upsert fields | — | No schema change; repository is a call wrapper |

### Database
- **Engine:** Browser `localStorage` / optional shared demo API (unchanged)
- **Tables Affected:** N/A (same JSON document model in `kundenStore`)
- **Schema Changes:** None
- **Key Queries:** N/A

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|-----|---|---|----|----|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (implementation detail behind `customerRepository`)
- **Actions/Selectors added:** None in store; new facade in `customerRepository`
- **Persisted:** Yes — unchanged persistence path

### i18n Keys Added
None (reuses existing history and customer keys).

### Dependencies Added
None.

### Notes / Known Limitations
`NewCustomerModal.tsx` remains large; further decomposition into feature tab components is recommended as a follow-up slice. Not every consumer of customer data has been switched to the repository yet.

---

## FEATURE-141: Edit customer — invoices list + open Rechnungen filtered by KundenNr
**Date:** 2026-04-01
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
Under **Customer & Address**, the **Invoices** block lists rows from the local Rechnungen DB matching the customer number. Two actions (aligned with FEATURE-058/059 split: list vs. create): **Show invoices** opens **Rechnungen** with **KundenNr** pre-filled; **New invoice** opens the same page with the same filter **and** the **Neue Rechnung** drawer (`sessionStorage` flags).

### Where It Was Added
- `frontend/src/pages/CustomersPage.tsx` — `rechnungenRowsForCustomer`, `openRechnungenForCustomer`, `formatRechnungDatumShort`, `dema-rechnungen-db-changed` listener; `editAdresseExtraContent` table + wired button
- `frontend/src/pages/RechnungenPage.tsx` — reads `dema-rechnungen-filter-kunden-nr` and `dema-rechnungen-open-new-invoice` on mount
- `frontend/src/contexts/LanguageContext.tsx` — `customersInvoicesEmpty`, `customersInvoicesNew`, `commonValueEmpty` (DE/EN/FR)

### What It Does (Technical)
1. `loadRechnungenDb().rows` filtered where `kunden_nr.trim()` equals the edited customer’s `kunden_nr`.
2. **Show invoices:** sets `dema-rechnungen-filter-kunden-nr`, navigates to `#/{department|sales}/rechnungen`, closes edit modal.
3. **New invoice:** sets the same KundenNr key plus `dema-rechnungen-open-new-invoice=1`, then navigates.
4. `RechnungenPage` `useEffect` applies stored KundenNr, then opens the new-invoice drawer if the flag is set, then removes both keys.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| draftKunde.kunden_nr | string | Yes | Match key for invoice rows |

### Database
- **Engine:** `rechnungenStore` local list (`dema-rechnungen-db`)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** Filter by `kunden_nr`

### API Endpoints (if applicable)
N/A.

### State / Store (if applicable)
N/A.

### i18n Keys Added
`customersInvoicesEmpty`, `customersInvoicesNew`, `commonValueEmpty`

### Dependencies Added
None.

### Notes / Known Limitations
- Only invoices present in the **frontend demo** Rechnungen store appear; no live ERP API yet.

---

## FEATURE-142: Customer invoices — Edit + Invoice files modal, demo PDF in new tab
**Date:** 2026-04-01
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
Restores the **Invoices** UX from FEATURE-141’s inline table + dual buttons: **Edit invoices** jumps to **Rechnungen** with `dema-rechnungen-filter-kunden-nr` (same as before **Show invoices**). **Invoice files** opens a modal listing matching Rechnungen rows; **Open PDF** generates a minimal demo PDF via `buildSimpleTextPdf` and opens it in a **new browser tab** (`window.open`); pop-up blockers show a translated alert. Badge on **Invoice files** shows the row count.

### Where It Was Added
- `frontend/src/common/utils/buildSimpleTextPdf.ts` — new
- `frontend/src/pages/CustomersPage.tsx` — `customerInvoicesOpen`, modal, `openInvoicePdfInBrowser`, `formatInvoiceListDateForPdf`; removed inline invoice table and **New invoice** shortcut from this block (`openRechnungenNewInvoiceForCustomer` removed)
- `frontend/src/contexts/LanguageContext.tsx` — de / en / fr keys for folder + PDF strings; removed `customersInvoicesEmpty`, `customersInvoicesNew`, `customersShowInvoices`

### What It Does (Technical)
1. `rechnungenRowsForCustomer` unchanged: filter `loadRechnungenDb().rows` by `kunden_nr`.
2. `openInvoicePdfInBrowser`: `URL.createObjectURL(buildSimpleTextPdf(lines))` → `window.open`; revoke after 60s or immediately if blocked.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| session `dema-rechnungen-filter-kunden-nr` | string | No | Set by **Edit invoices** |

### Database
- **Engine:** `rechnungenStore` (read-only list)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** In-memory filter

### API Endpoints (if applicable)
N/A.

### State / Store (if applicable)
N/A.

### i18n Keys Added
`customersInvoicesEditBtn`, `customersInvoicesFolderBtn`, `customersInvoicesFolderTitle`, `customersInvoicesFolderHint`, `customersInvoicesFolderEmpty`, `customersInvoicesFilePdf`, `customersInvoicesViewPdf`, `customersInvoicesPdfPopupBlocked`, `customersInvoicesPdfDocHeading`, `customersInvoicesPdfRule`, `customersInvoicesPdfCompany`, `customersInvoicesPdfFooter` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
**New invoice** from the customer card was removed in this block; users open **Edit invoices** then create on **Rechnungen**. Demo PDF is text-only. `npm run build` verified.

---

## FEATURE-143: Edit invoices — open latest customer invoice in Rechnungen drawer
**Date:** 2026-04-01
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
**Edit invoices** from the customer modal still sets `dema-rechnungen-filter-kunden-nr` and navigates to **Rechnungen**. If the demo store has at least one row for that **KundenNr**, it also sets **`dema-rechnungen-select-id`** to the **newest** invoice by `r_datum` (then `id`). **RechnungenPage** reads that key on mount (unless **Neue Rechnung** handoff is active) and opens the **side drawer** with that row selected—the same view as clicking the row in the list.

### Where It Was Added
- `frontend/src/pages/CustomersPage.tsx` — `openRechnungenForCustomer` picks latest matching row id
- `frontend/src/pages/RechnungenPage.tsx` — mount `useEffect` consumes `dema-rechnungen-select-id`

### What It Does (Technical)
1. Sort matching rows: `r_datum` descending, then `id` descending.
2. `setSelectedId(id)` + `setShowNewDrawer(false)` after `loadRechnungenDb()` validates the id exists.
3. `dema-rechnungen-open-new-invoice` still wins over `select-id` when both would apply.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| session `dema-rechnungen-select-id` | string (numeric id) | No | Row `id` in `rechnungenStore` |

### Database
- **Engine:** `rechnungenStore` / `loadRechnungenDb`
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** N/A

### API Endpoints (if applicable)
N/A.

### State / Store (if applicable)
N/A.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Demo drawer for an existing row is **read-only** detail (`dl`); full editable form exists only for the **(Neu)** path. If the customer has **no** invoices, only the filter is applied—no drawer. `npm run build` verified.

---

