# Feature Log — 2026-04-08

> Features added or modified on this date.

---

## FEATURE-137: VAT check error copy — empty HTTP body & CORS docs
**Date:** 2026-04-08  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
When `/api/v1/vat/check` returns an error without a JSON `detail` (e.g. proxy timeout or gateway 500 with empty body), the customer modal now explains **proxy timeout / cold start / worker crash** first and clarifies that a **visible HTTP status usually means CORS already allowed the response**. CORS-specific guidance is kept for **HTTP 0** and network failures. Non-JSON error bodies mention lowering `VIES_CHECK_ENDPOINT_MAX_TOTAL_SEC`. Backend `.env.example` documents `CORS_ORIGINS` and `CORS_ORIGIN_REGEX` for SPA + API split deployments.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — `vatErrorBodyHasDetail`, clearer branches for parse errors and empty payloads; i18n via `t()`.
- `frontend/src/contexts/LanguageContext.tsx` — new `vatCheck*` and `newCustomerVatEmptyInput` keys (de/en/fr).
- `backend/.env.example` — CORS section with Vercel preview regex example.

### What It Does (Technical)
1. After `fetch`, if status is not OK and JSON parse fails, show translated message with `{status}` and `{hint}` placeholders.
2. If JSON parses but has no `detail` / `message` / `error` payload, show 405-specific vs generic empty-body guidance.
3. `formatVatCheckDetail` fallback uses `t("vatCheckErrorNoServerDetail", ...)`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| N/A | N/A | N/A | UI-only copy changes |

### Database
- **Engine:** N/A  
- **Tables Affected:** N/A  
- **Schema Changes:** None  
- **Key Queries:** None  

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|--------|------|------|--------------|----------|
| N/A | N/A | N/A | N/A | N/A |

### State / Store (if applicable)
- **Store file:** N/A  
- **Actions/Selectors added:** None  
- **Persisted:** No  

### i18n Keys Added
`newCustomerVatEmptyInput`, `vatCheckErrorNoServerDetail`, `vatCheckErrorCorsOrNetwork`, `vatCheckErrorNonJsonHint`, `vatCheckErrorHttpNonJson`, `vatCheckErrorHttp405`, `vatCheckErrorHttpEmptyBody`, `vatCheckErrorNetworkHintApiBase`, `vatCheckErrorNetworkHintLocal`, `vatCheckErrorNetwork`

### Dependencies Added
None.

### Notes / Known Limitations
Empty-body 502/503 from some gateways may still be opaque; operators should rely on API host logs and proxy timeout settings.

---

## FEATURE-138: VIES response — title case for postal address
**Date:** 2026-04-08  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The VAT check API now formats the **postal address** field (`address`, `address_normalized`) in **title case** per line after existing country-aware display normalization. **Company / trader name** is unchanged (legal string). `address_original` and `address_search` stay derived from registry text as before. `normalization_version` is set to `vies_norm_v2_address_title`.

### Where It Was Added
- `backend/main.py` — `_title_case_vies_address`, applied in `_map_vies_check_response` before the “Not provided by VIES” placeholder.

### What It Does (Technical)
1. After `_normalize_vies_display_text` (and optional fallback address), each non-empty line of the address is passed through `str.strip().title()`.
2. Multiline addresses keep line breaks; blank lines are preserved in the join.
3. Placeholder text is not title-cased.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| address | string | No | Display address; title case per line |
| address_normalized | string | No | Same as display policy |
| normalization_version | string | No | `vies_norm_v2_address_title` |

### Database
- **Engine:** N/A  
- **Tables Affected:** N/A  
- **Schema Changes:** None  
- **Key Queries:** None  

### API Endpoints (if applicable)
| Method | Path | Auth | Request Body | Response |
|--------|------|------|--------------|----------|
| POST | /api/v1/vat/check | No | VatCheckRequest | VatCheckResponse (address title-cased) |

### State / Store (if applicable)
- **Store file:** N/A  
- **Actions/Selectors added:** None  
- **Persisted:** No  

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Python `str.title()` is locale-neutral; rare edge cases (e.g. particles, apostrophes) may not match manual typographic rules.

**Extended (same day):** Frontend `extractViesAddressForForm` + `titleCaseLatinAddressLine` (`addressLatinNormalize.ts`) title-case **street** and **city** for the VAT “Split for the form” preview and **Apply to form**, fixing ALL CAPS / mixed transliteration (e.g. EL/Greek VIES) that bypassed the API `address` line.

---
