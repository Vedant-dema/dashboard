# Feature Log — 2026-04-09

> Features added or modified on this date.

---

## FEATURE-143: Customer domain context pack for LLM/agent onboarding
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Added  

### What Was Added
A **curated documentation pack** under `docs/context/customer/` so engineers and generative models can understand the customer module **without pasting large source files**: hub README with mandatory read order and golden rules, glossary (German terms ↔ fields), module map (frontend/backend paths only), API/persistence **contracts**, implemented vs deferred **capabilities**, and a **changelog bridge** to `FEATURE-LOG.md`. Cross-links from `docs/README.md` (Assistant / LLM context), `docs/api/customer-api.md`, and `docs/operations/agent-playbook.md` (DoD + optional OpenAPI curl). **OpenAPI export** helper doc and **GitHub PR template** checklist for customer-context updates.

### Where It Was Added
- `docs/context/customer/README.md` — entry hub, read order, rules, OpenAPI pointer
- `docs/context/customer/glossary.md` — domain terms
- `docs/context/customer/module-map.md` — concern → path table
- `docs/context/customer/contracts.md` — endpoints, errors, persistence matrix
- `docs/context/customer/capabilities.md` — implemented vs roadmap
- `docs/context/customer/changelog-bridge.md` — FEATURE-LOG keywords
- `docs/context/customer/openapi-export.md` — snapshot commands
- `docs/README.md` — Assistant / LLM section; CustomersPage spec link
- `docs/api/customer-api.md` — pointer to context hub
- `docs/operations/agent-playbook.md` — §7 DoD + OpenAPI note
- `.github/pull_request_template.md` — customer context checklist
- `docs/FEATURE-LOG.md`, this file — FEATURE-143

### What It Does (Technical)
1. Establishes a **single entry path** for agents: read hub → contracts → glossary → module map → capabilities → ADR/API/product/spec → FEATURE-LOG.
2. Documents **known error codes** and **VAT timeout** behavior at summary level with links to `vat-api.md` and runtime OpenAPI.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| N/A | N/A | N/A | Documentation only |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
N/A (docs reference existing endpoints).

### State / Store (if applicable)
N/A.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Deep behavior remains in `docs/Detailed report/CustomersPage-Service-Spec.md`; this pack is intentionally short. Update `contracts.md` / `capabilities.md` when shipping customer changes.

---

## FEATURE-144: Shared customers load — visible errors and env trim
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
When **API mode** is enabled but the first **GET `/api/v1/demo/customers-db`** fails (CORS, 401, network, etc.), the Customers page now shows an **amber banner** with a clear explanation and optional **technical message**, instead of failing **silently** and leaving users thinking only local storage applies. **`VITE_CUSTOMERS_SOURCE`** and **`VITE_API_BASE_URL`** are **trimmed** at build time so accidental spaces in Vercel env values no longer disable API mode.

### Where It Was Added
- `frontend/src/pages/CustomersPage.tsx` — initial shared-load `catch` sets `syncNotice` with i18n
- `frontend/src/store/kundenStore.ts` — `.trim()` on customer source and API base URL
- `frontend/src/contexts/LanguageContext.tsx` — `customersSharedLoadFailed*`, `customersSharedLoadDetail` in de/en/fr
- `docs/FEATURE-LOG.md`, this file — FEATURE-144

### What It Does (Technical)
1. `useEffect` for shared load: on error, builds a notice from `t("customersSharedLoadFailed")`, `t("customersSharedLoadFailedHint")`, and optional `t("customersSharedLoadDetail")` with `{detail}` from `Error.message`.
2. `API_MODE` and `API_BASE` use trimmed env strings before comparison / URL construction.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| N/A | N/A | N/A | UI copy and env parsing only |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
N/A (still calls existing demo customers-db endpoint; no new routes).

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (env parsing only)
- **Persisted:** No

### i18n Keys Added
`customersSharedLoadFailed`, `customersSharedLoadFailedHint`, `customersSharedLoadDetail` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
Does not change Render or Vercel configuration; operators must still fix CORS and build-time env. The banner makes misconfiguration obvious without opening DevTools first.

---

## FEATURE-165: Einkaufs-Terminplan — legacy demo rows across three days + auto-merge
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
Purchase **timetable** demo data (already modeled on the legacy **Termin-Kalender** screenshot) is now **re-dated at runtime** and **split across yesterday, today, and tomorrow** so managers see rows when moving the date selector, not only on a single build-time day. If `localStorage` already has user rows but **no** `DEMO` buyer queue, the app **merges** the eight demo rows once on load so Tom Müller (and any user) gets a populated Tagesübersicht for demos.

### Where It Was Added
- `frontend/src/store/timetableStore.ts` — `buildSpreadSeeds`, `remapSeedDay`, `ensureSharedDemoTimetableRows`, `SEED_ENTRIES_RAW` + `SEED_PLACEHOLDER_DAY`; `fallbackState` / `loadTimetableDb` wiring
- `docs/FEATURE-LOG.md`, this file — FEATURE-165

### What It Does (Technical)
1. Seed templates use a fixed placeholder ISO day; `buildSpreadSeeds()` maps times onto **today−1**, **today**, **today+1** (3+3+2 rows).
2. `loadTimetableDb()` returns `ensureSharedDemoTimetableRows(...)` when persisted JSON parses successfully so existing installs without `buyer_name === DEMO` gain demo rows.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| N/A | N/A | N/A | Same `TimetableEntry` shape as before |

### Database
- **Engine:** Browser `localStorage` (`dema-purchase-timetable-v6`)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts`
- **Persisted:** Yes (`localStorage`)

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Removing all `DEMO` buyer rows is reverted on next load (demo re-merge). Timetable remains **not** synced to Render; Vercel vs local still differ per browser. Clearing site data or using a fresh profile still shows full spread via `fallbackState`.

---

## FEATURE-145: Purchase Kalender — Timetable UI (call planner prototype)
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Added  

### What Was Added
A dedicated **Purchase Kalender** screen at `#/purchase/kalender` (alias `#/purchase/timetable`) replacing the previous fallback to the home dashboard. **Modern UI** (gradient hero, glass table, rose/amber palette, personalized greeting from session user) mirroring the legacy **Termin-Kalender** workflow: buyer/processor filter, daily overview table, quick filters, date range + mini calendar, localStorage-backed rows with seed data, **new call slot** modal, **trucks available** follow-up modal appending to notes, **insert five drafts**, print list, toggle done, `tel:` links, and **offers-only (KA)** checkbox.

### Where It Was Added
- `frontend/src/App.tsx` — `TIMETABLE_ROUTES`, render `TimetablePage`
- `frontend/src/pages/timetable/TimetablePage.tsx` — main page UI and logic
- `frontend/src/pages/timetable/TimetableMiniCalendar.tsx` — month grid, locale-aware weekdays
- `frontend/src/pages/timetable/index.ts` — barrel export
- `frontend/src/types/timetable.ts` — `TimetableEntry`, filter mode types
- `frontend/src/store/timetableStore.ts` — `localStorage` load/save, seed rows, CRUD helpers
- `frontend/src/contexts/LanguageContext.tsx` — `timetable*` keys (de, en, fr)
- `docs/FEATURE-LOG.md`, this file — FEATURE-145

### What It Does (Technical)
1. On load, `loadTimetableDb()` reads `dema-purchase-timetable-v1` or seeds five demo rows for **today** (ISO date).
2. `filteredRows` applies date range, optional processor `vp`, optional `KA`-only filter, and filter mode (`day`, unfinished, other buyer, done without follow-up, parked).
3. Mutations call `saveTimetableDb` via `useEffect` on `db` changes.
4. Mini calendar updates selected day and syncs range to that day when picking a cell.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| kaArt | string | yes | Row type e.g. A, KA |
| datum | string (ISO date) | yes | Appointment date |
| zeit | string (HH:mm) | yes | Time |
| firmenname | string | yes | Company |
| telefon | string | no | Phone |
| ansprechpartner | string | no | Contact |
| zweck | string | yes | Purpose |
| bemerkung | string | no | Notes |
| erledigt | boolean | yes | Done flag |
| vp | string | yes | Buyer/processor code |
| parked | boolean | yes | Parked flag |
| hasFollowUp | boolean | yes | Follow-up scheduled |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** N/A

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts`
- **Actions/Selectors added:** `loadTimetableDb`, `saveTimetableDb`, `upsertTimetableEntry`, `addTimetableEntries`
- **Persisted:** Yes (`localStorage`)

### i18n Keys Added
`timetablePageTitle`, `timetableGreeting`, `timetableGreetingFallback`, `timetableBadgePurchase`, `timetableProcessingPerson`, `timetableAllProcessors`, `timetableActionsTitle`, `timetableNewAppointment`, `timetablePrint`, `timetableInsertFive`, `timetableMoveInList`, `timetableShowOffers`, `timetableDailyOverview`, `timetableAppointmentCount`, `timetableColKaArt`, `timetableColDate`, `timetableColTime`, `timetableColCompany`, `timetableColPhone`, `timetableColContact`, `timetableColPurpose`, `timetableColRemark`, `timetableColDone`, `timetableColVp`, `timetableColActions`, `timetableEmpty`, `timetableFilterDayView`, `timetableFilterUnfinished`, `timetableFilterUnfinishedOther`, `timetableFilterDoneNoFollowUp`, `timetableFilterParked`, `timetableFiltersTitle`, `timetableRangeTitle`, `timetableDateFrom`, `timetableDateTo`, `timetableMiniCalTitle`, `timetablePrevMonth`, `timetableNextMonth`, `timetableModalNewTitle`, `timetableModalTruckTitle`, `timetableTruckHint`, `timetableFieldVehicleTypes`, `timetableFieldQuantity`, `timetableFieldNotes`, `timetableDoneYes`, `timetableDoneNo`, `timetableRowCall`, `timetableRowTrucks`, `timetableToastSaved`, `timetableToastTruckLogged`, `timetableToastInsertedFive`, `timetableToastMoveSoon`, `timetablePlaceholderCompany` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
**Move in list** is a placeholder toast. **Print** uses `window.print()` without a dedicated print stylesheet. No backend sync; seed reloads empty store. Purchasing feedback may drive column and filter changes before API work.

---

## FEATURE-146: Purchase Timetable — premium visual refresh
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
**Visual upgrade** for the Purchase timetable: layered **radial + grid** page background; **slate/rose/indigo** hero with light streak, KPI strip (scheduled / open / offers for selected day), long-form **weekday date** in processor card; **action rail** with icon capsules and press feedback; **dark table header**, gradient **type badges** (KA vs A), **amber left rail** for open rows, **parked** row tint, refined **filter pills** with active glow; **modals** with gradient top bars and stronger focus rings; **mini calendar** restyle (slate header, ring-selected day).

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — layout, table, panels, modals
- `frontend/src/pages/timetable/TimetableMiniCalendar.tsx` — calendar chrome
- `frontend/src/contexts/LanguageContext.tsx` — `timetableHeroStat*`, `timetableTableToolbarHint` (de, en, fr)
- `docs/FEATURE-LOG.md`, this file — FEATURE-146

### What It Does (Technical)
Purely presentational Tailwind changes plus `heroStats` `useMemo` for three headline counts; no change to store schema or routing.

### Data It Accepts / Emits
N/A (UI only).

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
`timetableHeroStatScheduled`, `timetableHeroStatOpen`, `timetableHeroStatOffers`, `timetableTableToolbarHint` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
Print styling still browser-default.

---

## FEATURE-147: Purchase Timetable — layout & visual system refresh
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
**Information architecture:** **Date-first** experience — sticky **left rail** (xl) with large **selected-day** header, gradient accent, and **mini calendar**; **summary** card below. **Main column**: glass **command deck** (search, date input, primary CTA), **emerald create panel**, **filter chip bar**, then **queue title** + table. **Atmospheric** page background (soft gradient + dot grid + radial accents). **Hero** reflow with icon-backed KPI tiles and profile/owner chips. **TimetableTable** and **TimetableMiniCalendar** restyled (gradient headers, row outcome accents, refined actions). **i18n:** full **de/en/fr** keys for timetable workflow strings previously fallback-only (`timetablePageSubtitle`, filters, summary, columns, modals, `commonTotal`).

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — layout, chrome, panels
- `frontend/src/pages/timetable/TimetableMiniCalendar.tsx` — calendar UI
- `frontend/src/pages/timetable/TimetableTable.tsx` — table UI
- `frontend/src/contexts/LanguageContext.tsx` — timetable + `commonTotal` keys (de, en, fr)
- `docs/FEATURE-LOG.md`, this file — FEATURE-147

### What It Does (Technical)
CSS/Tailwind-only presentation and grid reorder (`xl:grid-cols-12`, sticky aside). No change to `timetableStore` contracts or filter logic.

### Data It Accepts / Emits
N/A.

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
Large set: `timetablePageSubtitle`, `timetableProfileLabel`, `timetableOwnerCode`, `timetableSearch`, `timetableSearchPlaceholder`, `timetableSelectedDay`, `timetableNewSlot`, `timetableCreatePanelTitle`, `timetableFilterAllDay`, `timetableFilterOpen`, `timetableFilterFollowUp`, `timetableFilterOffers`, `timetableFilterCompleted`, `timetableDailyQueue`, `timetableRowsLabel`, `timetableQueueSummary`, summary/performance keys, column and modal keys, outcome keys, `commonTotal` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
None beyond existing timetable scope.

---

## FEATURE-148: Purchase Timetable — header-only day summary KPIs
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
**Single professional summary:** Total, Open, Offers, and Follow-up counts appear **only** in the top **hero header** (glass KPI cards, color-aligned icons and labels, dark ambient background). The **duplicate** sidebar “Summary / Pipeline / Performance” block was **removed** so the layout matches a clean purchase-desk header. Added **`timetableHeaderSummary`** for the KPI region **accessible name** (`aria-label`) in **de / en / fr**.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — header KPI strip; removed sidebar insight tabs and related state
- `frontend/src/contexts/LanguageContext.tsx` — `timetableHeaderSummary` (de, en, fr)
- `docs/FEATURE-LOG.md`, this file — FEATURE-148

### What It Does (Technical)
Day-scoped `useMemo` still derives counts for the selected date; values render in four compact cards in the header flex row. Sidebar no longer mounts `TimetableSummaryTab` or `insightStats`.

### Data It Accepts / Emits
N/A (same store-derived counts as before).

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
`timetableHeaderSummary` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
Pipeline/performance narrative charts from the old sidebar are not shown; restore later only if needed and without duplicating the four KPI numbers.

---

## FEATURE-149: Purchase Timetable — operations-first toolbar, calendar on demand
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
**Layout:** Removed the **sticky left column** (large “selected day” card + always-visible mini calendar) so the **queue table uses full width** and daily work is not squeezed. **Date navigation:** **Previous / next day** icon buttons, a **single date control** (formatted label + calendar icon) that opens a **popover** with `TimetableMiniCalendar` and an **exact date** field, then closes on pick or outside click. **Toolbar:** One **operations card** — search, date strip, **primary “New call slot”** button (emerald gradient), and **quick filter chips** in a second row inside the same panel so actions read as one professional control surface.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — layout, popover, `shiftSelectedDay`, `selectedDate` sync effect
- `frontend/src/contexts/LanguageContext.tsx` — `timetableOpenDayPicker`, `timetablePrevDay`, `timetableNextDay`, `timetableExactDateLabel` (de, en, fr)
- `docs/FEATURE-LOG.md`, this file — FEATURE-149

### What It Does (Technical)
`selectedDate` changes sync `viewYear`/`viewMonth` and default `createForm.date` via `useEffect`. `dayPickerRef` + document `mousedown` closes the popover. Mini calendar uses `onPickDayFromCalendar` to set date and close.

### Data It Accepts / Emits
N/A.

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
`timetableOpenDayPicker`, `timetablePrevDay`, `timetableNextDay`, `timetableExactDateLabel` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
Popover positioning is `absolute` below the date button; very narrow viewports use `w-[min(calc(100vw-2rem),20.5rem)]`.

---

## FEATURE-150: Purchase Timetable — day picker z-index above table
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
Fixed **stacking order** so the **Jump to day** popover is not covered by the **NOTES / ACTIONS** table header: the operations toolbar card uses **`relative z-10`**, the queue/table block uses **`relative z-0`**, and the popover uses **`z-[60]`** inside the toolbar context. Root cause: the table is a **later DOM sibling** and was painting on top of the absolutely positioned calendar.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx`
- `docs/FEATURE-LOG.md`, this file — FEATURE-150

### What It Does (Technical)
Explicit z-index on sibling sections so the toolbar stacking context sorts above the table section; popover remains a descendant of the toolbar card.

### Data It Accepts / Emits
N/A.

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Modals remain at higher fixed z-index (`z-[70]` / `z-[80]`).

---

## FEATURE-151: Purchase Timetable — AI assistant preview panel
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Added  

### What Was Added
A **collapsible “AI assistant”** strip on the purchase timetable page (**default collapsed**) so daily operations stay uncluttered. Expanded content shows a **Preview** badge, roadmap hint for API mapping, and **four disabled action tiles**: summarize day, suggest call order, polish notes, offer recap — each with title, description, and “connect backend” footnote. Styled as a violet/indigo gradient card distinct from the emerald operations toolbar; **`z-[5]`** so it stacks between the toolbar (`z-10`) and queue (`z-0`).

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableAiAssistPanel.tsx` — new component
- `frontend/src/pages/timetable/TimetablePage.tsx` — panel below search/filters card
- `frontend/src/contexts/LanguageContext.tsx` — `timetableAi*` keys (de, en, fr)
- `docs/FEATURE-LOG.md`, this file — FEATURE-151

### What It Does (Technical)
Local `useState` toggles expand/collapse. Action buttons are `disabled` with `aria-label` and `title` hints; no network calls. Action list built in `useMemo` keyed on `t`.

### Data It Accepts / Emits
N/A.

### Database
N/A.

### API Endpoints (if applicable)
None (UI scaffold only).

### State / Store (if applicable)
N/A.

### i18n Keys Added
`timetableAiPanelAria`, `timetableAiPanelTitle`, `timetableAiBadgePreview`, `timetableAiExpand`, `timetableAiCollapse`, `timetableAiPanelSubtitle`, `timetableAiRoadmapHint`, `timetableAiActionSummarize`, `timetableAiActionSummarizeHint`, `timetableAiActionPrioritize`, `timetableAiActionPrioritizeHint`, `timetableAiActionNotes`, `timetableAiActionNotesHint`, `timetableAiActionOffer`, `timetableAiActionOfferHint`, `timetableAiDisabledHint` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
Tiles are non-functional until wired to an AI/backend module; easy to replace `disabled` with handlers and loading states later.

---

## FEATURE-152: Purchase Timetable — legacy Termin-Kalender parity (actions, filters, demo data)
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
**Legacy-style actions:** **Print** (`window.print()` + `timetable-print.css`), **5 draft rows** (`insertFiveDraftTimetableRows`), **Move in list** (toast using `timetableToastMoveSoon`), **Neuer Termin** primary CTA (`timetableNewAppointment`), **Angebote anzeigen** checkbox (narrows to KA / offer rows). **Bearbeitende Person:** scope **My queue & shared demo** vs **Everyone**. **Anz. Termine** counter. **Filters** aligned with legacy: open, open other buyer, **other buyers (Von–Bis range)**, follow-up, offers, completed, **done without follow-up**, **parked**. **Table** reworked to legacy-style columns: **Ka** (A/KA), date, time, company, tel, contact, **Zweck**, **Bemerkung** (clamp; full text when printing), outcome, follow-up, **Erledigt**, **Einkäufer**, actions. **Demo seed:** `TIMETABLE_DEMO_BUYER` rows with **2026-04-09** legacy-style German notes (Dahm, Wichmann, ABATTOIR, Beck, Gaugl, CARGOTRANS, Nordfrost) plus **FS** / **ES** rows and **today** copies; storage key **`dema-purchase-timetable-v3`**.

### Where It Was Added
- `frontend/src/types/timetable.ts` — extended `TimetableFilterMode`
- `frontend/src/store/timetableStore.ts` — v3 key, `TIMETABLE_DEMO_BUYER`, seed data, `insertFiveDraftTimetableRows`, `timetableRowIsMine` / `timetableRowIsOtherBuyer`
- `frontend/src/pages/timetable/TimetablePage.tsx` — toolbar, filters, range, toast, print root, `no-print` regions
- `frontend/src/pages/timetable/TimetableTable.tsx` — column layout, Ka, parked tag, print-friendly notes
- `frontend/src/pages/timetable/timetable-print.css` — print visibility scope
- `frontend/src/pages/timetable/components/TimetableAiAssistPanel.tsx` — `no-print`
- `frontend/src/contexts/LanguageContext.tsx` — `timetableScopeMine`, `timetableFilterOtherBuyerRange`, `timetableRowParkedTag`, label tweaks (`timetableShowOffers`, `timetableAppointmentCount`, `timetableColKaArt` DE)
- `docs/FEATURE-LOG.md`, this file — FEATURE-152

### What It Does (Technical)
`viewerRows` uses `timetableRowIsMine` when scope is mine (viewer code **or** `DEMO`). Range filter applies date window first, then other-buyer predicate. Checkbox ANDs `has_trucks` / `offer` onto the active filter.

### Data It Accepts / Emits
Same `TimetableEntry` shape; localStorage JSON under v3 key.

### Database
Browser `localStorage` (`dema-purchase-timetable-v3`).

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
`insertFiveDraftTimetableRows` appends five `Anruf` drafts for **selected day** and **current viewer** code.

### i18n Keys Added
`timetableScopeMine`, `timetableFilterOtherBuyerRange`, `timetableRowParkedTag`; adjusted `timetableShowOffers`, `timetableAppointmentCount`, DE `timetableColKaArt` (de, en, fr where new).

### Dependencies Added
None.

### Notes / Known Limitations
Existing **v2** localStorage is not read (new key); users get fresh demo seed unless v2 data is migrated manually. **Move in list** remains a placeholder until backend/list workflow exists.

---

## FEATURE-153: Purchase Timetable — modern toolbar, no processor/AI, v4 demo seed
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
**UX:** Removed **Buyer / processor** dropdown; list is always **your code + `DEMO`** (`timetableRowIsMine`). Removed **AI assistant** panel (component deleted). **Print**, **5 drafts**, **Move in list** moved into an **Actions** dropdown (`timetableActionsMenu`) with outside-click close; **Neuer Termin** stays a primary **emerald** button. **Show offers** is a **toggle pill** before quick filters; filters use **light rounded pills** and horizontal scroll on small screens. **Visual refresh:** softer page background, slimmer dark header, lighter KPI tiles, toolbar **rounded-2xl** / subtle shadow, table **slate-50** header row instead of heavy dark gradient.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — layout, actions menu, removed processor & AI
- `frontend/src/pages/timetable/TimetableTable.tsx` — modern table chrome
- `frontend/src/store/timetableStore.ts` — **`dema-purchase-timetable-v4`**, seed rows **all on local “today”** with Termin-Kalender-style German **Bemerkung** text (8 rows)
- `frontend/src/contexts/LanguageContext.tsx` — `timetableActionsMenu`, `timetableActionsMenuAria` (de, en, fr)
- Deleted: `frontend/src/pages/timetable/components/TimetableAiAssistPanel.tsx`
- `docs/FEATURE-LOG.md`, this file — FEATURE-153

### What It Does (Technical)
`actionsMenuRef` + `mousedown` listener; menu `z-[70]` above day picker. Seed uses `TODAY` at module load so first paint matches **Tagesübersicht** density for the current calendar day.

### Data It Accepts / Emits
N/A.

### Database
Browser `localStorage` key **`dema-purchase-timetable-v4`**.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
`timetableActionsMenu`, `timetableActionsMenuAria` (de, en, fr). Unused `timetableAi*` keys remain in `LanguageContext` for possible reuse.

### Dependencies Added
None.

### Notes / Known Limitations
Users on **v3** key get a **new** empty store until they clear storage or migrate; v4 forces fresh **demo** seed on first load without prior data.

---

## FEATURE-154: Purchase Timetable — filters in dropdown by search
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The timetable **quick filters** (show offers, filter mode chips, optional Von/Bis range) are no longer a permanent strip under the toolbar. A **filter icon button** sits **immediately before** the search field; clicking it toggles a **dropdown panel** anchored under that button. **Click outside** closes the panel; opening the **date picker** or **Actions** menu closes filters to avoid stacked overlays. The filter button shows an **active** state (emerald + dot) when any filter is applied, and a **pressed** style while the panel is open.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — `filtersOpen`, `filtersMenuRef`, outside-click handler, filter button + dropdown layout, removed always-visible quick-filters row
- `frontend/src/contexts/LanguageContext.tsx` — `timetableFiltersMenuAria` (de, en, fr) for the filter control button
- `docs/FEATURE-LOG.md`, this file — FEATURE-154

### What It Does (Technical)
1. `filtersMenuRef` wraps the filter button and dropdown; `useEffect` listens for `mousedown` on `document` and closes when the target is outside the ref.
2. Toolbar row: label “Search” / search row with square **Filter** icon button then flex-1 search input; dropdown uses `absolute` positioning and `z-[62]` below the actions menu layer.
3. `filtersActive` derives from `showOffersOnly` or `filterMode !== 'all_day'` for button styling.

### Data It Accepts / Emits
N/A (same Zustand timetable filter state as before).

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
Unchanged: `timetableStore` filter fields (`filterMode`, `showOffersOnly`, range fields, etc.).

### i18n Keys Added
`timetableFiltersMenuAria` (de, en, fr). Existing `timetableFiltersTitle` and filter chip keys reused in the dropdown.

### Dependencies Added
None.

### Notes / Known Limitations
Dropdown stays open after choosing a chip (user can adjust multiple options); Escape-to-close not added unless requested.

---

## FEATURE-155: Purchase Timetable — filter/search column labels, filters panel polish, no appointment count
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
**FILTER** and **SEARCH** are **separate column labels**: uppercase micro-label above the filter icon only and above the search field only (`sm:items-end` aligns controls). The filters **dropdown** has a **header** (title + short subtitle), **Offer visibility** section with a **full-width row + toggle affordance**, a **divider** and **Row filters** section with mode chips in a **responsive two-column grid**, and a **date-range** block when “other buyers (range)” is selected. The toolbar **“Appointments: {n}”** line next to Actions was **removed** (counts remain in hero stats).

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — toolbar layout, dropdown structure, removed `timetableAppointmentCount` UI
- `frontend/src/contexts/LanguageContext.tsx` — `timetableFilterLabel`, `timetableFiltersSubtitle`, `timetableFiltersSectionOffers`, `timetableFiltersSectionModes` (de, en, fr)
- `docs/FEATURE-LOG.md`, this file — FEATURE-155

### What It Does (Technical)
1. `filtersMenuRef` wraps a `relative` column: label, filter button, absolutely positioned panel under the column (`top-[calc(100%+0.35rem)]`).
2. Show-offers control is a single `button` with `aria-pressed` and a decorative iOS-style track (knob uses `transition-transform`).

### Data It Accepts / Emits
N/A.

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
Unchanged timetable filter/search state.

### i18n Keys Added
`timetableFilterLabel`, `timetableFiltersSubtitle`, `timetableFiltersSectionOffers`, `timetableFiltersSectionModes` (de, en, fr). `timetableAppointmentCount` retained in messages for possible reuse elsewhere.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-156: Purchase Timetable — Termin-Kalender demo rows, Ka/Arte columns, storage v5
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
**Seed data** matches the legacy **Termin-Kalender** screenshot: **eight rows** for the selected calendar day with the same **Firmenname**, **Tel**, **Ansprechpartner**, **Zweck** (Anruf), **Bemerkung** text, **Zeit** slots (two at 00:00, four at 00:30, two at 01:00), and **Ka / Arte** codes (**A** + **KA** or **A**). The table now has **two header columns** **Ka** and **Arte** instead of a single merged type column. Optional **`legacy_ka`** / **`legacy_arte`** on `TimetableEntry` persist through **`normalizeEntry`**. **localStorage** key bumped to **`dema-purchase-timetable-v5`** so the new demo loads without merging old v4 JSON.

### Where It Was Added
- `frontend/src/store/timetableStore.ts` — `SEED_ENTRIES`, `STORAGE_KEY` v5, `normalizeEntry` optional legacy fields
- `frontend/src/types/timetable.ts` — `legacy_ka?`, `legacy_arte?`
- `frontend/src/pages/timetable/TimetableTable.tsx` — Ka + Arte columns, `COL_COUNT` 14
- `frontend/src/contexts/LanguageContext.tsx` — `timetableColKa`, `timetableColArte` (de, en, fr)
- `docs/FEATURE-LOG.md`, this file — FEATURE-156

### What It Does (Technical)
1. First body column shows `legacy_ka ?? 'A'`; second shows `legacy_arte ?? kaArtFromRow(row)` for rows without legacy arte.
2. Outcomes/offers tuned for UX (e.g. ABATTOIR/Beck **has_trucks** + **offer**; Wichmann/Brand **follow_up** with optional **follow_up_at**).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| legacy_ka | string | No | Shown in Ka column; default A |
| legacy_arte | string | No | Shown in Arte column; else derived from outcome/offer |

### Database
Browser **localStorage** key **`dema-purchase-timetable-v5`** (v4 data not auto-imported).

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
`loadTimetableDb` / `saveTimetableDb` unchanged aside from key string.

### i18n Keys Added
`timetableColKa`, `timetableColArte` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
Users with data only under **v4** must re-enter rows or export/import if added later; no migration path from v4 in this change.

---

## FEATURE-157: Purchase Timetable — Kundenkontakt drawer (modern customer contact)
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Added  

### What Was Added
Clicking a **timetable row** (outside links/buttons) opens a **right-hand customer-contact workspace** styled like a modern replacement for legacy **Kundenkontakt**: sticky **header** (company, Ka/Arte, slot date/time, customer number), primary **Log call** / **Add or edit offer**, **secondary toolbar** (Open offers → vehicle tab, customer data / mask / park / purchase confirmation → toast “CRM soon”), and **pill tabs**: **Overview** (contact & company, optional extra contacts, assignment, fleet), **Appointments** (history table), **Vehicle** (offer + optional registration/status block), **Correspondence** (activity text). **Escape** and **backdrop click** close the drawer. **Optional `contact_profile`** on `TimetableEntry` drives rich sections; rows without it still show useful defaults from the entry. **Demo row Dahm (id 1)** includes full profile + **MB Atego** offer aligned with the reference screenshot. **localStorage** key **`dema-purchase-timetable-v6`**.

### Where It Was Added
- `frontend/src/types/timetable.ts` — `TimetableContactProfile` and related interfaces; `contact_profile?` on `TimetableEntry`
- `frontend/src/store/timetableStore.ts` — `normalizeContactProfile` chain, `STORAGE_KEY` v6, enriched seed id 1
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — new drawer UI
- `frontend/src/pages/timetable/TimetableTable.tsx` — row `cursor-pointer` + `onOpenContact`; actions column `stopPropagation`
- `frontend/src/pages/timetable/TimetablePage.tsx` — drawer state, live entry by id from `db`, toast z-index; queue hint
- `frontend/src/pages/timetable/TimetableCallModal.tsx` / `TimetableOfferModal.tsx` — z-index above drawer
- `frontend/src/contexts/LanguageContext.tsx` — `timetableContact*` and `timetableQueueOpenRow` (de, en, fr)
- `docs/FEATURE-LOG.md`, this file — FEATURE-157

### What It Does (Technical)
1. `contactDrawerEntryId` resolves to current `TimetableEntry` from `db.entries` so saves from modals refresh the drawer.
2. `normalizeEntry` persists `contact_profile` from JSON for future API/local edits.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| contact_profile | object | No | Optional CRM-style blocks (industry, address, history rows, activity_notes, vehicle_extra, …) |

### Database
Browser **localStorage** **`dema-purchase-timetable-v6`**.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
Same `timetableStore` persistence; new optional nested object on entries.

### i18n Keys Added
`timetableContact*` (many), `timetableQueueOpenRow` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
Customer mask / park / purchase confirmation are **placeholders** (toast). **Correspondence** is read-only display; append still via **Log call** on the row/modal.

---

## FEATURE-158: Purchase Timetable — customer contact as centered popup (customer-edit style)
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The **Kundenkontakt** UI no longer opens as a **right-hand drawer**; it uses the same **centered modal pattern** as **`NewCustomerModal`**: `fixed inset-0` overlay with **`bg-slate-900/55`**, **`items-center justify-center`**, **`max-h-[88vh]`** card, **`rounded-2xl`**, **gradient title bar** (`from-slate-800 to-slate-900`) with **absolute close** control, **backdrop click** to dismiss, inner **`stopPropagation`**. Toolbar + tabs sit in a **white strip** under the header; body **scrolls** inside the card. Queue hint and **`timetableContactClose`** copy updated for **popup / window** wording (de, en, fr).

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — layout restructure (component name unchanged)
- `frontend/src/contexts/LanguageContext.tsx` — `timetableQueueOpenRow`, `timetableContactClose` tweaks
- `docs/FEATURE-LOG.md`, this file — FEATURE-158

### What It Does (Technical)
Outer `role="presentation"` + `onClick={onClose}`; dialog panel matches customer modal stacking (`z-[100]` unchanged vs page).

### Data It Accepts / Emits
N/A.

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
None (string updates only).

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-159: Purchase Timetable — customer contact toolbar & layout productivity pass
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
**Toolbar:** Single row — **segmented tab control** (inset `rounded-xl` track, white active segment) + **primary actions** (**Log call** emerald, **Offer** gradient) + **“More”** dropdown bundling CRM links (offers → tab, notes → tab, customer data/mask, park, purchase). **Escape** closes the dropdown first, then the modal. **Outcome** status chip in the **gradient header**. **Overview:** `xl:grid-cols-12` (**8+4**), **definition list** as **2-column grid** on `sm+`; sidebar cards tighter padding. **History:** **sticky** table header + **max-height** scroll region. **Vehicle:** **3-column** spec grid on large screens; full-width rows span `lg:col-span-3`. Modal **max-width** `96rem`. New i18n: `timetableContactCrmMenu`, `timetableContactOfferShort` (de, en, fr).

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx`
- `frontend/src/contexts/LanguageContext.tsx`
- `docs/FEATURE-LOG.md`, this file

### What It Does (Technical)
`crmMenuRef` + outside mousedown; `tab` change resets menu open state.

### Data It Accepts / Emits
N/A.

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
`timetableContactCrmMenu`, `timetableContactOfferShort` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-160: Purchase Timetable — full editable Kundenkontakt workspace (draft, save, legacy fields)
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The **customer contact modal** is now a **full editing workspace** for agents on the phone: **draft state** per row (reset on open / row change), **Save contact** and **Discard** in a sticky footer, **unsaved indicator**, and **sync from store** when the row updates externally while not dirty (e.g. after **Log call**). **Overview** tab: editable company, contact, phone, purpose, buyer, **Ka/Arte**, outcome, done, parked, purchase confirmed, slot **datetime-local**, follow-up, KNr., industry, address, fleet, list notes. **Extra contacts** and **assignment** blocks with **add/remove**. **History** tab: **editable appointment grid** with add/remove rows. **Vehicle** tab: **inline offer** fields plus **vehicle_extra** (body, EZ, replacement km, customer price, **three processor roles**, sold/deregistered). **Activity** tab: large **correspondence** textarea. Toolbar shows **all** legacy shortcuts as **visible buttons** (log call, offer dialog, open offers tab, notes tab, CRM placeholders, park toggle, purchase toggle). **Store** normalizes **processor_entered/fetched/negotiated** and **purchase_confirmed**; demo seed extended for processor trio.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — draft UI, persistence footer, editable tabs
- `frontend/src/pages/timetable/TimetablePage.tsx` — `onPersist` / toast
- `frontend/src/store/timetableStore.ts` — normalizers + seed `vehicle_extra` processors
- `frontend/src/types/timetable.ts` — types (from prior step; referenced here)
- `frontend/src/contexts/LanguageContext.tsx` — new `timetableContact*` keys (de, en, fr)
- `docs/FEATURE-LOG.md`, this file

### What It Does (Technical)
1. `useLayoutEffect` resets **draft** when the drawer opens or **row id** changes; `entryFingerprint` + `useEffect` reapplies store copy when `!dirty`.
2. `onPersist` calls `upsertTimetableEntry` via parent; **Save** strips empty **offer** / **vehicle_extra** and sets **captured_at** when first saving an offer.
3. `ensureProfile` returns a **shallow clone** so React state is not mutated in place.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `onPersist` | `(TimetableEntry) => void` | Yes | Writes full row to local timetable DB |
| Draft fields | `TimetableEntry` + nested `contact_profile` | Yes | Mirrors timetable row model |

### Database
- **Engine:** Browser `localStorage` (timetable DB)
- **Tables Affected:** N/A (JSON store)
- **Schema Changes:** None
- **Key Queries:** N/A

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts`
- **Actions/Selectors added:** Extended `normalizeVehicleExtra`, `normalizeContactProfile`; seed data
- **Persisted:** Yes (existing timetable persistence)

### i18n Keys Added
`timetableContactSaved`, `timetableContactSave`, `timetableContactDiscard`, `timetableContactAddRow`, `timetableContactRemoveRow`, `timetableContactAddAppointment`, `timetableContactProcessorEntered`, `timetableContactProcessorFetched`, `timetableContactProcessorNegotiated`, `timetableContactPurchaseConfirmed`, `timetableContactParkedLabel`, `timetableContactSlotDateTime`, `timetableContactTableNotes`, `timetableContactTableNotesPh`, `timetableContactFax`, `timetableContactNoExtraContacts`, `timetableContactNoAssignments`, `timetableContactVehicleInlineHint`, `timetableContactActivityPlaceholder`, `timetableContactUnsaved`, `timetableContactSavedState` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
**Customer data** / **customer mask** still call `onComingSoon` until CRM is wired. **Log call** / **offer** modals persist immediately (parent store); drawer **Discard** reloads the last stored row for that id.

---

## FEATURE-161: Purchase Timetable — Kundenkontakt modal UX (cards, spacing, actions)
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
**Less visual noise, more breathing room:** dialog sized to **~52–60rem** width, **taller** shell (`min-h` ~720px cap), **rounded-3xl** chrome, softer backdrop. **Header** adds a short **guidance line** and larger title. **Tabs** use roomy **pill** controls. **Primary actions** only on the bar: **Log call** + **Offer** (larger hit targets); **secondary** CRM / park / purchase / jump-to-tab live under **More actions** (dropdown + outside click + Escape). **Overview** is **stacked ContactCards** (identity → appointment → codes → location) plus a fixed-width **sidebar** for people & assignments. **Appointments** tab uses **one card per appointment** instead of a wide dense table. **Vehicle** and **Correspondence** use the same **card** pattern; footer **Save / Discard** are larger. New **i18n** keys for card titles, hints, header hint, more-actions label, vehicle-extra hint, appointment card label (de, en, fr).

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `ContactCard`, layout, history cards, toolbar
- `frontend/src/contexts/LanguageContext.tsx` — new keys (de, en, fr)
- `docs/FEATURE-LOG.md`, this file

### What It Does (Technical)
`ContactCard` wraps sections with optional `LucideIcon`; `actionsMenuRef` + mousedown closes menu; Escape closes menu before modal.

### Data It Accepts / Emits
N/A (presentation only; same `onPersist` / draft model).

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
`timetableContactHeaderHint`, `timetableContactMoreActions`, `timetableContactCardIdentity`, `timetableContactCardIdentityHint`, `timetableContactCardAppointment`, `timetableContactCardAppointmentHint`, `timetableContactCardClassification`, `timetableContactCardClassificationHint`, `timetableContactCardLocation`, `timetableContactCardLocationHint`, `timetableContactCardPeopleHint`, `timetableContactCardAssignHint`, `timetableContactAppointmentCardLabel`, `timetableContactVehicleExtraHint` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
**Extended (same day):** Dialog **max-width** raised to **~90rem** (`min(90rem, 100vw - 2rem)`), **removed** the previous **min-height** so the shell is not artificially tall; **max-height** caps at **92vh**. Overview uses **lg** breakpoint for main + sidebar row; main cards use a **2xl** two-column grid where space allows. History / vehicle / correspondence content uses **full modal width**. Correspondence textarea **min-height** reduced slightly so vertical space is not inflated.

---

## FEATURE-162: Kundenkontakt overview — compact columns, wide long-text row only
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The **Overview** tab no longer stretches every block to the full modal width. On **xl** screens, the **first row** uses a **12-column grid** with **narrower** columns for identity (**3**), appointment (**4**), and contacts + assignment (**5**). A **second full-width row** holds only **Address, Fleet, and Call / list notes** with **taller, resizable** text areas so long notes stay readable. The **dialog shell** is **capped** at **`min(80rem, 100vw - inset)`** so the modal does not span the entire monitor. Removed an **unused** overview textarea CSS constant after consolidating long-form styling.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — xl grid splits, location card full-width row, dialog `w`/`max-w`, dead code removal
- `docs/FEATURE-LOG.md`, this file

### What It Does (Technical)
1. Overview scroll area uses `xl:grid-cols-12` with `col-span-3` / `4` / `5` for the first row and `xl:col-span-12` for the address/fleet/notes `ContactCard`.
2. Long-form fields use `overviewNotesTextareaClass` (`resize-y`, comfortable `min-h`); other overview inputs use tighter `overviewInputClass` / `overviewLabelClass` where applicable.
3. Modal panel width classes: `w-[min(80rem,calc(100vw-12px))]` (sm breakpoint uses 16px inset).

### Data It Accepts / Emits
N/A (layout and styling only; same draft / `onPersist` model).

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Below **xl**, overview remains a **single-column** stack; only **xl** gets the compact three-column top row. Adjust **80rem** or add **lg** mirroring if agents need the split on smaller desktops.

---

## FEATURE-163: Kundenkontakt overview aligned with customer edit (Kunde tab) layout
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The **Overview** tab is restructured to follow the same **panel grid** and **visual language** as **`NewCustomerModal`** on the **Kunde** tab: **`rounded-xl`**, **`border-slate-200/80`**, **`p-4`**, **`shadow-sm`**, **uppercase section titles** (`text-[11px]` / hint `text-[10px]`), and **field labels** matching the customer form (**uppercase `text-xs` semibold**). The main area uses **`lg:grid-cols-12`** with **three balanced columns (`lg:col-span-4` each)** for identity + codes, appointment/outcome, and contact persons + assignment; **Address / Fleet / notes** remain a **full-width** fourth row (`lg:col-span-12`). **Add** actions sit in the **panel header** row (like address “New” in customer edit). **Inputs** reuse the same **blue focus ring** pattern as **`inputClass`** in `NewCustomerModal`. **`ContactCard`** on other tabs is simplified (removed unused **`dense`** variant). Unused **`MapPin` / `Users`** imports removed from the drawer.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `OverviewPanel`, overview grid/`lg` breakpoints, field markup, `ContactCard` cleanup
- `docs/FEATURE-LOG.md`, this file

### What It Does (Technical)
1. `OverviewPanel` renders header (title, hint, optional `actions`) + `mt-4` body, same shell classes as customer kunde column panels.
2. Overview fields use `<label className={overviewFieldClass}>` with `<span className={overviewLabelClass}>` + control; `overviewInputClass` / `overviewNotesTextareaClass` align with customer chrome.
3. Top-row layout activates from **`lg`** (not only `xl`), improving laptops just below the previous breakpoint.

### Data It Accepts / Emits
N/A (presentation only; same draft / `onPersist`).

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Other tabs (**History**, **Vehicle**, **Activity**) still use **`ContactCard`** with icons — only **Overview** matches the flat customer-edit panel style. Checkbox labels in the appointment panel use the same uppercase label styling as overview fields; adjust if readability feedback prefers sentence case for toggles.

---

## FEATURE-164: Kundenkontakt modal — wider shell, shorter height, parallel tabs
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The **customer contact** dialog is **wider** (`**92rem**` max width instead of **80rem**) and **shorter** vertically (**`78dvh`** cap vs near-full viewport). The **tab bar** uses a fixed **`grid-cols-4`** so **Overview**, **Appointments**, **Vehicle**, and **Correspondence** stay **parallel** at all breakpoints (compact icon+label, `line-clamp` for long translations). The **toolbar** under tabs stays on **one row** with horizontal scroll if needed (`flex-nowrap`). **Header**, **footer**, and **Overview** panels use **tighter padding**; **identity** fields use a **2-column grid on `lg`** to reduce stack height; **notes** text areas use **smaller default rows / min-height**.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — shell dimensions, nav grid, toolbar, overview density
- `docs/FEATURE-LOG.md`, this file

### What It Does (Technical)
1. Dialog: `h`/`max-h` → `min(78dvh, calc(100dvh − inset))`; `w`/`max-w` → `min(92rem, calc(100vw − inset))`.
2. `<nav role="tablist">`: `grid w-full grid-cols-4 gap-1` with per-tab `min-w-0`, stacked icon+label on xs, row layout from `sm`.
3. Overview: `OverviewPanel` padding/hint clamp; identity inner grid `lg:grid-cols-2`; `overviewInputClass` slightly shorter on xs; `overviewNotesTextareaClass` and `rows={3}`.

### Data It Accepts / Emits
N/A.

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Very narrow phones may show **two-line tab labels** (`line-clamp-2`). If **78dvh** feels tight on small laptops, raise to **82dvh** or restore content-driven height.

---

## FEATURE-166: Kundenkontakt overview — fix address card overlap + modern cards
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The **Übersicht** tab no longer places the full-width **Address, fleet & quick notes** panel in the same 12-column grid row as the three main columns (which could visually overlap **Codes & profile**). Layout is now **two blocks**: a **top** `lg:grid-cols-12` row (identity + codes | appointment | contacts/assignment) and a **separate** full-width section below with a **top border**. **Overview** cards use **rounded-2xl**, **ring**, and **softer shadow**; scroll area shows a **thin scrollbar**; **Angebot bearbeiten** uses **emerald** outline styling aligned with the primary actions.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — overview layout, `OverviewPanel` shell, offer button classes
- `docs/FEATURE-LOG.md`, this file — FEATURE-166

### What It Does (Technical)
1. Wraps the three-column grid and the location `OverviewPanel` in a vertical flex stack; location is **outside** the 12-col grid.
2. `overflow-y-auto` on an inner scroller with `min-h-0` preserves modal height without hiding stacked sections.

### Data It Accepts / Emits
N/A.

### Database
N/A.

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
N/A.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---
