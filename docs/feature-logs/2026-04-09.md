# Feature Log — 2026-04-09

> Features added or modified on this date.

---

## FEATURE-143: Customer domain context pack for LLM/agent onboarding
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Added  

### What Was Added
A **curated documentation pack** under `docs/context/customer/` so engineers and generative models can understand the customer module **without pasting large source files**: hub README with mandatory read order and golden rules, glossary (German terms ↔ fields), module map (frontend/backend paths only), API/persistence **contracts**, implemented vs deferred **capabilities**, and a **changelog bridge** to `FEATURE-LOG.md`. Cross-links from `docs/README.md` (Assistant / LLM context), `docs/api/customer-api.md`, and `docs/operations/agent-playbook.md` (DoD + optional OpenAPI curl). **OpenAPI export** helper doc and **GitHub PR template** checklist for customer-context updates.

### Where It Was Added
- `docs/context/customer/README.md` — entry hub, read order, rules, OpenAPI pointer
- `docs/context/customer/glossary.md` — domain terms
- `docs/context/customer/module-map.md` — concern → path table
- `docs/context/customer/contracts.md` — endpoints, errors, persistence matrix
- `docs/context/customer/capabilities.md` — implemented vs roadmap
- `docs/context/customer/changelog-bridge.md` — FEATURE-LOG keywords
- `docs/context/customer/openapi-export.md` — snapshot commands
- `docs/README.md` — Assistant / LLM section; CustomersPage spec link
- `docs/api/customer-api.md` — pointer to context hub
- `docs/operations/agent-playbook.md` — §7 DoD + OpenAPI note
- `.github/pull_request_template.md` — customer context checklist
- `docs/FEATURE-LOG.md`, this file — FEATURE-143

### What It Does (Technical)
1. Establishes a **single entry path** for agents: read hub → contracts → glossary → module map → capabilities → ADR/API/product/spec → FEATURE-LOG.
2. Documents **known error codes** and **VAT timeout** behavior at summary level with links to `vat-api.md` and runtime OpenAPI.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| N/A | N/A | N/A | Documentation only |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
N/A (docs reference existing endpoints).

### State / Store (if applicable)
N/A.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Deep behavior remains in `docs/Detailed report/CustomersPage-Service-Spec.md`; this pack is intentionally short. Update `contracts.md` / `capabilities.md` when shipping customer changes.

---

## FEATURE-144: Shared customers load — visible errors and env trim
**Date:** 2026-04-09  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
When **API mode** is enabled but the first **GET `/api/v1/demo/customers-db`** fails (CORS, 401, network, etc.), the Customers page now shows an **amber banner** with a clear explanation and optional **technical message**, instead of failing **silently** and leaving users thinking only local storage applies. **`VITE_CUSTOMERS_SOURCE`** and **`VITE_API_BASE_URL`** are **trimmed** at build time so accidental spaces in Vercel env values no longer disable API mode.

### Where It Was Added
- `frontend/src/pages/CustomersPage.tsx` — initial shared-load `catch` sets `syncNotice` with i18n
- `frontend/src/store/kundenStore.ts` — `.trim()` on customer source and API base URL
- `frontend/src/contexts/LanguageContext.tsx` — `customersSharedLoadFailed*`, `customersSharedLoadDetail` in de/en/fr
- `docs/FEATURE-LOG.md`, this file — FEATURE-144

### What It Does (Technical)
1. `useEffect` for shared load: on error, builds a notice from `t("customersSharedLoadFailed")`, `t("customersSharedLoadFailedHint")`, and optional `t("customersSharedLoadDetail")` with `{detail}` from `Error.message`.
2. `API_MODE` and `API_BASE` use trimmed env strings before comparison / URL construction.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| N/A | N/A | N/A | UI copy and env parsing only |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
N/A (still calls existing demo customers-db endpoint; no new routes).

### State / Store (if applicable)
- **Store file:** `frontend/src/store/kundenStore.ts` (env parsing only)
- **Persisted:** No

### i18n Keys Added
`customersSharedLoadFailed`, `customersSharedLoadFailedHint`, `customersSharedLoadDetail` (de, en, fr).

### Dependencies Added
None.

### Notes / Known Limitations
Does not change Render or Vercel configuration; operators must still fix CORS and build-time env. The banner makes misconfiguration obvious without opening DevTools first.

---
