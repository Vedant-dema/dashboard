# Feature Log — 2026-04-13

> Features added or modified on this date.

---

## FEATURE-185: Timetable toolbar — filter popover visible (overflow fix)
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The purchase timetable (“calendar”) toolbar filter funnel now opens a fully visible quick-filters panel. Previously the panel was clipped because its parent card used `overflow-hidden` while the menu is absolutely positioned and does not expand the card’s layout height.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — toolbar glass card: `overflow-hidden` → `overflow-visible` so filter, day-picker, and actions popovers can extend below the card without being clipped.

### What It Does (Technical)
1. User toggles `filtersOpen` on the filter button.
2. A `position: absolute` dialog renders with `top: calc(100% + 0.35rem)` under the filter control.
3. The toolbar wrapper no longer clips overflow, so the dialog paints outside the in-flow height of the card (absolute children do not contribute to parent height).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | No data contract change |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
If future content inside this card must be strictly clipped to rounded corners, scope `overflow-hidden` to an inner wrapper that does not contain these popovers, or use a portal/fixed positioning for menus.

---

## FEATURE-186: Timetable — single-day legacy demo seeds; clear demos on first saved appointment
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The purchase timetable ships with **legacy Termin-Kalender demo rows** (same companies/remarks as the reference screenshot) on **today’s date** so the day view and mini-calendar look populated out of the box. When the user saves a **new appointment** from “New appointment” (non-empty company), all rows owned by the shared demo buyer (`DEMO`) are removed so only real data remains.

### Where It Was Added
- `frontend/src/store/timetableStore.ts` — replace spread-across-three-days seeding with `demoSeedsForDay()`; add `stripTimetableDemoBuyerRows()`; simplify `fallbackState` / `ensureSharedDemoTimetableRows`; remove unused `addDaysIso`.
- `frontend/src/pages/timetable/TimetablePage.tsx` — after `addTimetableEntries` in `createSlot`, call `stripTimetableDemoBuyerRows`.

### What It Does (Technical)
1. Empty or invalid storage → `fallbackState()` loads `SEED_ENTRIES_RAW` remapped to **today** with ids `1..n`.
2. Valid storage without any `buyer_name === DEMO` row → `ensureSharedDemoTimetableRows` appends the same demo set starting at `nextId`, all on today.
3. User submits create form with company → append row with viewer buyer code, then filter out all `DEMO` buyer entries.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `buyer_name` | string | Yes | `TIMETABLE_DEMO_BUYER` (`DEMO`) marks removable demo rows |

### Database
- **Engine:** Browser `localStorage` (`dema-purchase-timetable-v6`)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** N/A

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts`
- **Actions/Selectors added:** `stripTimetableDemoBuyerRows`
- **Persisted:** Yes (same timetable JSON blob)

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Demo rows are not removed when inserting five empty drafts or when only editing demo rows in the contact drawer; removal is tied to saving a **new** row from the create panel. Existing persisted timetables keep their data unchanged (no storage-key bump).

---

## FEATURE-187: Timetable contact drawer — wider overview column for address / fleet / notes
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
In the contact drawer **Overview** horizontal strip, the **Address, fleet & quick notes** card column is slightly wider than the other columns so long address and note text have more room.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — new `overviewHScrollColLocationClass`; location column wrapper uses it instead of `overviewHScrollColClass`.

### What It Does (Technical)
1. Other overview columns keep `overviewHScrollColClass` (~19.25–22rem).
2. Location column uses wider min/max widths (~21.5–27rem from `sm` up).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---


## FEATURE-188: Timetable toolbar — remove Actions dropdown; print + insert five as direct buttons
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The timetable **Actions** / **More actions** dropdown is removed. **Print list** and **Insert five drafts** are now primary toolbar buttons next to **New appointment** (icon-only on narrow widths with `title` / `aria-label` for accessibility). The placeholder **Move in list** (toast only) entry is removed.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — drop `actionsMenuOpen`, `actionsMenuRef`, outside-click effect, dropdown JSX; add two `button` controls; remove unused `ChevronDown` / `MoreHorizontal` imports.

### What It Does (Technical)
1. Print: closes filter popover if open, then `window.print()`.
2. Insert five: closes filter popover, runs `insertFiveDraftTimetableRows` for the selected day and shows the existing toast.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
`MESSAGES` keys for “Move in list” / bulk-move toast remain for possible future use.

---

## FEATURE-189: Timetable toolbar — remove print + insert-five buttons (no secondary actions)
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
Following removal of the Actions dropdown (FEATURE-188), the replacement **Print** and **Insert five drafts** toolbar buttons are removed so the timetable header only shows filter, search, day navigation, and **New appointment** — no extra actions that were easy to miss or redundant for the user.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — delete print/insert-five `button` block, `handlePrint` / `handleInsertFiveDrafts`, and unused `Printer` / `ListPlus` / `insertFiveDraftTimetableRows` imports.

### What It Does (Technical)
1. Users can still print via the browser (e.g. Ctrl/Cmd+P) where print CSS applies.
2. `insertFiveDraftTimetableRows` remains available in `timetableStore` for a future UI entry if needed.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---


## FEATURE-190: Timetable contact drawer — remove “More actions” overflow menu
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **Customer contact** drawer header no longer shows the **More actions** dropdown (next to **Smart summary**). That menu duplicated shortcuts that already exist: **Vehicle** / **Correspondence** tabs, **Parked** and **Purchase confirmed** on the Overview card, and placeholder CRM entries (“Customer data”, “Open customer form”) that only showed a toast.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — remove actions menu state, ref, menu JSX, outside-click / Escape branches; simplify Smart summary click-outside effect; drop unused `onComingSoon` prop and lucide imports only used by the menu.
- `frontend/src/pages/timetable/TimetablePage.tsx` — remove `onComingSoon` prop from `TimetableContactDrawer`.

### What It Does (Technical)
1. Tab change still closes the Smart summary popover.
2. Escape closes Smart summary popover, then drawer (unchanged flow minus actions menu step).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
`MESSAGES` keys such as `timetableContactMoreActions` remain for possible reuse; no new keys added.

---

## FEATURE-191: Timetable contact drawer — wider Address / fleet / notes overview column
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **Address, fleet & quick notes** overview card in the contact drawer horizontal strip is widened again (extends FEATURE-187) so long notes and addresses get more horizontal space.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — increase `overviewHScrollColLocationClass` widths (base / `sm` / `md` min and max).

### What It Does (Technical)
1. Location column uses `w-[min(...,24rem)]`, `sm:w-[25rem]`, `md:min-w-[27rem] md:max-w-[31rem]` vs prior 21.5 / 22 / 23.5–27rem.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---


## FEATURE-192: Timetable contact drawer — remove overview card hint subtitles
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The grey instructional lines under each **Overview** column title (who you are calling, appointment & outcome, address / fleet / notes, contact persons, assignment) are removed so only the uppercase section titles remain above the fields.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — drop all `hint={t(...)}` props from `OverviewPanel` usages in the overview horizontal strip (`OverviewPanel` still supports optional `hint` for reuse).

### What It Does (Technical)
1. `OverviewPanel` renders the hint `<p>` only when `hint` is passed; with no prop, the block is omitted.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Existing `timetableContactCard*Hint` translation keys remain in `LanguageContext.tsx` unused until removed in a separate i18n cleanup if desired.

---

## FEATURE-193: Timetable contact drawer — fix right-side clipping (toolbar + overview strip)
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
Overview **Smart summary** and the **Contact persons** / **Assignment** **Add** controls no longer sit flush against a hard clip: the sub-header action row can wrap on small widths, overview cards with header actions stack title then actions in narrow columns, and the horizontal overview scroller gains trailing padding so the last column stays fully visible when scrolled.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `OverviewPanel` header layout; tab-strip toolbar flex; overview `w-max` row `pr-*` / `pb-1`.

### What It Does (Technical)
1. Toolbar: `min-w-0`, `flex-wrap` by default, `sm:flex-nowrap`; Smart summary group `w-full justify-end` on small screens, `sm:w-auto`.
2. `OverviewPanel` with `actions`: column stack below `sm`, row + `justify-between` from `sm`; actions `self-end` / `sm:self-start`.
3. Horizontal strip: `pr-4 sm:pr-5 md:pr-6` on the inner flex row.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-194: Timetable contact drawer — slightly wider dialog shell
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The customer **contact** modal uses a slightly larger maximum width and a bit more usable viewport width (`100vw` minus smaller gutters, plus `lg:` margins) so the four-column overview form has more room and the right column is less cramped.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — dialog shell `w` / `max-w` classes (`92rem` → `96rem`; `calc(100vw-12px/16px)` → `10px/14px`; add `lg:… calc(100vw-20px)`).

### What It Does (Technical)
1. Cap raised from **92rem** (~1472px at 16px root) to **96rem** (~1536px).
2. Viewport-based width uses slightly tighter side subtraction so the shell can grow a few pixels on typical laptop widths.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-195: Timetable contact drawer — remove header “Demo” CRM badge
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The red **Demo** pill next to the CRM snapshot (customer number / industry) in the contact drawer header is removed. Demo timetable rows still use buyer code `DEMO` in the **Einkäufer** field when applicable; only the decorative header badge is gone.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — remove `showDemoBadge` from `headerCrmDisplay` and delete the conditional badge JSX.

### What It Does (Technical)
1. `headerCrmDisplay` now returns only `kuNr` and `branche` (unchanged fallback chain for displayed values).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
`MESSAGES.timetableContactDemoBadge` (and related demo snapshot keys) remain unused in this component until optional i18n cleanup.

---

## FEATURE-196: Timetable contact drawer — remove header subtitle hint
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The grey subtitle under the company title (“Work through the cards below — save when the call is done.”) is removed from the customer contact drawer header.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — delete the `<p>` that used `timetableContactHeaderHint`.

### What It Does (Technical)
1. Header now shows **Customer contact** label, company `h2`, then the status / codes row without the intermediate hint line.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
`timetableContactHeaderHint` remains in `LanguageContext.tsx` unused until optional i18n cleanup.

---

## FEATURE-197: Cursor rule — i18n 12-language full coverage agent
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Added 

### What Was Added
An always-applied Cursor rule (`.cursor/rules/i18n-12-language-coverage-agent.mdc`) that instructs the agent to keep **all 12 dashboard languages** in sync when adding or changing UI copy, to show **only the selected language** (no stray literals), and to re-verify i18n on every relevant edit.

### Where It Was Added
- `.cursor/rules/i18n-12-language-coverage-agent.mdc` — new rule file.

### What It Does (Technical)
1. Documents `LanguageContext.tsx` as source of truth and the de/en/fr + `LANGUAGE_OVERRIDES` model.
2. Mandates parity workflow and regression checks; defers literal “infinite” execution to per-session enforcement.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Complements existing `.cursor/rules/i18n-always.mdc`; does not add runtime automation.

---

## FEATURE-198: Timetable (calendar) module — locale + empty-cell i18n hardening
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The purchase timetable page now derives `Intl` locale tags from the shared `localeTagForLanguage` helper (all 12 language codes, consistent with the rest of the app). New drafts and the “create row” form no longer default the **purpose** field to the German word “Anruf”. Empty table cells use the existing `commonPlaceholderDash` translation key so placeholders stay behind the i18n layer.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — `localeTagForLanguage`; removed duplicate `pickLocaleTag`; empty default purpose; save/reset behaviour.
- `frontend/src/pages/timetable/TimetableTable.tsx` — `useMemo` + `commonPlaceholderDash` for empty cells; robust “missing phone” detection for em dash / hyphen placeholders.
- `frontend/src/store/timetableStore.ts` — `createDraftTimetableEntry` and `insertFiveDraftTimetableRows` use empty `purpose` instead of German default.

### What It Does (Technical)
1. `TimetablePage` calls `localeTagForLanguage(language as LanguageCode)` for dates/times/mini-calendar.
2. Draft rows store `purpose: ''`; the create form starts and resets with `purpose: ''`; saved payload uses trimmed user input only.
3. `TimetableTable` memoizes `t('commonPlaceholderDash', '—')` and uses it for empty contact/purpose/notes/follow-up/phone display; phone column treats Unicode em dash, hyphen, and en dash as empty for the tel link.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `purpose` | string | No | May be empty for new user rows; demo seed rows unchanged (still German legacy copy). |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts`
- **Actions/Selectors added:** None (default field value change only)
- **Persisted:** Yes (existing timetable storage key)

### i18n Keys Added
None (reuses `commonPlaceholderDash`).

### Dependencies Added
None.

### Notes / Known Limitations
Demo `SEED_ENTRIES_RAW` content remains German sample data by design; only UI defaults and empty placeholders were aligned with multi-language rules.

---

## FEATURE-199: Timetable customer contact drawer — full de/en/fr message catalogue
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
All user-visible strings for the **customer contact** workspace (tabs Overview / Appointments / Vehicle / Correspondence, smart summary, footer, CRM strip, and related keys) now resolve from **`MESSAGES.de` / `MESSAGES.en` / `MESSAGES.fr`** instead of falling through to English fallbacks. Added missing **`timetableColKa`**, **`timetableColKaPh`**, **`timetableColArte`**, **`timetableRowParkedTag`**, **`timetableContactSaved`**, **`timetableFieldNotesAppendPlaceholder`**, and the full **`timetableContact*`** catalogue.

### Where It Was Added
- `frontend/src/contexts/LanguageContext.tsx` — German, English, and French entries for the keys above.
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — smart-summary slot line uses `{outcome} · {slot}`; empty company title and slot use `commonPlaceholderDash`; registration field uses `timetableContactRegistrationPh`.

### What It Does (Technical)
1. `t(key)` for `language === "de"` reads `MESSAGES.de[key]`; previously missing keys fell back to `MESSAGES.en` or the JSX English fallback.
2. Secondary locales that use live MT still batch-translate from **`MESSAGES.en`**, so English strings remain the canonical source for those languages.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Copy-only change |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
`timetableColKa`, `timetableColKaPh`, `timetableColArte`, `timetableRowParkedTag`, `timetableContactSaved`, `timetableFieldNotesAppendPlaceholder`, and all `timetableContact*` keys used by `TimetableContactDrawer` / timetable toast (see `LanguageContext.tsx`).

### Dependencies Added
None.

### Notes / Known Limitations
User-editable row content (notes, company names) is not translated. ES / IT / PT / TR / RU / HI / AR / ZH / JA still inherit from `en` or machine translation unless overrides are added later.

---

