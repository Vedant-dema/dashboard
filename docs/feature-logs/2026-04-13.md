# Feature Log — 2026-04-13

> Features added or modified on this date.

---

## FEATURE-185: Timetable toolbar — filter popover visible (overflow fix)
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The purchase timetable (“calendar”) toolbar filter funnel now opens a fully visible quick-filters panel. Previously the panel was clipped because its parent card used `overflow-hidden` while the menu is absolutely positioned and does not expand the card’s layout height.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — toolbar glass card: `overflow-hidden` → `overflow-visible` so filter, day-picker, and actions popovers can extend below the card without being clipped.

### What It Does (Technical)
1. User toggles `filtersOpen` on the filter button.
2. A `position: absolute` dialog renders with `top: calc(100% + 0.35rem)` under the filter control.
3. The toolbar wrapper no longer clips overflow, so the dialog paints outside the in-flow height of the card (absolute children do not contribute to parent height).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | No data contract change |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
If future content inside this card must be strictly clipped to rounded corners, scope `overflow-hidden` to an inner wrapper that does not contain these popovers, or use a portal/fixed positioning for menus.

---

## FEATURE-186: Timetable — single-day legacy demo seeds; clear demos on first saved appointment
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The purchase timetable ships with **legacy Termin-Kalender demo rows** (same companies/remarks as the reference screenshot) on **today’s date** so the day view and mini-calendar look populated out of the box. When the user saves a **new appointment** from “New appointment” (non-empty company), all rows owned by the shared demo buyer (`DEMO`) are removed so only real data remains.

### Where It Was Added
- `frontend/src/store/timetableStore.ts` — replace spread-across-three-days seeding with `demoSeedsForDay()`; add `stripTimetableDemoBuyerRows()`; simplify `fallbackState` / `ensureSharedDemoTimetableRows`; remove unused `addDaysIso`.
- `frontend/src/pages/timetable/TimetablePage.tsx` — after `addTimetableEntries` in `createSlot`, call `stripTimetableDemoBuyerRows`.

### What It Does (Technical)
1. Empty or invalid storage → `fallbackState()` loads `SEED_ENTRIES_RAW` remapped to **today** with ids `1..n`.
2. Valid storage without any `buyer_name === DEMO` row → `ensureSharedDemoTimetableRows` appends the same demo set starting at `nextId`, all on today.
3. User submits create form with company → append row with viewer buyer code, then filter out all `DEMO` buyer entries.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `buyer_name` | string | Yes | `TIMETABLE_DEMO_BUYER` (`DEMO`) marks removable demo rows |

### Database
- **Engine:** Browser `localStorage` (`dema-purchase-timetable-v6`)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** N/A

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts`
- **Actions/Selectors added:** `stripTimetableDemoBuyerRows`
- **Persisted:** Yes (same timetable JSON blob)

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Demo rows are not removed when inserting five empty drafts or when only editing demo rows in the contact drawer; removal is tied to saving a **new** row from the create panel. Existing persisted timetables keep their data unchanged (no storage-key bump).

---

## FEATURE-187: Timetable contact drawer — wider overview column for address / fleet / notes
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
In the contact drawer **Overview** horizontal strip, the **Address, fleet & quick notes** card column is slightly wider than the other columns so long address and note text have more room.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — new `overviewHScrollColLocationClass`; location column wrapper uses it instead of `overviewHScrollColClass`.

### What It Does (Technical)
1. Other overview columns keep `overviewHScrollColClass` (~19.25–22rem).
2. Location column uses wider min/max widths (~21.5–27rem from `sm` up).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---


## FEATURE-188: Timetable toolbar — remove Actions dropdown; print + insert five as direct buttons
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The timetable **Actions** / **More actions** dropdown is removed. **Print list** and **Insert five drafts** are now primary toolbar buttons next to **New appointment** (icon-only on narrow widths with `title` / `aria-label` for accessibility). The placeholder **Move in list** (toast only) entry is removed.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — drop `actionsMenuOpen`, `actionsMenuRef`, outside-click effect, dropdown JSX; add two `button` controls; remove unused `ChevronDown` / `MoreHorizontal` imports.

### What It Does (Technical)
1. Print: closes filter popover if open, then `window.print()`.
2. Insert five: closes filter popover, runs `insertFiveDraftTimetableRows` for the selected day and shows the existing toast.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
`MESSAGES` keys for “Move in list” / bulk-move toast remain for possible future use.

---

## FEATURE-189: Timetable toolbar — remove print + insert-five buttons (no secondary actions)
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
Following removal of the Actions dropdown (FEATURE-188), the replacement **Print** and **Insert five drafts** toolbar buttons are removed so the timetable header only shows filter, search, day navigation, and **New appointment** — no extra actions that were easy to miss or redundant for the user.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — delete print/insert-five `button` block, `handlePrint` / `handleInsertFiveDrafts`, and unused `Printer` / `ListPlus` / `insertFiveDraftTimetableRows` imports.

### What It Does (Technical)
1. Users can still print via the browser (e.g. Ctrl/Cmd+P) where print CSS applies.
2. `insertFiveDraftTimetableRows` remains available in `timetableStore` for a future UI entry if needed.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---


## FEATURE-190: Timetable contact drawer — remove “More actions” overflow menu
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **Customer contact** drawer header no longer shows the **More actions** dropdown (next to **Smart summary**). That menu duplicated shortcuts that already exist: **Vehicle** / **Correspondence** tabs, **Parked** and **Purchase confirmed** on the Overview card, and placeholder CRM entries (“Customer data”, “Open customer form”) that only showed a toast.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — remove actions menu state, ref, menu JSX, outside-click / Escape branches; simplify Smart summary click-outside effect; drop unused `onComingSoon` prop and lucide imports only used by the menu.
- `frontend/src/pages/timetable/TimetablePage.tsx` — remove `onComingSoon` prop from `TimetableContactDrawer`.

### What It Does (Technical)
1. Tab change still closes the Smart summary popover.
2. Escape closes Smart summary popover, then drawer (unchanged flow minus actions menu step).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
`MESSAGES` keys such as `timetableContactMoreActions` remain for possible reuse; no new keys added.

---

## FEATURE-191: Timetable contact drawer — wider Address / fleet / notes overview column
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **Address, fleet & quick notes** overview card in the contact drawer horizontal strip is widened again (extends FEATURE-187) so long notes and addresses get more horizontal space.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — increase `overviewHScrollColLocationClass` widths (base / `sm` / `md` min and max).

### What It Does (Technical)
1. Location column uses `w-[min(...,24rem)]`, `sm:w-[25rem]`, `md:min-w-[27rem] md:max-w-[31rem]` vs prior 21.5 / 22 / 23.5–27rem.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---


## FEATURE-192: Timetable contact drawer — remove overview card hint subtitles
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The grey instructional lines under each **Overview** column title (who you are calling, appointment & outcome, address / fleet / notes, contact persons, assignment) are removed so only the uppercase section titles remain above the fields.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — drop all `hint={t(...)}` props from `OverviewPanel` usages in the overview horizontal strip (`OverviewPanel` still supports optional `hint` for reuse).

### What It Does (Technical)
1. `OverviewPanel` renders the hint `<p>` only when `hint` is passed; with no prop, the block is omitted.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Existing `timetableContactCard*Hint` translation keys remain in `LanguageContext.tsx` unused until removed in a separate i18n cleanup if desired.

---

## FEATURE-193: Timetable contact drawer — fix right-side clipping (toolbar + overview strip)
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
Overview **Smart summary** and the **Contact persons** / **Assignment** **Add** controls no longer sit flush against a hard clip: the sub-header action row can wrap on small widths, overview cards with header actions stack title then actions in narrow columns, and the horizontal overview scroller gains trailing padding so the last column stays fully visible when scrolled.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `OverviewPanel` header layout; tab-strip toolbar flex; overview `w-max` row `pr-*` / `pb-1`.

### What It Does (Technical)
1. Toolbar: `min-w-0`, `flex-wrap` by default, `sm:flex-nowrap`; Smart summary group `w-full justify-end` on small screens, `sm:w-auto`.
2. `OverviewPanel` with `actions`: column stack below `sm`, row + `justify-between` from `sm`; actions `self-end` / `sm:self-start`.
3. Horizontal strip: `pr-4 sm:pr-5 md:pr-6` on the inner flex row.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-194: Timetable contact drawer — slightly wider dialog shell
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The customer **contact** modal uses a slightly larger maximum width and a bit more usable viewport width (`100vw` minus smaller gutters, plus `lg:` margins) so the four-column overview form has more room and the right column is less cramped.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — dialog shell `w` / `max-w` classes (`92rem` → `96rem`; `calc(100vw-12px/16px)` → `10px/14px`; add `lg:… calc(100vw-20px)`).

### What It Does (Technical)
1. Cap raised from **92rem** (~1472px at 16px root) to **96rem** (~1536px).
2. Viewport-based width uses slightly tighter side subtraction so the shell can grow a few pixels on typical laptop widths.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-195: Timetable contact drawer — remove header “Demo” CRM badge
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The red **Demo** pill next to the CRM snapshot (customer number / industry) in the contact drawer header is removed. Demo timetable rows still use buyer code `DEMO` in the **Einkäufer** field when applicable; only the decorative header badge is gone.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — remove `showDemoBadge` from `headerCrmDisplay` and delete the conditional badge JSX.

### What It Does (Technical)
1. `headerCrmDisplay` now returns only `kuNr` and `branche` (unchanged fallback chain for displayed values).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
`MESSAGES.timetableContactDemoBadge` (and related demo snapshot keys) remain unused in this component until optional i18n cleanup.

---

## FEATURE-196: Timetable contact drawer — remove header subtitle hint
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The grey subtitle under the company title (“Work through the cards below — save when the call is done.”) is removed from the customer contact drawer header.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — delete the `<p>` that used `timetableContactHeaderHint`.

### What It Does (Technical)
1. Header now shows **Customer contact** label, company `h2`, then the status / codes row without the intermediate hint line.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
`timetableContactHeaderHint` remains in `LanguageContext.tsx` unused until optional i18n cleanup.

---

## FEATURE-197: Cursor rule — i18n 12-language full coverage agent
**Date:** 2026-04-13 
**Author/Agent:** Cursor AI 
**Status:** Added 

### What Was Added
An always-applied Cursor rule (`.cursor/rules/i18n-12-language-coverage-agent.mdc`) that instructs the agent to keep **all 12 dashboard languages** in sync when adding or changing UI copy, to show **only the selected language** (no stray literals), and to re-verify i18n on every relevant edit.

### Where It Was Added
- `.cursor/rules/i18n-12-language-coverage-agent.mdc` — new rule file.

### What It Does (Technical)
1. Documents `LanguageContext.tsx` as source of truth and the de/en/fr + `LANGUAGE_OVERRIDES` model.
2. Mandates parity workflow and regression checks; defers literal “infinite” execution to per-session enforcement.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Complements existing `.cursor/rules/i18n-always.mdc`; does not add runtime automation.

---

## FEATURE-198: Timetable (calendar) module — locale + empty-cell i18n hardening
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The purchase timetable page now derives `Intl` locale tags from the shared `localeTagForLanguage` helper (all 12 language codes, consistent with the rest of the app). New drafts and the “create row” form no longer default the **purpose** field to the German word “Anruf”. Empty table cells use the existing `commonPlaceholderDash` translation key so placeholders stay behind the i18n layer.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — `localeTagForLanguage`; removed duplicate `pickLocaleTag`; empty default purpose; save/reset behaviour.
- `frontend/src/pages/timetable/TimetableTable.tsx` — `useMemo` + `commonPlaceholderDash` for empty cells; robust “missing phone” detection for em dash / hyphen placeholders.
- `frontend/src/store/timetableStore.ts` — `createDraftTimetableEntry` and `insertFiveDraftTimetableRows` use empty `purpose` instead of German default.

### What It Does (Technical)
1. `TimetablePage` calls `localeTagForLanguage(language as LanguageCode)` for dates/times/mini-calendar.
2. Draft rows store `purpose: ''`; the create form starts and resets with `purpose: ''`; saved payload uses trimmed user input only.
3. `TimetableTable` memoizes `t('commonPlaceholderDash', '—')` and uses it for empty contact/purpose/notes/follow-up/phone display; phone column treats Unicode em dash, hyphen, and en dash as empty for the tel link.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `purpose` | string | No | May be empty for new user rows; demo seed rows unchanged (still German legacy copy). |

### Database
- **Engine:** Browser `localStorage`
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts`
- **Actions/Selectors added:** None (default field value change only)
- **Persisted:** Yes (existing timetable storage key)

### i18n Keys Added
None (reuses `commonPlaceholderDash`).

### Dependencies Added
None.

### Notes / Known Limitations
Demo `SEED_ENTRIES_RAW` content remains German sample data by design; only UI defaults and empty placeholders were aligned with multi-language rules.

---

## FEATURE-199: Timetable customer contact drawer — full de/en/fr message catalogue
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
All user-visible strings for the **customer contact** workspace (tabs Overview / Appointments / Vehicle / Correspondence, smart summary, footer, CRM strip, and related keys) now resolve from **`MESSAGES.de` / `MESSAGES.en` / `MESSAGES.fr`** instead of falling through to English fallbacks. Added missing **`timetableColKa`**, **`timetableColKaPh`**, **`timetableColArte`**, **`timetableRowParkedTag`**, **`timetableContactSaved`**, **`timetableFieldNotesAppendPlaceholder`**, and the full **`timetableContact*`** catalogue.

### Where It Was Added
- `frontend/src/contexts/LanguageContext.tsx` — German, English, and French entries for the keys above.
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — smart-summary slot line uses `{outcome} · {slot}`; empty company title and slot use `commonPlaceholderDash`; registration field uses `timetableContactRegistrationPh`.

### What It Does (Technical)
1. `t(key)` for `language === "de"` reads `MESSAGES.de[key]`; previously missing keys fell back to `MESSAGES.en` or the JSX English fallback.
2. Secondary locales that use live MT still batch-translate from **`MESSAGES.en`**, so English strings remain the canonical source for those languages.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Copy-only change |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
`timetableColKa`, `timetableColKaPh`, `timetableColArte`, `timetableRowParkedTag`, `timetableContactSaved`, `timetableFieldNotesAppendPlaceholder`, and all `timetableContact*` keys used by `TimetableContactDrawer` / timetable toast (see `LanguageContext.tsx`).

### Dependencies Added
None.

### Notes / Known Limitations
User-editable row content (notes, company names) is not translated. ES / IT / PT / TR / RU / HI / AR / ZH / JA still inherit from `en` or machine translation unless overrides are added later.

---

## FEATURE-200: Timetable customer contact drawer — single-page scroll form
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The timetable **customer contact** modal is now **one continuous vertical form** (sections A–F) instead of four tabs. A **sticky pill bar** and optional **left step rail** (01–06) jump to anchored regions; **appointment history** sits in a **collapsible** `<details>` block (expanded by default when rows exist). Save/discard, smart summary, and all field handlers are unchanged.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — orchestrator: removed tab state and tablist; single scroll shell via section nav + `TimetableContactDrawerFormBody`.
- `frontend/src/pages/timetable/contactDrawerSectionIds.ts` — stable DOM ids for scroll targets.
- `frontend/src/pages/timetable/contactDrawerFormUtils.ts` — shared draft helpers (`EMPTY_OFFER`, `ensureProfile`, outcome/datetime/EUR helpers, etc.).
- `frontend/src/pages/timetable/contactDrawerFormClasses.ts` — shared Tailwind class strings for inputs and overview-style fields.
- `frontend/src/pages/timetable/components/TimetableContactDrawerSectionNav.tsx` — sticky pills + sm+ step rail + scroll spy.
- `frontend/src/pages/timetable/components/TimetableContactDrawerFormBody.tsx` — sections A–F (contact, location, people, history, vehicle, activity).
- `frontend/src/pages/timetable/components/contactDrawerCards.tsx` — `ContactCard` and `ContactSmartSummaryCard` extracted from the drawer file.
- `frontend/src/contexts/LanguageContext.tsx` — section nav/titles/subtitles, vehicle hint strings, smart-summary copy (tab → section), history expand/collapse hints.

### What It Does (Technical)
1. `TimetableContactDrawer` keeps `draft` / `patchDraft` / save / discard and wires `TimetableContactDrawerSectionNav` with `forwardRef` to the scrollable column.
2. `TimetableContactDrawerSectionNav` renders a fixed-width left rail (hidden `< sm`) and a scroll container with **sticky** pill links; scroll position updates **active pill** via a scroll listener; pill/rail clicks smooth-scroll using `getBoundingClientRect` math.
3. `TimetableContactDrawerFormBody` renders regions `id=timetable-contact-section-*` with `scroll-mt-28`, `role="region"`, and `aria-labelledby` tied to card titles.
4. History uses controlled `<details open={historyOpen} onToggle=…>` with `useEffect` to open when appointment count becomes positive.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| Same as before | `TimetableEntry` / `contact_profile` | Yes | No schema change; layout-only |

### Database
- **Engine:** N/A (in-memory draft until Save)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No (unchanged `onPersist` contract)

### i18n Keys Added
`timetableContactSectionNavContact`, `timetableContactSectionNavLocation`, `timetableContactSectionNavPeople`, `timetableContactSectionNavHistory`, `timetableContactSectionNavVehicle`, `timetableContactSectionNavActivity`, `timetableContactSectionRailAria`, `timetableContactSectionNavAria`, `timetableContactSectionContactTitle`, `timetableContactSectionContactSubtitle`, `timetableContactSectionLocationTitle`, `timetableContactSectionLocationSubtitle`, `timetableContactSectionPeopleTitle`, `timetableContactSectionPeopleSubtitle`, `timetableContactHistoryCollapse`, `timetableContactHistoryExpand`, `timetableContactVehicleInlineHint`, `timetableContactVehicleExtraHint`; updated `timetableContactAiBulletOfferGap`, `timetableContactAiBulletActivityEmpty` (de/en/fr).

### Dependencies Added
None.

### Notes / Known Limitations
- Wireframes live in `docs/wireframes/timetable-contact-drawer-wireframe.md` (referenced by the product plan; plan file not modified).
- Optional two-column vehicle layout on `xl` was not implemented (cards remain stacked for predictable field width).

---

## FEATURE-201: Timetable contact drawer — larger viewport + grid section pills
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The customer contact modal uses **more of the viewport** (taller and slightly wider shell), the form body column can grow to **`max-w-7xl` / `2xl:max-w-[88rem]`**, and the **sticky section pills** use a **responsive CSS grid** (`2` / `3` / `6` columns) so all six destinations stay reachable **without horizontal scrolling**. Scroll offsets and `scroll-margin-top` on sections were increased to match the taller sticky bar; pill links use **line-clamp**, **motion-safe** hover/press scale, and slightly stronger sticky chrome (`bg-white/90`).

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — dialog shell: `92dvh` height cap, `100rem` width cap vs prior `78dvh` / `96rem`.
- `frontend/src/pages/timetable/components/TimetableContactDrawerSectionNav.tsx` — pill `nav` layout grid; adaptive `scrollSectionIntoView` padding; scroll spy marker tweak; inner wrapper max-width tokens.
- `frontend/src/pages/timetable/components/TimetableContactDrawerFormBody.tsx` — `scroll-mt-36 sm:scroll-mt-32` on section regions.

### What It Does (Technical)
1. Modal `className` uses `min(92dvh, …)` and `min(100rem, …)` so large monitors show a bigger workspace while still respecting small viewports.
2. Pills: `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6` replaces `flex overflow-x-auto`, with `min-w-0` and `line-clamp-2` on labels for long translations.
3. Smooth-scroll subtracts `88px` or `104px` from target top depending on scroll root height so headings clear the sticky cluster.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Layout-only |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Very long pill labels in some locales may still wrap to two lines; grid keeps equal cell rhythm. Further “bento” two-column *section* layout on `xl` is not part of this entry.

---

## FEATURE-202: Timetable contact drawer — two-pane desktop (A–E + correspondence column)
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
On **`lg` breakpoints and up**, the contact form body uses a **two-column grid**: sections **A–E** stay in the **left** column (single vertical scroll of the modal body); **section F (correspondence)** sits in a **sticky right aside** with a **taller textarea** and **`max-h` + `overflow-y-auto`** on the pane when notes exceed the viewport — similar to legacy “Bemerkung” beside the form. **Below `lg`**, layout remains a **single column** A→F with one scroll. Section nav **scroll spy** skips the activity id during the main loop and selects it **near scroll bottom**; **“Correspondence”** pill scroll jumps to **`scrollHeight`**.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableContactDrawerFormBody.tsx` — outer `lg:grid` wrapper; left `div` wraps A–E; `aside` wraps F; `aria-label` via `timetableContactCorrespondencePanelAria`.
- `frontend/src/pages/timetable/components/TimetableContactDrawerSectionNav.tsx` — `ACTIVITY_SECTION_ID` handling in `updateActiveFromScroll` and `scrollSectionIntoView`.
- `frontend/src/contexts/LanguageContext.tsx` — `timetableContactCorrespondencePanelAria` (de/en/fr).
- `docs/wireframes/timetable-contact-drawer-wireframe.md` — viewport note for two-pane desktop.

### What It Does (Technical)
1. `lg:grid-cols-[minmax(0,1fr)_minmax(17.5rem,34%)]` (wider at `xl`) places the aside **after** the main stack in DOM; mobile `flex-col` stacks F under E.
2. Aside: `lg:sticky lg:top-28 lg:self-start lg:max-h-[min(86dvh,calc(100dvh-14rem))] lg:overflow-y-auto` keeps correspondence visible while scrolling A–E; long text scrolls inside the aside.
3. Pill “Correspondence” calls `scrollTo({ top: scrollHeight })` so the main scroll reaches the end (aside aligns with sticky behavior).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Layout-only |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
`timetableContactCorrespondencePanelAria` (de/en/fr).

### Dependencies Added
None.

### Notes / Known Limitations
Ultra-short `lg` viewports may feel tight at 34% width for correspondence; `minmax(17.5rem,34%)` sets a floor. True split-pane **resizer** is not implemented.

---

## FEATURE-203: Timetable contact drawer — legacy-style split + hidden scrollbars
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
Closer to the **classic Kundenkontakt** screenshot: **wider correspondence column** (`~44%` at `lg`, up to **`~48%`** at `xl`), **tighter vertical rhythm** in the left stack (`lg:space-y-5`), **full-width** form body on desktop (removed `max-w-7xl` cap on the scroll child). **No visible scrollbars** on the main scroll surface and correspondence aside via shared **`scrollbarHiddenY`** utility (content still scrolls). Right pane gets **rounded glass** treatment (gradient, blur, soft indigo ring/shadow); correspondence textarea **`min-h`** increased on large screens.

### Where It Was Added
- `frontend/src/pages/timetable/contactDrawerFormClasses.ts` — `scrollbarHiddenY`.
- `frontend/src/pages/timetable/components/TimetableContactDrawerSectionNav.tsx` — main scroll `div` uses `scrollbarHiddenY`; inner content `lg:max-w-none` + horizontal padding.
- `frontend/src/pages/timetable/components/TimetableContactDrawerFormBody.tsx` — grid column ratios, aside styling, left `space-y` tweak, textarea `lg:min-h`.
- `docs/wireframes/timetable-contact-drawer-wireframe.md` — viewport / two-pane notes.

### What It Does (Technical)
1. `scrollbarHiddenY`: `[scrollbar-width:none]`, `[-ms-overflow-style:none]`, zero-size webkit scrollbar.
2. Grid: `lg:grid-cols-[minmax(0,1fr)_minmax(20rem,44%)]`, `xl:…minmax(22rem,48%)`.
3. Aside: `lg:rounded-2xl`, gradient background, `backdrop-blur-md`, `scrollbarHiddenY` on same element as `overflow-y-auto`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Presentation-only |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Hiding scrollbars does **not** remove the need to scroll when content exceeds the viewport; users without a scroll wheel rely on touch drag or keyboard. Optional future: `prefers-reduced-transparency` fallback reducing `backdrop-blur` on the aside.

---

## FEATURE-204: Kunden list — unified filter label typography
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
All **filter field labels** on the Kunden (customers) list card now use the **same font size and color** as the **“Firmenname Schnellsuche”** / company quick-search label (`text-sm`, `font-medium`, `text-slate-600`), so secondary filters no longer appear as smaller, lighter captions.

### Where It Was Added
- `frontend/src/pages/CustomersPage.tsx` — `<label>` elements in the main filter grid and “More filters” date row: `text-xs text-slate-500` → `text-sm text-slate-600` (aligned with quick-search `<span>`).

### What It Does (Technical)
1. Quick search row already used `className="text-sm font-medium text-slate-600"` on the label span.
2. Grid and expandable filter `<label>` classes updated from `mb-1 block text-xs font-medium text-slate-500` to `mb-1 block text-sm font-medium text-slate-600` for visual parity.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Presentation-only |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-205: Kunden list — filter label typography (re-applied after main sync)
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Extended (see FEATURE-204)

### What Was Added
Re-applied **FEATURE-204** after `frontend/` was aligned with **`main`**: all Kunden filter `<label>` elements again use **`text-sm font-medium text-slate-600`**, matching the company quick-search line, instead of **`text-xs` / `text-slate-500`**.

### Where It Was Added
- `frontend/src/pages/CustomersPage.tsx` — same label class swap as FEATURE-204 on the current `main`-based `CustomersPage` (including `shrink-0` quick-search row layout from upstream).

### What It Does (Technical)
1. Replaces `mb-1 block text-xs font-medium text-slate-500` with `mb-1 block text-sm font-medium text-slate-600` on all filter grid labels and “More filters” date labels.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Presentation-only |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Upstream `CustomersPage` may gain new filter fields; use the same label utility/class pattern for consistency.

---

## FEATURE-206: New customer modal — remove header tab-instruction line
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
Removed the subtitle line **“Gehen Sie die Register unten durch — speichern Sie, wenn Sie fertig sind.”** (and EN/FR equivalents) from the new/edit customer modal header so that area stays compact.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — removed `<p className="customers-modal-genz-header-hint">` + `t("newCustomerHeaderHint", …)`.
- `frontend/src/index.css` — removed unused `.customers-modal-genz-header-hint` rule.
- `frontend/src/contexts/LanguageContext.tsx` — removed `newCustomerHeaderHint` from `MESSAGES.de`, `MESSAGES.en`, and `MESSAGES.fr`.

### What It Does (Technical)
1. Header summary stack no longer renders the hint paragraph.
2. i18n key `newCustomerHeaderHint` deleted from primary language objects (no override files referenced it).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
Removed: `newCustomerHeaderHint` (de/en/fr).

### Dependencies Added
None.

### Notes / Known Limitations
Keyboard shortcut hint (`newCustomerKeyboardShortcutsHint`) elsewhere unchanged.

---

## FEATURE-207: New customer modal — header address typography matches company name
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **summary address** line under the company name in the new/edit customer modal now uses the **same type scale and weight** as the name (`text-lg` / `sm:text-xl`, **bold**, tight tracking). **Map pin** and **copy** icons scale up slightly for balance. CSS ensures the address link uses the **same headline white** (and **cyan hover**) as the company link instead of the muted slate inherited from `.text-inherit`.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — `customers-modal-genz-header-address-row` Tailwind classes; pin and copy icon sizes.
- `frontend/src/index.css` — address row color + scoped link/hover overrides; `html[data-dema-theme="dark"]` mirrors for dark theme.

### What It Does (Technical)
1. Address row: `text-sm` → `text-lg font-bold tracking-tight sm:text-xl`, `gap-2` → `gap-2.5 sm:gap-3`.
2. `index.css`: row text `rgb(248 250 252)`; higher-specificity rules for `.customers-modal-genz-header-address-row .customers-modal-genz-header-link.text-inherit` (+ `:hover`).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Presentation-only |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Very long addresses wrap with `leading-snug`; layout remains `flex` + `break-words` on the link.

---

## FEATURE-208: New customer modal — slightly larger audit line (created / last edited)
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **edit metadata** row in the modal header (“Erstellt von …”, “Zuletzt bearbeitet von …”, timestamps) uses a **slightly larger** type size: **`text-[11px]` → `text-xs`** (Tailwind `0.75rem`).

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — wrapper `div` for `auditCreatedBy` / `auditLastEditedBy` / `auditNoTrail` in the genz header summary.

### What It Does (Technical)
1. Single class change on the flex wrapper containing audit spans.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-209: New customer modal — tab icons for Kunde, Art, Verlauf
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
Customer modal **tab strip** now shows a **leading Lucide icon** on every tab: **Building2** for *Kunde & Adresse*, **Landmark** for *Art / Buchungskonto*, **Clock** for *Verlauf* (edit mode), matching existing icons for VAT, Waschanlage, and Fahrzeug-Beziehungen.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — `lucide-react` imports (`Building2`, `Landmark`); tab button icon branch for `kunde`, `art`, `history`.

### What It Does (Technical)
1. Tab `map` render: ordered checks `vat` → `kunde` → `art` → `waschanlage` → `history` → `beziehungenFzg`, each with shared `h-3.5 w-3.5 sm:h-4 sm:w-4` + `opacity-80`, `aria-hidden` (labels remain text).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Icons are decorative; tab `aria` relies on visible translated labels.

---

## FEATURE-210: Customer modal header chips — Gen Z glass + animated conic border
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
**Status / VAT / profile / risk / document / last-saved** pills in the new customer modal dark header use **saturated glass gradients** per semantic state and a **continuous animated conic-gradient “ring”** on the pill edge (cyan → blue → violet → pink → teal) via registered **`--customers-modal-genz-h-chip-orbit`**. **`prefers-reduced-motion: reduce`** freezes the angle and disables orbit + entrance stagger.

### Where It Was Added
- `frontend/src/index.css` — root `@property` + `@keyframes customers-modal-genz-h-chip-orbit`; rebuilt `.customers-modal-genz-h-chip` and all `--status-*`, `--vat-*`, `--metric`, `--risk-*`, `--doc-*`, `--meta` modifiers as **CSS-variable inner fills** + shared animated border; hover/focus use brightness/shadow instead of legacy flat `background`.

### What It Does (Technical)
1. Dual `background-image`: inner `linear-gradient` (padding-box) from `--genz-h-chip-fill-*` RGB + opacity; outer `conic-gradient` (border-box) angle driven by `@keyframes` on custom property.
2. `backdrop-filter` + inset highlight + tinted drop shadow for depth.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Orbit animation requires a browser that supports **`@property`** with `<angle>` (current Chromium, Safari, Firefox). Where unsupported, gradient may not animate but pills still render with new colors.

---

## FEATURE-211: Customer modal chips — per-tag ring colours + sparkle orbit fix
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Extended (see FEATURE-210)

### What Was Added
Each chip variant sets **`--genz-h-chip-ring-a/b/c`** so the **animated conic border** uses **different hues** per tag (emerald/lime status, amber VAT invalid, violet/pink profile, etc.) with **bright white “sparkle” peaks** between stops. **Outer glow** uses `rgb(var(--genz-h-chip-ring-b) / …)` per tag. **Orbit + entrance:** `.customer360-strip-chips > *` now runs **both** `dema-customer360-chip-enter` and `customers-modal-genz-h-chip-orbit` (previously entrance shorthand overwrote orbit); **staggered negative delays** on the second animation offset the sparkle phase per pill.

### Where It Was Added
- `frontend/src/index.css` — `.customers-modal-genz-h-chip` conic uses ring vars; all `--status-*`, `--vat-*`, `--metric`, `--risk-*`, `--doc-*`, `--meta` set `--genz-h-chip-ring-*`; `.customer360-strip-chips` animation block + nth-child delays.

### What It Does (Technical)
1. Conic: `from var(--orbit)` through `rgb(var(--genz-h-chip-ring-a))`, white peaks, `ring-b`, white, `ring-c`, …
2. Dual `animation` on strip children; `animation-delay: <enter>, <orbit>` per nth-child.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-212: Customer modal chips — semantic fills, slow orbit, golden sparkle
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Extended (see FEATURE-211)

### What Was Added
- **Smarter inner backgrounds:** each chip sets **`--genz-h-chip-fill-angle`** plus tuned **`--genz-h-chip-fill-1/2`** so Status / VAT / Profil / Risiko / Dokumente / Meta read as **different roles** (e.g. VAT unknown = compliance blue, doc calm = teal→navy, meta = charcoal→slate, billing risk = deeper orange).
- **Slower ring motion:** orbit animation **3.8s → 14s**; staggered orbit delays **0 … -14s** in 2s steps so pills stay out of phase on the longer loop.
- **Golden sparkle:** conic “highlights” use **yellow/amber/champagne** (`253 224 71`, `250 204 21`, `254 243 199`, `245 158 11`, `252 211 77`) instead of white; inset top line and interactive hover/focus use a **subtle gold** tint.

### Where It Was Added
- `frontend/src/index.css` — `.customers-modal-genz-h-chip` gradient + conic; all variant blocks; `.customer360-strip-chips` animation duration and delays.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-213: Customer modal chips — unique tag palettes, silver sparkle, slower orbit
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Extended (see FEATURE-212)

### What Was Added
- **Unique colour per tag type:** stronger separation between chips (e.g. inactive **zinc**, VAT unknown **indigo**, profile **fuchsia**, billing risk **ochre**, doc calm **aqua cyan**, meta **graphite + violet ring**, doc alert **pink/fuchsia ring**, risk low **aquamarine**, etc.).
- **Silver sparkle ring:** conic uses **frost / slate silver** peaks (`248 250 252`, `226 232 240`, `203 213 225`, `148 163 184`) between each tag’s ring hues; chrome highlights on hover/focus use **silver frost**, not gold.
- **Slower orbit:** **14s → 26s**; orbit delay steps **-3.25s** (`0` through `-22.75s`) for eight children.

### Where It Was Added
- `frontend/src/index.css` — conic stops, shadows, all chip variant `--genz-h-chip-*` vars; `.customer360-strip-chips` animation.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
None.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-214: Timetable contact drawer — extra contacts UI aligned with customer edit modal
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The timetable **customer contact** drawer list of additional contacts (“Weitere Ansprechpartner” / other contact persons) now uses the same **nested card**, **avatar + title row**, **trash remove control**, **label typography**, **neutral bordered inputs**, and **Plus + “New”** add pattern as the **Contact person** column in `NewCustomerModal` (customer create/edit). Field labels reuse the customer modal keys for name and phone; fax keeps the existing timetable fax key.

### Where It Was Added
- `frontend/src/pages/timetable/contactDrawerFormClasses.ts` — exported shared Tailwind class strings matching the modal’s kontakt field chrome and add button.
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `OverviewPanel` optional `sectionTitleClassName`; overview “more contacts” block restructured; `Plus` / `Trash2` imports.
- `frontend/src/pages/timetable/components/TimetableContactDrawerFormBody.tsx` — same extra-contacts UI for the wide layout form body.
- `frontend/src/contexts/LanguageContext.tsx` — English/French copy for `timetableContactMoreContacts` clarified to “other contact persons” sense.

### What It Does (Technical)
1. User opens a timetable row’s contact drawer and adds or edits entries under the extra-contacts section.
2. Each entry renders as `customers-modal-genz-kontakt-nested-card` with a header row (initials badge, display name, icon remove) and a body with three fields bound to `contact_profile.contacts[]` (`name`, `phone`, `fax`).
3. Add appends `emptyContactPerson()` via existing `patchDraft` / `ensureProfile` flow; remove filters the array by index.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `contact_profile.contacts` | `TimetableContactPerson[]` | No | Secondary contacts; each `{ name, phone?, fax? }` |

### Database
- **Engine:** N/A (in-memory / persisted via existing timetable entry save path)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A (draft state local to drawer)
- **Actions/Selectors added:** None
- **Persisted:** As today — follows existing timetable entry persistence

### i18n Keys Added
None (reused `newCustomerKontaktNewBtn`, `newCustomerLabelName`, `newCustomerLabelTelefon`, `newCustomerNamePh`, `newCustomerPhPhone`, `newCustomerKontaktDefaultLabel`, `newCustomerKontaktRemove`, `timetableContactFax`; **modified** `timetableContactMoreContacts` in `MESSAGES.en` / `MESSAGES.fr`).

### Dependencies Added
None.

### Notes / Known Limitations
The customer modal still offers role, mobile, e-mail, and note per kontakt; the timetable profile model only stores name/phone/fax for extra contacts — UI parity is visual and for those three fields only.

---

## FEATURE-215: Customer modal header chips — data-scoped palettes + faster orbit
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Extended

### What Was Added
Each Customer 360 header pill now has a stable `data-dema-chip` id, and colours are applied only under `.customers-modal-genz-header` with **stronger, more distinct** three-stop inner gradients (so VAT unknown, risk rows, and doc chips no longer wash into the same violet). The conic “sparkle” border orbit runs in **18s** (was 26s) with delays rescaled for eight children; chip **backdrop blur** is reduced so hues read more clearly.

### Where It Was Added
- `frontend/src/components/NewCustomerModal.tsx` — `data-dema-chip` on status, VAT, profile, risk, document, and audit chips.
- `frontend/src/index.css` — base chip: `--genz-h-chip-fill-mid` / `fill-op-mid`, three-stop `linear-gradient`, `blur(6px)`; variant colours moved to `.customers-modal-genz-header .customers-modal-genz-h-chip[data-dema-chip='…']`; orbit duration/delays on `.customer360-strip-chips > *`.

### What It Does (Technical)
1. Each chip element sets `data-dema-chip` to a stable token (`status-*`, `vat-*`, `profile`, `risk-*`, `doc-alert` | `doc-calm`, `audit`).
2. Header-scoped selectors override CSS variables for fill angle, three RGB stops, opacities, text, and conic ring stops so specificity wins over the dark glass header and lighter backdrop.
3. The inner fill uses a mid stop at 50% for a clearer dominant hue; reduced backdrop blur limits colour flattening.
4. `customers-modal-genz-h-chip-orbit` animation uses **18s**; nth-child delays use **-2.25s** steps (18/8) to keep phases evenly staggered.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `data-dema-chip` | string attribute | Yes (on chips in this row) | Stable style key for CSS (`status-active`, `vat-unknown`, `profile`, etc.) |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Chips outside `.customers-modal-genz-header` would only get the base default variables unless given matching scoped rules.

---

## FEATURE-216: Customer modal chips — profile & billing hues + faster orbit
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **Profile** completion chip no longer uses magenta/fuchsia; it now uses a **sky → deep blue** gradient with matching conic ring and a scoped accent colour for the percentage. The **Risk: billing incomplete** chip no longer uses bright yellow/gold; it uses a **rust / deep amber** gradient so it stays a warning tone but reads distinctly from **risk-payment** orange. The header chip conic border orbit was shortened from **18s to 14s**, with stagger delays rescaled to `-1.75s` steps (14 ÷ 8).

### Where It Was Added
- `frontend/src/index.css` — `[data-dema-chip='profile']`, profile `.customers-modal-genz-h-chip-accent` override, `[data-dema-chip='risk-billing']`, `.customer360-strip-chips` orbit duration and nth-child delays.

### What It Does (Technical)
1. Profile chip variables point to sky/cyan RGB stops and sky-toned ring stops.
2. A higher-specificity rule sets the inner percentage span (`customers-modal-genz-h-chip-accent`) to sky-100 so it stays legible on the new fill.
3. Billing chip uses darker amber/brown stops and ring colours aligned to that hue.
4. Orbit animation period 14s; negative delays `-1.75s × (n−1)` for children 2–8 preserve even phase spread.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Visual-only CSS |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-217: Customer modal chips — distinct hue map + faster orbit
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
VAT **not verified** and **Profile** no longer sit in the same cool cyan/blue glass family: unknown uses a **warm taupe/stone** fill with **soft violet** sparkle accents; profile uses **royal blue** only (no cyan sky mix). **Risk low** moved off blue to **sage/lime** so it does not collide with profile. **VAT invalid** skews **rose/vermillion** to separate from **payment** orange. **Document deadlines (calm)** leans **violet-indigo** so it is not confused with profile blue. Conic border orbit sped from **14s to 10s** with delays scaled to **−1.25s** steps. **Status active** fill nudged to a slightly warmer **forest emerald** vs VAT-valid teal.

### Where It Was Added
- `frontend/src/index.css` — `[data-dema-chip]` blocks: `vat-unknown`, `profile`, `vat-invalid`, `risk-low`, `doc-calm`, `status-active`; profile accent colour; `.customer360-strip-chips` orbit timing.

### What It Does (Technical)
1. Each chip keeps a single dominant hue family (green / slate / red / teal / rose / taupe / blue / crimson / orange / rust / lime / pink / violet / charcoal).
2. VAT unknown RGB stops are warm neutrals; conic ring includes `196 181 253` for separation from billing rust and inactive cool slate.
3. Orbit animation period 10s; stagger `-(n-1)*1.25s` for children 2–8.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | CSS-only |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-218: Timetable `contact_profile.contacts` — `KontaktEntry` parity + shared pill editor
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
Timetable **customer contact** drawer extra contacts now use the **same data model as the customer modal** (`KontaktEntry` / `KundenKontakt`): stable `id`, **name**, **rolle** (same option values as `NewCustomerModal`), **telefon** / **handy** (with blur whitespace trim via shared `normalizePhoneValue`), **email**, **website**, **bemerkung**, plus default **telefonCode** / **handyCode** `+49`. UI matches the modal pattern: **pill tabs** for switching contacts, one **nested card** with avatar header and trash, and the same input/select classes. Legacy persisted rows using `{ name, phone, fax }` are **migrated on read** (`splitStoredPhone` for `phone`, fax text folded into `bemerkung` when needed). `normalizePhoneValue` is **exported from `customerFormMapper`** and reused by `NewCustomerModal`.

### Where It Was Added
- `frontend/src/types/timetable.ts` — `TimetableContactPerson` is now a type alias of `KontaktEntry`.
- `frontend/src/pages/timetable/components/TimetableExtraKontakteBlock.tsx` — new shared editor (pill strip + card + fields).
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` / `TimetableContactDrawerFormBody.tsx` — render the shared block (overview uses embedded “New” row under `OverviewPanel` title).
- `frontend/src/pages/timetable/contactDrawerFormUtils.ts` — `emptyContactPerson()` delegates to `emptyKontakt()`.
- `frontend/src/pages/timetable/contactDrawerFormClasses.ts` — `customerModalKontaktSelectClass`.
- `frontend/src/store/timetableStore.ts` — `normalizeContactPerson` migration + demo seed contacts updated to full shape; `splitStoredPhone` import.
- `frontend/src/features/customers/mappers/customerFormMapper.ts` — export `normalizePhoneValue`.
- `frontend/src/components/NewCustomerModal.tsx` — import shared `normalizePhoneValue`.

### What It Does (Technical)
1. `patchDraft` updates `contact_profile.contacts[i]` immutably with full `KontaktEntry` fields.
2. Store hydration runs `normalizeContactPerson` per array element; legacy `phone`/`fax` are normalized into kontakt fields.
3. UI state keeps an active pill index inside `TimetableExtraKontakteBlock` (`useState` + clamp on length change).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `contact_profile.contacts[]` | `KontaktEntry` | No | Same fields as customer form kontakt list (`id`, `name`, `rolle`, `telefonCode`, `telefon`, `handyCode`, `handy`, `email`, `website`, `bemerkung`) |

### Database
- **Engine:** Browser `localStorage` (timetable v6 payload) / N/A server
- **Tables Affected:** N/A
- **Schema Changes:** JSON shape of `contacts` array entries extended (backward-compatible read path)
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts`
- **Actions/Selectors added:** None (normalization only)
- **Persisted:** Yes — timetable entries as today

### i18n Keys Added
None (reused existing `newCustomer*` / `customersLabelWebsite` keys).

### Dependencies Added
None.

### Notes / Known Limitations
Legacy **fax** is no longer a dedicated field; it is merged into **bemerkung** on load when present. Role `<option>` labels remain German literals (same as `NewCustomerModal`) — future work could add per-locale keys.

---

## FEATURE-219: Timetable contact drawer — kontakt header matches customer modal (accent bar + row)
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Extended (see FEATURE-218)

### What Was Added
The timetable overview **Contact person** panel header now matches **NewCustomerModal** column 3: same **flex row** (`space-between` on wide screens), **left accent** via `customers-modal-genz-kunde-col-title--3` (3px border + padding + dark text from `index.css`), and the **same “New”** button classes. `OverviewPanel` accepts an optional **`header`** slot so title + button live on one row without a duplicate “New” under the title. **`appendFocusTick`** on `TimetableExtraKontakteBlock` focuses the last pill after adding from that header; tick resets when switching timetable rows. The wide-form `TimetableExtraKontakteBlock` toolbar uses the same title accent class and modal spacing (`flex flex-col gap-3`, no extra divider under the title row).

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `OverviewPanel` `header` prop; contact panel header JSX; `extraKontaktAppendTick` state; imports `Plus`, `customerModalKontaktNewBtnClass`, `emptyContactPerson`.
- `frontend/src/pages/timetable/components/TimetableExtraKontakteBlock.tsx` — `appendFocusTick` + ref guard; toolbar title uses `customers-modal-genz-kunde-col-title--3`; outer `gap-3` layout.

### What It Does (Technical)
1. Custom `header` renders inside the panel’s top `border-b` wrapper.
2. “New” in the header runs `patchDraft` + increments `extraKontaktAppendTick`.
3. Block effect selects `contacts.length - 1` when tick increases and `tick > 0`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | UI-only |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
Nested kontakt card chrome that depends on `.customers-modal-genz-kunde-col--3` in CSS still applies only inside the customer modal DOM; timetable keeps the same nested card class without that parent wrapper.

---

## FEATURE-220: Timetable contact drawer overview — three customer columns (Kundenart · Stammdaten · Kontakt)
**Date:** 2026-04-13  
**Author/Agent:** Cursor AI  
**Status:** Added  

### What Was Added
The purchase timetable **contact drawer → Overview** tab now includes the same **three-column “Customer & Address”** layout as the customer modal: **customer classification** (number, type, status, industry, vehicle trader, legal form, acquisition, profile/operational notes, stub invoice actions), **master data** (company prefix/name, single address card with street/ZIP/city/country/VAT/tax/website), and **contact persons** (reusing `TimetableExtraKontakteBlock`) plus a **compact appointments table** (date, time, purpose, done, remove) backed by `contact_profile.appointment_history`. Draft fields persist on the timetable row with the existing Save flow.

### Where It Was Added
- `frontend/src/types/timetable.ts` — `TimetableOverviewKundeDraft`, `overview_kunde` on `TimetableContactProfile`.
- `frontend/src/pages/timetable/timetableOverviewKunde.ts` — empty factory, `timetableOverviewFromEntry`, `normalizeTimetableOverviewKunde`.
- `frontend/src/store/timetableStore.ts` — hydrate `overview_kunde` in `normalizeContactProfile`.
- `frontend/src/pages/timetable/components/TimetableOverviewCustomerColumns.tsx` — new UI grid + wiring.
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — render columns above the horizontal overview strip; `+ New` kontakt increments `extraKontaktAppendTick`.
- `frontend/src/contexts/LanguageContext.tsx` — new `timetableOverview*` keys (de/en/fr + secondary overrides).

### What It Does (Technical)
1. On row open (`draft.id`), if `contact_profile.overview_kunde` or `contacts` is missing/empty, `patchDraft` seeds `overview_kunde` from `timetableOverviewFromEntry` and/or one `emptyContactPerson()` prefilled from row `contact_name` / `phone`.
2. Column edits call `patchOverview`, which updates `overview_kunde` and mirrors **customer number**, **industry**, **company name**, and a one-line **address** string into `contact_profile` / row for consistency with other overview cards.
3. Contact pills and nested card reuse `TimetableExtraKontakteBlock` (toolbar hidden; header `+ New` in parent).
4. Appointments rows reuse the same shape as the History tab (`emptyAppointmentRow`, array patches on `appointment_history`).
5. Global address search and multi-address “+ New” are **disabled** with explanatory copy (frontend-only scope); invoice buttons are **non-persisting stubs**.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `contact_profile.overview_kunde` | `TimetableOverviewKundeDraft` | No | Extended CRM-style draft for the three columns; persisted with the row JSON. |
| `contact_profile.contacts` | `TimetableContactPerson[]` | No | Kontakt entries; auto-seeded when empty. |
| `contact_profile.appointment_history` | `TimetableAppointmentHistoryRow[]` | No | Termine table rows. |

### Database
- **Engine:** Browser `localStorage` (via existing timetable store)  
- **Tables Affected:** N/A  
- **Schema Changes:** New optional JSON fields under `contact_profile` on timetable entries.  
- **Key Queries:** None  

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts`  
- **Actions/Selectors added:** `normalizeContactProfile` extended to read `overview_kunde`.  
- **Persisted:** Yes (timetable v6 payload).

### i18n Keys Added
`timetableOverviewArtLandIl`, `timetableOverviewArtLandEu`, `timetableOverviewArtLandThird`, `timetableOverviewAddrSearchPh`, `timetableOverviewAddressMultiSoon`, `timetableOverviewNewAppointment`, `timetableOverviewAppointmentsEmpty`, `timetableOverviewAppointmentActionsAria` — in `MESSAGES.de` / `MESSAGES.en` / `MESSAGES.fr` and `LANGUAGE_OVERRIDES` for `es`, `it`, `pt`, `tr`, `ru`, `hi`, `ar`, `zh`, `ja`.

### Dependencies Added
None.

### Notes / Known Limitations
Invoice actions remain non-persisting stubs. Address search is now a local field on the row (no geocoder API). Multi-address is supported via `overview_kunde.adressen[]` (see FEATURE-221).

---

## FEATURE-221: Timetable Kundenkontakt — closer modal mirror (RECHNUNGEN, addresses, globe search, footer)
**Date:** 2026-04-13  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
Kalender **Kundenkontakt** three-column block is closer to the customer modal reference: **RECHNUNGEN** subsection label above invoice buttons; **multiple address pills** with working **+ Neu** / remove; address card header with **globe** + type dropdown; **worldwide address search** as an editable local field with **globe** inside the input; **Termine** “new” control uses a **plus** icon; drawer **footer** mirrors the modal with **Zuständige Person** `<select>` (persisted on `contact_profile.zustaendige_person`), **Abbrechen** (`commonCancel`), and **Änderungen speichern** (`timetableContactSaveChanges`). All data stays on the timetable row JSON — no backend.

### Where It Was Added
- `frontend/src/types/timetable.ts` — `TimetableOverviewAdresseDraft`; `overview_kunde` adds `addr_search_query`, `active_adresse_idx`, `adressen[]`; profile adds `zustaendige_person?`.
- `frontend/src/pages/timetable/timetableOverviewKunde.ts` — multi-address normalize/migrate from legacy flat fields; `formatOverviewAddressLine`; `emptyOverviewAdresse`.
- `frontend/src/pages/timetable/components/TimetableOverviewCustomerColumns.tsx` — UI/behaviour updates.
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — footer layout + responsible dropdown.
- `frontend/src/store/timetableStore.ts` — `zustaendige_person` in `normalizeContactProfile`.
- `frontend/src/contexts/LanguageContext.tsx` — `timetableOverviewAddrSearchPlaceholder`, `timetableContactZustaendigePerson`, `timetableContactZustaendigePlaceholder`, `timetableContactSaveChanges` (+ overrides).

### What It Does (Technical)
1. `overview_kunde.adressen` holds 1..n address rows; `active_adresse_idx` selects the nested card; `patchActiveAdresse` updates the active row and `patchOverview` refreshes `contact_profile.address` from `formatOverviewAddressLine`.
2. `addr_search_query` is persisted text only (UI mirror, no API).
3. Footer `<select>` writes `contact_profile.zustaendige_person`; options union `buyer_name`, stored value, and codes ES/MS/VS/IT.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `overview_kunde.adressen` | `TimetableOverviewAdresseDraft[]` | Yes (normalized) | Address tabs + card fields. |
| `overview_kunde.addr_search_query` | `string` | No | Local search line. |
| `contact_profile.zustaendige_person` | `string` | No | Responsible person code/label. |

### Database
- **Engine:** Browser `localStorage` (timetable store)  
- **Tables Affected:** N/A  
- **Schema Changes:** Extended optional JSON on timetable rows.  
- **Key Queries:** None  

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts`  
- **Actions/Selectors added:** `zustaendige_person` normalization.  
- **Persisted:** Yes.

### i18n Keys Added
`timetableOverviewAddrSearchPlaceholder`, `timetableContactZustaendigePerson`, `timetableContactZustaendigePlaceholder`, `timetableContactSaveChanges` — primaries + secondary overrides.

### Dependencies Added
None.

### Notes / Known Limitations
Invoice buttons still have no navigation. Address search does not geocode or fill fields automatically.

---

## FEATURE-222: Remove Kalender three-column Kundenkontakt form from contact drawer
**Date:** 2026-04-13  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The large **three-column customer-style form** (`TimetableOverviewCustomerColumns`: Kundenart · Stammdaten · Kontakt + appointments) was **removed** from the timetable **Kundenkontakt** drawer Overview tab. The horizontal overview strip (identity, appointment, location, extra contacts, assignments) and **Discard / Save contact** footer are restored to the prior layout. **No backend change.**

### Where It Was Added
- **Deleted:** `frontend/src/pages/timetable/components/TimetableOverviewCustomerColumns.tsx`
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — removed component render, import, and the modal-style footer (`Zuständige Person` + `Save changes`); restored original footer.

### What It Does (Technical)
1. Overview scroll area no longer mounts `TimetableOverviewCustomerColumns`.
2. `contact_profile.overview_kunde` / `zustaendige_person` may still exist in persisted timetable JSON from earlier builds; `timetableStore` normalization is unchanged so old data does not crash.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | UI removal only; optional profile fields unchanged in schema. |

### Database
- **Engine:** Browser `localStorage` (unchanged)  
- **Tables Affected:** N/A  
- **Schema Changes:** None  
- **Key Queries:** None  

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A (no store logic removed)  
- **Actions/Selectors added:** None  
- **Persisted:** Legacy `overview_kunde` blobs may remain in saved rows until edited/saved without those keys.

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
If the product wants the form again, restore from git history (FEATURE-220 / FEATURE-221 era).

---

## FEATURE-223: Timetable Kundenkontakt drawer — shell size matches edit customer modal
**Date:** 2026-04-13  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The Kalender **Kundenkontakt** dialog shell now uses the same **max width / max height / padding** pattern as **`NewCustomerModal`** in edit mode with side content (`max-w-[min(118rem,99vw)]`, `max-h-[92vh]`, backdrop `p-3 sm:p-4 md:p-5`). Fixed `78dvh` / `96rem` sizing was removed in favour of `w-full` + max constraints and `rounded-2xl` to align with the customer modal panel.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — outer backdrop and inner dialog `className` strings.

### What It Does (Technical)
1. Backdrop padding matches `NewCustomerModal` so the panel’s effective viewport inset is consistent.
2. Inner panel: `h-auto min-h-0 w-full max-h-[92vh] max-w-[min(118rem,99vw)]` replaces previous `min(96rem,…)` width and `78dvh` height caps.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Layout only |

### Database
- **Engine:** N/A  
- **Tables Affected:** N/A  
- **Schema Changes:** None  
- **Key Queries:** None  

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A  
- **Actions/Selectors added:** None  
- **Persisted:** No  

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-224: Timetable Kundenkontakt drawer — remove tab bar, single scroll for all sections
**Date:** 2026-04-13  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The horizontal **Übersicht / Termine / Fahrzeug / Korrespondenz** tab strip under the drawer toolbar was removed. **Appointments (history), Vehicle, and Correspondence** content now appears **below** the overview column strip inside the **same vertical scroll**, so users scroll down instead of switching tabs.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — removed `tab` state, tab `<nav>`, and tab-gated rendering; merged sections into one scroll region; smart-summary popover closes on `entry?.id` change.
- `frontend/src/contexts/LanguageContext.tsx` — updated copy for `timetableContactOverviewVerticalScrollAria`, `timetableContactAiBulletOfferGap`, and `timetableContactAiBulletActivityEmpty` in **de / en / fr** (no more “tab” wording; points to sections below).

### What It Does (Technical)
1. Toolbar row (**Log call**, **Offer**, **Smart summary**) is unchanged; only the **tablist** UI was deleted.
2. Main body uses one **overflow-y-auto** region: overview horizontal columns, then a bordered **max-w-6xl** block with vehicle offer cards and correspondence textarea stacked vertically (see FEATURE-225: further-appointments block removed).
3. `getContactSmartBullets` English fallbacks aligned with “section below” wording (primary UI strings come from `t()` keys).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Presentation only; same `TimetableEntry` / `contact_profile` fields as before |

### Database
- **Engine:** N/A  
- **Tables Affected:** N/A  
- **Schema Changes:** None  
- **Key Queries:** None  

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A  
- **Actions/Selectors added:** None (removed local `tab` state)  
- **Persisted:** No  

### i18n Keys Added
None (updated strings for existing keys: `timetableContactOverviewVerticalScrollAria`, `timetableContactAiBulletOfferGap`, `timetableContactAiBulletActivityEmpty` in de/en/fr).

### Dependencies Added
None.

### Notes / Known Limitations
- Keys `timetableContactTab*` / `timetableContactTabsAria` remain in `LanguageContext.tsx` for potential reuse; they are no longer referenced by this drawer.

---

## FEATURE-225: Timetable Kundenkontakt drawer — remove “Weitere Kundentermine” block
**Date:** 2026-04-13  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The **“Weitere Kundentermine”** section (heading, hint, **Termin hinzufügen**, empty state / appointment cards) was removed from the Kalender **Kundenkontakt** drawer. Users no longer edit `contact_profile.appointment_history` from this UI.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — deleted the appointment-history JSX block; removed local `emptyAppointmentRow` helper and `TimetableAppointmentHistoryRow` type import.

### What It Does (Technical)
1. ~~Lower scroll area~~ **Superseded by FEATURE-226:** the whole multi-column form and lower **Vehicle** / **Correspondence** blocks were later removed; see FEATURE-226.
2. Existing persisted `appointment_history` arrays on rows are unchanged on disk/store shape; they are simply not shown or editable here until a future UI restores them.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `contact_profile.appointment_history` | `TimetableAppointmentHistoryRow[]` | No | Optional; still part of `TimetableContactProfile` type and store normalization — not surfaced in this drawer |

### Database
- **Engine:** Browser / in-memory timetable row state (as before)  
- **Tables Affected:** N/A  
- **Schema Changes:** None  
- **Key Queries:** None  

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A  
- **Actions/Selectors added:** None  
- **Persisted:** No change to persistence format  

### i18n Keys Added
None (keys such as `timetableContactSectionHistory` remain for `TimetableContactDrawerFormBody.tsx` and other potential use).

### Dependencies Added
None.

### Notes / Known Limitations
- `TimetableContactDrawerFormBody.tsx` still contains an appointment-history UI if that component is wired in later; only `TimetableContactDrawer` was updated.

---

## FEATURE-226: Timetable Kundenkontakt drawer — strip all form fields (empty body)
**Date:** 2026-04-13  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
All editable **Kundenkontakt** form sections were removed from the drawer: **Wen Sie anrufen**, **Termin & Ergebnis**, **Adresse / Fuhrpark / Notizen**, **Kontaktperson**, **Bearbeitung**, **Fahrzeugangebot**, **Zulassung & Status**, and **Korrespondenz**. The main area is an **empty placeholder** with i18n copy; **header** (title, outcome, Ka/Arte, CRM snapshot, slot time, badges), **toolbar** (log call, offer, smart summary), and **footer** (discard / save) remain. Row data is unchanged until the user edits header fields or saves.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — removed overview columns, vehicle/correspondence blocks, and unused helpers/components (`OverviewPanel`, `ContactCard`, offer/vehicle field setters, etc.); fixed drawer body wrapper closing tags.
- `frontend/src/contexts/LanguageContext.tsx` — new keys `timetableContactDrawerBodyAria`, `timetableContactFormBodyEmpty`; removed unused `timetableContactOverviewVerticalScrollAria`; updated smart-summary bullet strings (`timetableContactAiBulletOfferGap`, `timetableContactAiBulletActivityEmpty`) in **de / en / fr**; added the same keys (and bullet updates where applicable) to **es, it, pt, tr, ru, hi, ar, zh, ja** overrides.

### What It Does (Technical)
1. `handleSave` / `handleDiscard` / `patchDraft` still operate on the full `TimetableEntry` clone; only the ** JSX surface** for bulk editing was removed.
2. Smart summary bullets still derive from `draft` + `contact_profile` via `getContactSmartBullets` (copy adjusted for “not edited in this view”).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| Same as before | `TimetableEntry` | Yes | No API change; editing limited to header Ka/Arte and actions that open other flows |

### Database
- **Engine:** N/A  
- **Tables Affected:** N/A  
- **Schema Changes:** None  
- **Key Queries:** None  

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A  
- **Actions/Selectors added:** None  
- **Persisted:** Unchanged  

### i18n Keys Added
- `timetableContactDrawerBodyAria`, `timetableContactFormBodyEmpty` (de, en, fr + secondary overrides).
- Updated: `timetableContactAiBulletOfferGap`, `timetableContactAiBulletActivityEmpty` (de, en, fr + secondary overrides).
- Removed: `timetableContactOverviewVerticalScrollAria` (unused after body replacement).

### Dependencies Added
None.

### Notes / Known Limitations
- To restore the full form, recover prior JSX from git history (pre-FEATURE-226) or reuse `TimetableContactDrawerFormBody.tsx` patterns.

---

## FEATURE-227: Timetable Kundenkontakt — customer-modal 4-column layout (light/dark)
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The purchase timetable **Kundenkontakt** drawer again shows a full **four-column** form aligned with the **customer edit modal**: same `customers-modal-genz-*` shell (backdrop, panel, header, scroll body, footer), same **12-column / 3+3+3+3** grid from wide breakpoints, **Stammdaten** (overview company + address card + website), **Kontaktperson** (`TimetableExtraKontakteBlock`), **Call log** (row slot/outcome/notes/correspondence + compact appointments table), and **Offer** (inline offer + vehicle extra fields + link to full offer editor). The UI renders through **`createPortal(..., document.body)`** so it stacks above the app header. Opening a row **seeds** `contact_profile.overview_kunde` and optional `contacts[0]` from the row when missing, and merges **Kundenstamm** when resolved.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — layout, seeding, columns, portal, footer responsible-person select
- `frontend/src/pages/timetable/components/TimetableExtraKontakteBlock.tsx` — optional `sectionTitleColSuffix` for correct column title BEM class
- `frontend/src/contexts/LanguageContext.tsx` — new `timetableContact*` keys (de/en/fr + all 9 `LANGUAGE_OVERRIDES`)

### What It Does (Technical)
1. On row open (`useLayoutEffect` reset): `cloneEntry` → ensure `overview_kunde` via `timetableOverviewFromEntry` + optional `mergeMasterOverview` from `resolveKundeForTimetableRow` → seed `contacts` from `contact_name` / `phone` when empty.
2. **Stammdaten** column mutates `contact_profile.overview_kunde` through `patchOverviewKunde` / `patchAdresseIdx`; **GlobalAddressSearch** patches the active address when a result is chosen.
3. **Save** copies `overview_kunde.firmenname` → `company_name` and first contact name/phone → row fields before `onPersist`.
4. Toolbar **Log call** / **Add|Edit offer** / **Smart summary** unchanged in behaviour.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `TimetableEntry` | object | Yes | Same persisted row shape; `contact_profile.overview_kunde` now commonly present after first open |

### Database
- **Engine:** Browser `localStorage` (timetable store unchanged)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A (drawer local state + `onPersist` to timetable store)
- **Actions/Selectors added:** None
- **Persisted:** Yes — via existing timetable persistence when user saves

### i18n Keys Added
`timetableContactHeaderEyeline`, `timetableContactCalendarEditBadge`, `timetableContactAufnahmeMeta`, `timetableContactColCallLog`, `timetableContactColOffer`, `timetableContactCorrespondenceTitle`, `timetableContactAppointmentsShort`, `timetableContactNewAppointmentShort`, `timetableContactApptColDate`, `timetableContactApptColTime`, `timetableContactApptColPurpose`, `timetableContactApptColDone` — in **MESSAGES.de/en/fr** and all **LANGUAGE_OVERRIDES** locales.

### Dependencies Added
None.

### Notes / Known Limitations
- Country **ISO** is a two-letter text field (not the full country dropdown from `NewCustomerModal`).
- `ART_LAND` option labels in the select are still the raw codes (`IL` / `EU` / `Drittland`) for third column parity; localised labels can be added later.
- `TimetableContactDrawerFormBody.tsx` remains unused by this drawer path.

---

## FEATURE-228: Timetable Kundenkontakt drawer — remove inline “Anruf protokollieren” column
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The third drawer column that duplicated **call logging** (company/contact/phone/buyer, slot, follow-up, outcome, flags, row notes, correspondence, appointments mini-table) was **removed**. The contact workspace is now **three columns**: **Stammdaten** | **Kontaktperson** | **Angebot**. Wide layout uses **4+4+4** grid spans; on `lg` two-column layout the offer panel spans **full width** under the first two columns. Toolbar **“Anruf loggen”** still opens **`TimetableCallModal`** for quick outcome/notes; table row action unchanged.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — delete column 3 JSX; adjust `TT_EDIT_KUNDE_COL` / new `TT_EDIT_KUNDE_COL_OFFER`; drop unused imports and `outcomeLabels` memo.

### What It Does (Technical)
1. Removed the `customers-modal-genz-kunde-col--3` call-log block.
2. Offer column BEM suffix `--4` → `--3` to match third physical column for dark-theme CSS.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Same `TimetableEntry`; fields remain editable via timetable row / call modal / offer modal |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** Unchanged

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
- Inline editing of `notes`, `purpose`, `scheduled_at`, etc. from the drawer is no longer available; users rely on the main timetable row editor patterns or **Log call** / list flows outside this column.

---

## FEATURE-229: Timetable Kundenkontakt column 2 — edit-customer parity (kontakt card + Termine)
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The timetable **Kontaktperson** column in `TimetableContactDrawer` now mirrors the **customer edit** experience: same nested contact card behaviour (no per-contact website field, remove contact only when more than one, ASCII transliteration on name/note blur, avatar circle uses the same pill colour index as pills), plus a second **Termine** card matching `CustomersPage`’s `editKundeAppointmentsContent` (toggle “New appointment” inline form, table with date/time/purpose/done/remove, data stored on `contact_profile.appointment_history`).

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableExtraKontakteBlock.tsx` — optional `showCustomersStyleAppointments` + `appointmentHistory`; contact UI parity; appointments UI and `patchAppointmentHistory`.
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — pass `showCustomersStyleAppointments` and `appointmentHistory={p.appointment_history}` into the kontakt column block.

### What It Does (Technical)
1. Contact edits still go through `patchDraft` → `ensureProfile` → `contacts[]` updates; remove adjusts `activeIdx` like `NewCustomerModal`.
2. `commitAsciiNormalized` + `transliterateToAscii` / `transliterateToAsciiMultiline` on name and note fields on blur/composition end.
3. When `showCustomersStyleAppointments` is true, the block reads `appointmentHistory` from props and writes merged `appointment_history` on the row profile (cleared key when list empty).
4. “New appointment” collects date (required), optional time, purpose (required), then appends a `TimetableAppointmentHistoryRow` built from `emptyAppointmentRow()`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `contacts` | `TimetableContactPerson[]` | No | Extra contacts on `contact_profile.contacts` |
| `appointmentHistory` | `TimetableAppointmentHistoryRow[]` | No | Read slice for Termine table when appointments UI enabled |
| `appointment_history` (persisted) | same | No | Updated when adding/toggling/removing rows |

### Database
- **Engine:** Browser persistence via existing timetable row store (same as other `contact_profile` fields)
- **Tables Affected:** N/A (frontend row JSON)
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts` (unchanged contract; normalizes existing `appointment_history` shape)
- **Actions/Selectors added:** None
- **Persisted:** Yes — with timetable entry

### i18n Keys Added
None (reuses `customersAppointmentsTitle`, `customersNewAppointment`, `customersAppt*`, `customersNoAppointments`, `customersTerminZweckPh`, `commonSave`, `commonCancel`, `commonRemove`, `commonPlaceholderDash`, and existing kontakt keys).

### Dependencies Added
None.

### Notes / Known Limitations
- `TimetableContactDrawerFormBody` does not pass `showCustomersStyleAppointments`; the wide scroll form still has its separate “Further customer appointments” section.
- Role `<option>` labels remain German literals (same as `NewCustomerModal`).

---

## FEATURE-230: Timetable Kundenkontakt Angebot column — wireframe field styling + CTA
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The third column **Angebot** in `TimetableContactDrawer` keeps the same field stack as the wireframe (Fahrzeugtyp, Marke, Modell, Aufbau, Zulassung, Jahr / Kilometerstand, Preisvorstellung, Angebotsnotizen) but uses the **customer-modal kontakt** input/label styling (`customerModalKontakt*` classes), `customers-modal-genz-frost-card` padding aligned with other nested cards, accessible `htmlFor`/`id` pairs, and a **full-width** outline CTA with **sky** border/text on white. The footer button label matches the column title key (`timetableContactColOffer`) so German shows **Angebot** as in the reference.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — Angebot column JSX and `contactDrawerFormClasses` imports.

### What It Does (Technical)
1. All offer and `vehicle_extra` fields in that column use `customerModalKontaktLabelClass` / `customerModalKontaktInputClass` instead of the drawer’s default `labelClass` / `inputClass` (rounded-xl / sky ring).
2. Bottom action calls `onEditOffer(draft)`; **FEATURE-231** removed the `has_trucks` disable gate on this button.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Same `TimetableTruckOffer` + `vehicle_extra` bindings as before |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** Unchanged

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-231: Timetable Angebot actions — always enabled (drawer + table)
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **Truck · Angebot** controls were only active when `outcome === 'has_trucks'`, which made the toolbar pill and table row button look **disabled** and blocked opening **`TimetableOfferModal`** for typical rows. Those gates are **removed**: the drawer header button, the Angebot column footer button, and the timetable table **Angebot** row action are always clickable. Saving an offer in the modal still sets **`outcome: 'has_trucks'`** via existing `applyOffer` logic.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — remove `disabled` / disabled styling from header offer button and column CTA.
- `frontend/src/pages/timetable/TimetableTable.tsx` — remove `disabled` and grey “locked” styling; use the same amber gradient affordance for all rows.

### What It Does (Technical)
1. `onEditOffer(draft)` / `onOpenOffer(row)` fire regardless of `TimetableOutcome`.
2. No change to `TimetableOfferModal` save behaviour.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Same as before |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** Unchanged

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-232: Timetable Angebot column — underline form + offer generator
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Added

### What Was Added
The **Angebot** column in `TimetableContactDrawer` now uses a **minimal underline field stack** (labels + bottom-border inputs) mapped to the existing offer model: **Fahrzeugart**, **Hersteller / Modell** (two inline fields), **Aufbauart**, **Erstzulassung**, **KM-Stand**, **Ausstattung** (`offer.notes`), **Preisvorstellung**; optional **chevron** opens the full **`TimetableOfferModal`**. Below that, an **Angebots-Generator** card (indigo header, hint, textarea, **Copy / Format / Save**) runs **on-device heuristics** (`parseOfferFreeText`) to fill mileage, price, registration/year, body hints, vehicle type, brand/model from pasted text — no API.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferMinimalBlock.tsx` — UI + generator state.
- `frontend/src/pages/timetable/timetableOfferFreeTextParse.ts` — parse helpers.
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — replace inline offer form with `TimetableOfferMinimalBlock`.
- `frontend/src/contexts/LanguageContext.tsx` — `timetableOfferMinimal*` and `timetableOfferGen*` keys (de/en/fr + overrides es,it,pt,tr,ru,hi,ar,zh,ja).

### What It Does (Technical)
1. **Format** parses `genText`, merges patches via `setOfferField` / `setVehicleExtra`, replaces textarea with a short summary line list when matches exist.
2. **Save** parses, applies patches, clears textarea, marks drawer dirty.
3. **Copy** writes textarea to clipboard when possible.
4. Generator panel can be collapsed (X) and reopened via a compact strip button.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| Same as `TimetableTruckOffer` + `vehicle_extra.body_type` / `registration_mm_yyyy` | | | |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** Via existing drawer Save on row

### i18n Keys Added
`timetableOfferMinimalFahrzeugart`, `timetableOfferMinimalHerstellerModell`, `timetableOfferMinimalAufbauart`, `timetableOfferMinimalErstzulassung`, `timetableOfferMinimalKmStand`, `timetableOfferMinimalAusstattung`, `timetableOfferMinimalPreisvorstellung`, `timetableOfferMinimalUnderlinePh`, `timetableOfferGenTitle`, `timetableOfferGenHint`, `timetableOfferGenPlaceholder`, `timetableOfferGenCopy`, `timetableOfferGenFormat`, `timetableOfferGenSave`, `timetableOfferGenCloseAria`, `timetableOfferMinimalMoreAria`, `timetableOfferGenApplied`, `timetableOfferGenEmpty`, `timetableOfferGenCopyFailed`, `timetableOfferGenCopyDone` — in **MESSAGES.de/en/fr** and all **LANGUAGE_OVERRIDES**.

### Dependencies Added
None.

### Notes / Known Limitations
- Parsing is heuristic only; edge cases may need a future API-backed step.
- **Year** is derived from **Erstzulassung** `MM/YYYY` when parsed from free text; there is no separate year line in the minimal form.

---

## FEATURE-233: Timetable Angebot column — LKW-modal field parity + reference styling
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **Angebot** tab in the timetable contact drawer keeps the same **underline shell**, floating **chevron → full modal**, and **Angebots-Generator** block, but the **visible field grid** now matches **`TimetableOfferModal` / LKW-Angebot**: two-column **Vehicle type | Brand**, **Model | Location**, **Year | Mileage (km)**, **Quantity | Expected price (EUR)**, plus full-width **Offer notes** textarea. Primary CTA uses **`#4A5568`**; generator header **`#7C3AED`** with **slate** body panel per design references.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferMinimalBlock.tsx` — field grid, CTA and generator visual tokens; `vehicleExtra` destructured as `_vehicleExtra` (still accepted for prop compatibility; generator may still patch `vehicle_extra` via parser).

### What It Does (Technical)
1. User edits `TimetableTruckOffer` fields through `setOfferField` as before.
2. **Format / Save** on the generator still call `parseOfferFreeText` → `setOfferField` / `setVehicleExtra`; patches may include `vehicle_extra` keys not shown in the minimal grid.
3. **Chevron** and bottom **Angebot** button both open the full offer modal via `onOpenFullOfferModal`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `offer.vehicle_type`, `offer.brand`, `offer.model`, `offer.location`, `offer.year`, `offer.mileage_km`, `offer.quantity`, `offer.expected_price_eur`, `offer.notes` | string / number / null | No | Same as full modal offer model |
| `vehicle_extra` (via generator only) | partial `TimetableVehicleDisplayExtra` | No | Optional merge from free-text parse |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** Via existing drawer save on timetable row

### i18n Keys Added
None (reuses `timetableOfferVehicleType`, `timetableOfferBrand`, `timetableOfferModel`, `timetableOfferLocation`, `timetableOfferYear`, `timetableOfferMileage`, `timetableOfferQuantity`, `timetableOfferExpectedPrice`, `timetableOfferNotes`, `timetableContactColOffer`, `timetableOfferGen*`, `timetableOfferMinimalUnderlinePh`, `timetableOfferMinimalMoreAria`).

### Dependencies Added
None.

### Notes / Known Limitations
- Free-text parser may still populate **registration / body** into `vehicle_extra` while those inputs are not in the minimal grid; users can open the full modal to see or edit all fields.

---

## FEATURE-234: Timetable contact drawer — workspace tabs (call vs offer); generator only on Angebot
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The customer contact drawer now has an explicit **two-tab workspace** under the toolbar: **Anruf loggen** (call & contact) shows **Stammdaten + Kontakt/Termine** only; **Angebot** shows the **offer column** with `TimetableOfferMinimalBlock` (underline fields, CTA, **Angebots-Generator**). The generator no longer appears alongside the call/contact columns. Opening **Add/Edit offer** from the toolbar switches to the **Angebot** tab (and still opens the full `TimetableOfferModal`). Changing the selected timetable row resets the workspace to the call tab.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `drawerWorkspaceTab` state, tablist + tabpanels (`display: contents` wrapper for the two-column call view), conditional grid; removed unused `TT_EDIT_KUNDE_*` grid constants.
- `frontend/src/contexts/LanguageContext.tsx` — `timetableContactDrawerTabCall`, `timetableContactDrawerTabsAria` in **MESSAGES.de/en/fr** and all **LANGUAGE_OVERRIDES** (es, it, pt, tr, ru, hi, ar, zh, ja).

### What It Does (Technical)
1. `useEffect` on `entry?.id` sets `drawerWorkspaceTab` to `'call'`.
2. Tab buttons set `'call' | 'offer'`; offer panel mounts `TimetableOfferMinimalBlock` only when `'offer'`.
3. Truck toolbar button calls `setDrawerWorkspaceTab('offer')` then `onEditOffer(draft)`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | No change to persisted row shape |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** Local React state `drawerWorkspaceTab` on `TimetableContactDrawer`
- **Persisted:** No

### i18n Keys Added
`timetableContactDrawerTabCall`, `timetableContactDrawerTabsAria` — **MESSAGES.de/en/fr** + overrides **es, it, pt, tr, ru, hi, ar, zh, ja**. Offer tab label reuses `timetableContactColOffer`.

### Dependencies Added
None.

### Notes / Known Limitations
- Switching away from **Angebot** unmounts `TimetableOfferMinimalBlock` (generator textarea state is cleared until user returns to the tab).

### Extended (same day)
**Toolbar merge:** The duplicate second row of **Anruf loggen / Angebot** was removed. A single toolbar row contains workspace **tabs** (text labels, active = gradient fill) and **Smart summary**.

**Icon strip removed:** The phone and truck **square icon-only** buttons next to the tabs were removed; **Log call** remains on the timetable row actions; **full offer** remains via **Angebot** tab (`TimetableOfferMinimalBlock` chevron / primary button).

---

## FEATURE-235: Timetable contact drawer — Bemerkungen column (row notes) on call workspace
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Added

### What Was Added
On the **Anruf loggen** workspace tab, the main grid is now **three columns**: Stammdaten | Kontakt/Termine | **Bemerkungen**. The third panel is a tall, resizable **textarea** filling the column, bound to the timetable row’s **`notes`** (same field as the list “Notes” column). Placeholder reuses **`timetableContactTableNotesPh`**.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `lg:grid-cols-3`, `items-stretch`, new `customers-modal-genz-kunde-col--4` panel; `textareaClass` import.
- `frontend/src/contexts/LanguageContext.tsx` — **`timetableContactBemerkungen`** in **MESSAGES.de/en/fr** and all **LANGUAGE_OVERRIDES**.

### What It Does (Technical)
1. `patchDraft` updates `draft.notes` on textarea change; existing save path already persists `notes` on the entry.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `notes` | string | No | Timetable row notes / Bemerkungen |

### Database
- **Engine:** Browser `localStorage` (timetable store)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts` (unchanged contract)
- **Actions/Selectors added:** None
- **Persisted:** Yes — with existing contact save

### i18n Keys Added
`timetableContactBemerkungen` — **de/en/fr** + overrides **es, it, pt, tr, ru, hi, ar, zh, ja**.

### Dependencies Added
None.

### Notes / Known Limitations
- Narrow view stacks three panels vertically; the Bemerkungen block uses viewport-based **min-height** so the notes area stays usable.

---

## FEATURE-236: Timetable contact drawer — Offer generator as sibling column outside Angebot card
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
On the **Angebot** tab, the **Angebots-Generator** (paste free text → format/save into offer fields) is no longer nested inside the frosted **Angebot** card beside the underline form. It sits in a **separate left column** at tab level; the **Angebot** heading and frosted card with the minimal offer form occupy the **right column** (`lg:grid-cols-2`). Narrow screens stack generator above the offer block.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — Offer tabpanel: `grid grid-cols-1 lg:grid-cols-2`, import and render `TimetableOfferGeneratorBlock`; removed `max-w-[min(100%,42rem)] mx-auto` from the offer workspace.
- `frontend/src/pages/timetable/components/TimetableOfferMinimalBlock.tsx` — Form-only: removed embedded generator UI and props `rowKey`, `vehicleExtra`, `setVehicleExtra`, `onGeneratorApplied`.
- `frontend/src/pages/timetable/components/TimetableOfferGeneratorBlock.tsx` — (existing) holds generator state, copy/format/save, collapse strip; consumed from drawer only.

### What It Does (Technical)
1. User opens **Angebot** tab → left column mounts `TimetableOfferGeneratorBlock` with `rowKey={String(draft.id)}`; textarea resets when the row changes.
2. **Format** / **Save** call `setOfferField` / `setVehicleExtra` from the drawer’s callbacks and `onGeneratorApplied` → `setDirty(true)`.
3. Right column shows section title **Offer** + `customers-modal-genz-frost-card` wrapping `TimetableOfferMinimalBlock` (underline fields + chevron + primary **Offer** CTA).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| Same as before | `TimetableTruckOffer` / `TimetableVehicleDisplayExtra` patches | No | Generator still emits the same `Partial<>` patches via drawer callbacks |

### Database
- **Engine:** Browser `localStorage` (timetable store)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A (draft state in drawer)
- **Actions/Selectors added:** None
- **Persisted:** Yes — unchanged save path on drawer save

### i18n Keys Added
None (reuses existing `timetableOfferGen*` and `timetableContactColOffer` keys).

### Dependencies Added
None.

### Notes / Known Limitations
- Leaving the **Angebot** tab still unmounts the generator; draft textarea clears until the user returns (same as prior nested behavior).

---

## FEATURE-237: Timetable Angebot tab — vehicle extra fields on minimal offer form
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Added

### What Was Added
The **Angebot** tab’s frosted minimal form now includes the same **vehicle extra** block as wireframe Section E “Package” / `TimetableContactDrawerFormBody`: **body**, **registration (MM/YYYY)**, **replacement engine km**, **customer price**, **processor** + **entered / fetched / negotiated by**, and **Sold** / **Deregistered** checkboxes. Values persist on `contact_profile.vehicle_extra` via existing `setVehicleExtra`.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferMinimalBlock.tsx` — props `vehicleExtra`, `setVehicleExtra`; new subsection under offer notes with underline styling.
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — restore `ve` from profile; pass into `TimetableOfferMinimalBlock`.

### What It Does (Technical)
1. User edits fields → `setVehicleExtra` merges into `ensureProfile(draft).vehicle_extra`.
2. `patchDraft` marks the drawer dirty; **Save contact** uses existing persist path.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `vehicle_extra` | `TimetableVehicleDisplayExtra` | No | Same shape as main contact form body |

### Database
- **Engine:** Browser `localStorage` (timetable row / contact profile)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes — with contact save

### i18n Keys Added
None — reuses `timetableContactVehicleExtra`, `timetableContactBodyType`, `timetableContactRegistration`, `timetableContactRegistrationPh`, `timetableContactMileageReplacement`, `timetableContactPriceCustomer`, `timetableContactProcessor*`, `timetableContactSold`, `timetableContactDeregistered`, `timetableOfferMinimalUnderlinePh`.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-238: Timetable Angebot tab — minimal offer form reduced to seven fields
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **Angebot** frosted minimal form shows **only** the seven fields from the product reference: **Fahrzeugart**, **Hersteller und Modell** (one line; first word → `brand`, remainder → `model`), **Aufbauart**, **Erstzulassung (Monat/Jahr)**, **KM-Stand**, **Ausstattung** (bound to `offer.notes`), **Preisvorstellung** (`expected_price_eur`). All other minimal fields and the extra “registration & status” processor block were removed from this surface; **full offer modal** and **Anruf loggen** vehicle section still carry the full set for legacy rows.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferMinimalBlock.tsx` — replaced grid with single-column seven-field layout; labels use `timetableOfferMinimal*` keys (already in `LanguageContext.tsx`).

### What It Does (Technical)
1. `vehicle_type`, `mileage_km`, `expected_price_eur`, `notes` via `setOfferField`.
2. `body_type`, `registration_mm_yyyy` via `setVehicleExtra`.
3. Chevron + bottom **Offer** button still opens `onEditOffer` for the full dialog.

### Data It Accepts / Emits
| Field | Storage | Description |
|----|---|----|
| Fahrzeugart | `offer.vehicle_type` | |
| Hersteller und Modell | `offer.brand` + `offer.model` | Split on first space on edit |
| Aufbauart | `vehicle_extra.body_type` | |
| Erstzulassung | `vehicle_extra.registration_mm_yyyy` | |
| KM-Stand | `offer.mileage_km` | |
| Ausstattung | `offer.notes` | Same JSON field as former “offer notes” |
| Preisvorstellung | `offer.expected_price_eur` | |

### Database
- **Engine:** Browser `localStorage` (timetable row)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** Yes — unchanged save path

### i18n Keys Added
None — uses existing `timetableOfferMinimal*` and `timetableContactRegistrationPh`.

### Dependencies Added
None.

### Notes / Known Limitations
- **Hersteller und Modell:** multi-word makes (e.g. “Mercedes Benz Actros”) split at the **first** space only; users can fix split in the full offer modal.
- Other offer columns (location, year, quantity, processors, sold flags, etc.) remain in persisted data but are not editable on this minimal strip until the user opens the full offer dialog.

---

## FEATURE-239: Timetable contact drawer — icons on Call / Offer workspace tabs
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **Anruf loggen** (call workspace) and **Angebot** (offer workspace) tab buttons in the contact drawer toolbar now show **lucide-react** icons beside the label: **Phone** on the call tab and **FileText** on the offer tab. Icons inherit tab text colour (white when active, themed when inactive) and use `aria-hidden` because the tab text remains the accessible name.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `Phone`, `FileText` imports; `gap-1.5` + icon + `<span>` label in each `role="tab"` button.

### What It Does (Technical)
1. Tablist buttons render icon + translated string; no change to tab state or `aria-controls` wiring.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-240: Timetable contact drawer — Offer workspace tab uses truck icon
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **Angebot** workspace tab icon was changed from **FileText** to **Truck** (`lucide-react`) for a truck-offer visual; **Anruf loggen** still uses **Phone**.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `Truck` import replaces `FileText` for the offer tab only.

### What It Does (Technical)
1. Offer `role="tab"` renders `<Truck aria-hidden />` with the same sizing classes as before.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | N/A |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None.

### Dependencies Added
None.

### Notes / Known Limitations
None.

---

## FEATURE-241: Angebots-Generator — workflow copy, tooltips, Ausstattung extraction
**Date:** 2026-04-13
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
Clarified the **offer generator** story: customers reach out by **call / email / messenger** with unstructured truck-for-sale text; **Format** extracts values, shows a **structured summary** in the textarea, and **fills the offer fields** on the right; **Save** applies the same parsing and clears the box. Added **`timetableOfferGenFormatTitle`** / **`timetableOfferGenSaveTitle`** as button `title` tooltips. Extended **`parseOfferFreeText`** to populate **`offer.notes`** (Ausstattung) from parenthetical lists and common equipment tokens (Retarder, Klima, Navi, etc.).

### Where It Was Added
- `frontend/src/pages/timetable/timetableOfferFreeTextParse.ts` — `extractEquipmentNotes`, `offerPatch.notes` + summary line.
- `frontend/src/pages/timetable/components/TimetableOfferGeneratorBlock.tsx` — hint/placeholder fallbacks; `title` on Format/Save.
- `frontend/src/contexts/LanguageContext.tsx` — updated **`timetableOfferGenHint`**, **`timetableOfferGenPlaceholder`** in **de/en/fr** and **LANGUAGE_OVERRIDES** (es, it, pt, tr, ru, hi, ar, zh, ja); new keys in all those bundles.

### What It Does (Technical)
1. User pastes text → **Format** / **Save** runs `parseOfferFreeText` → `setOfferField` / `setVehicleExtra` merge patches.
2. Equipment phrases add **`notes`** when detected; structured summary lines include `Ausstattung: …` (German label in machine summary for consistency with existing `Preis:` / `KM:` lines).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `notes` | string | No | Ausstattung / equipment when parser finds tokens |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None.

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No (same as row save path when user saves contact)

### i18n Keys Added
`timetableOfferGenFormatTitle`, `timetableOfferGenSaveTitle` — **MESSAGES.de/en/fr** + overrides **es, it, pt, tr, ru, hi, ar, zh, ja**. Updated **`timetableOfferGenHint`**, **`timetableOfferGenPlaceholder`** in the same bundles.

### Dependencies Added
None.

### Notes / Known Limitations
- Summary lines in the textarea after **Format** remain partly German abbreviations (`KM:`, `Preis:`, `Ausstattung:`) regardless of UI language; full i18n for that preview would need `t()` inside the parser or a separate formatter.

---

