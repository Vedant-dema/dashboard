# Feature Log — 2026-04-15

> Features added or modified on this date.

---

## FEATURE-258: Timetable Bemerkungen — large comfort-reading dialog
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Added 

### What Was Added
In the calendar **customer contact** drawer, the **Bemerkungen** (dated activity notes) column now has a **“Large view”** control that opens the same notes thread in a **near full-screen modal** with **larger typography**, **spacious cards**, and **44px-class touch targets** so staff can read long correspondence comfortably (accessibility / older users). The overlay stacks above the drawer (`z-[110]`), supports **backdrop click** and **Escape** to close, and reuses the same `patchDraft` data path as the inline thread.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `bemerkungenComfortOpen` state; header row with open button; second `createPortal` overlay with dialog chrome; Escape order (overlay before smart summary before drawer close).
- `frontend/src/pages/timetable/components/TimetableActivityNotesThread.tsx` — new `layout="comfortOverlay"` variant: expanded scroll region, `text-base`/`text-lg` note body, larger composer, `min-h-11` icon actions, rounded-xl cards.
- `frontend/src/contexts/LanguageContext.tsx` — i18n keys `timetableContactBemerkungenOpenLarge`, `timetableContactBemerkungenLargeTitle`, `timetableContactBemerkungenLargeHint` in **de / en / fr** and all **LANGUAGE_OVERRIDES** locales (es, it, pt, tr, ru, hi, ar, zh, ja).

### What It Does (Technical)
1. User clicks **Large view** beside the Bemerkungen heading → `setBemerkungenComfortOpen(true)`.
2. A portal renders a `role="dialog"` overlay: title + hint from `t()`, primary **Close** control, backdrop `onClick` closes.
3. `TimetableActivityNotesThread` mounts with `layout="comfortOverlay"` — same `buildActivityNotesDisplayList` / `patchDraft` as the embedded column, so edits stay in the shared `draft` until **Save contact**.
4. `Escape` key closes the overlay first (does not close the whole drawer while overlay is open).
5. Switching timetable row (`entry.id`) resets the overlay closed.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `draft` | `TimetableEntry` | Yes | Same draft object as the drawer; notes thread reads/writes `contact_profile.activity_notes_log` etc. |
| `patchDraft` | function | Yes | Updates draft + marks dirty like the inline column |
| `bemerkungenComfortOpen` | `boolean` | Yes | Controls portal visibility |

### Database
- **Engine:** None (in-memory draft until user saves timetable row)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No (inherits drawer save)

### i18n Keys Added
`timetableContactBemerkungenOpenLarge`, `timetableContactBemerkungenLargeTitle`, `timetableContactBemerkungenLargeHint` — all primaries and overrides; `commonClose` reused for close control `aria-label`.

### Dependencies Added
None

### Notes / Known Limitations
Two instances of the thread (column + overlay) are not open at once by UX; only one portal mounts when large view is on. Reduced-motion users still get larger type; motion is limited to backdrop blur only.

---

## FEATURE-259: Kunden — print / PDF field handout for on-site staff
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Added | **Extended** (same day): fuller contact tree, all addresses, billing/bank, VIES snapshot, wash invoice address, document path, printed-by meta, on-site purpose blurb in header; **DEMA letterhead** (DE/MA logo, GmbH & Co. KG, services tagline, vector truck graphic) on print — **no** corporate address/tax block or bank/legal footer; confidentiality line only. **Extended again:** print window `<title>` shortened to `DEMA · {Kunden-Nr.}` so the browser’s optional header line is less noisy; print button carries a **tooltip** and **screen-reader** text explaining that **headers/footers** must be turned off in the print dialog (Chrome/Edge/Firefox) — **web CSS cannot remove** that margin text. **Extended again:** field handout is **PDF-only** (html2pdf.js): hidden iframe + blob document, download — **no** `window.print()` or pop-up tab. **Extended again:** print layout **slimmed** — no subtitle (“internal reference”), no generated/printed-by line, no purpose blurb box; **FIRMA & STAMMDATEN** drops legal form, customer no., industry, responsible person, business unit, and intake date; **Contacts & phone tree** and **Invoicing, limits & bank** sections removed entirely from the handout.

### What Was Added
In **Kunden** (customer edit modal), a **Print / Field handout** control next to **Open documents** opens a **print-optimised HTML summary** (UTF-8) in a new window. It is structured for **gate checks, vehicle ID, wash authorization, and escalation**: company & legal master data (incl. legal/natural type, legal form, booking account, VAT IDs, responsible person, entry date), **every registered address** with per-address tax lines, a **contacts & phone tree** (how-to-use line, primary Stamm contact with preferred channel, all `kontakte` with phone codes, email, **contact website**, notes), **billing / credit / bank / SEPA** (master), **last VIES snapshot**, expanded **Waschanlage** (Bukto, invoice address, wash bank), appointments, linked companies **with linked customer phone when available**, notes (incl. marketing), **Unterlagen path**, payment blocked, **last master update**; header includes **purpose blurb** and **printed-by** when the user is signed in. Staff use the browser **Print** dialog to print or **Save as PDF**.

### Where It Was Added
- `frontend/src/features/customers/utils/customerFieldBriefPrint.ts` — `buildCustomerFieldBriefSections`, `buildCustomerFieldBriefHtml`, `downloadCustomerFieldBriefPdf` (A4-oriented CSS; hidden iframe + **html2pdf.js** for download; no `window.print()`).
- `frontend/package.json` — dependency `html2pdf.js`.
- `frontend/src/pages/CustomersPage.tsx` — `handlePrintCustomerFieldBrief`; `editHeaderDocumentsAction` wraps documents + print buttons.
- `frontend/src/contexts/LanguageContext.tsx` — `customersFieldBrief*` keys in **de / en / fr** and **LANGUAGE_OVERRIDES** (es, it, pt, tr, ru, hi, ar, zh, ja).

### What It Does (Technical)
1. User opens a customer row → edit `NewCustomerModal`.
2. Clicks **PDF / Field handout** → `buildCustomerFieldBriefSections` collects from `draftKunde` + `termineForCustomer`: **Firma & Stammdaten** (no account status, no master VAT ID; incl. company website), **Standorte & Adressen** (every `adressen` row with country + Art Land + tax lines), **Ansprechpartner** (master + all `kontakte`), **Termine** (up to 48 rows). **No** VIES block; no footer line; letterhead without truck graphic.
3. `downloadCustomerFieldBriefPdf` builds HTML, mounts styles + `.sheet` **off-screen in the main document** (DOMParser) so html2canvas sees normal computed styles (better in Firefox/Safari); on failure it **falls back** to a hidden `iframe` + `blob:` URL; waits for `fonts.ready` / rAF / short timeout, then runs **html2pdf.js** and triggers download.
4. If `window.open` is blocked, `alert` uses `customersFieldBriefPopupBlocked`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `draftKunde` | `KundenStamm` | Yes | Source for identity, address, contacts, notes |
| `draftWash` | `KundenWashStamm \| null` | No | Wash / fleet block when present |
| `termineForCustomer` | `KundenTermin[]` | No | Up to 24 appointment rows |
| `beziehungenForCustomer` | `KundenBeziehung[]` | No | Relationship rows with resolved company names |

### Database
- **Engine:** N/A (reads existing in-memory / repository state already loaded on Customers page)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
Original `customersFieldBrief*` keys plus **extended** set: `customersFieldBriefPrintedBy`, `customersFieldBriefPurposeBlurb`, `customersFieldBriefLabelCustomerType`, `customersFieldBriefSectionLocations`, `customersFieldBriefSectionPeople`, `customersFieldBriefLabelCallPriority`, `customersFieldBriefContactsLead`, `customersFieldBriefSectionFinancial`, `customersFieldBriefSectionVies`, billing/VIES/channel/marketing/wash-invoice labels, etc. — **de / en / fr** and all **LANGUAGE_OVERRIDES**; row labels still reuse `history*` / `newCustomer*` where applicable. Plus `customersFieldBriefPrintPageTitle`, `customersFieldBriefPrintBrowserHint`, `customersFieldBriefPrintBrowserHintShort` (all primaries + overrides).

### Dependencies Added
None

### Notes / Known Limitations
Relies on pop-up permission; output is browser print/PDF quality (not server-side PDF). Long notes flow across pages; sections use `page-break-inside: avoid` where practical. **Browser default headers/footers** (title, URL, date, page numbers) are outside app control — users must disable them in the print dialog for a “clean letterhead only” sheet.

---

## FEATURE-260: Kunden field brief PDF — dashboard logo in letterhead
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **customer field handout PDF** letterhead now shows the **same raster logo** the app uses in the **sidebar** (`/dema-logo.png`), **top-right** next to the DE/MA wordmark and tagline, sized for A4 capture (max height/width constraints). The image `src` is built as an **absolute application URL** so **html2canvas** still loads the asset in the **blob-iframe** fallback (relative `/…` would break against a `blob:` document base).

### Where It Was Added
- `frontend/src/features/customers/utils/customerFieldBriefPrint.ts` — `fieldBriefLetterheadLogoSrc()`, letterhead flex layout, `.dema-letterhead-logo-wrap` / `.dema-letterhead-logo` styles, `<img>` in `buildCustomerFieldBriefHtml`.

### What It Does (Technical)
1. `buildCustomerFieldBriefHtml` resolves logo href via `new URL("/dema-logo.png", window.location.origin)` when `window` exists; otherwise falls back to `"/dema-logo.png"`.
2. Letterhead is a **horizontal flex**: left column unchanged (DE/MA + GmbH & Co. KG + tagline), right column optional-looking block with **decorative** `alt=""` image (intrinsic `width`/`height` hints for layout; CSS caps display size).
3. `downloadCustomerFieldBriefPdf` unchanged; off-screen mount and iframe path both receive the same HTML string.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Static asset path only; no new opts on `CustomerFieldBriefHtmlOpts`. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
The **Außendienst-PDF** letterhead right column uses **`/dema-logo.png`** (same as the sidebar). FEATURE-262’s truck SVG was removed again in **FEATURE-263**.

---

## FEATURE-262: Kunden field brief PDF — letterhead truck graphic (not dashboard logo)
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **field handout PDF** letterhead **right-hand motif** is again the **two stylized HGV tractor outlines** (corporate blue line art), implemented as an **inline SVG** embedded in the HTML. The **`/dema-logo.png` `<img>`** was removed so the PDF matches printed stationery and does **not** rely on asset URLs inside **blob iframe** capture.

### Where It Was Added
- `frontend/src/features/customers/utils/customerFieldBriefPrint.ts` — `buildDemaFieldBriefLetterheadTrucksSvg()`; letterhead markup uses `.dema-letterhead-trucks-wrap` + SVG; removed `fieldBriefLetterheadLogoSrc`; CSS classes `.dema-letterhead-trucks` / `.dema-letterhead-trucks-wrap`.

### What It Does (Technical)
1. `buildCustomerFieldBriefHtml` injects a single-line **SVG** string (viewBox `0 0 148 42`): rear cab path + wheels, front cab path + wheels, stroke `#1a2744`, no fill.
2. **html2canvas** rasterizes the vector like other DOM; no CORS/network image load for this element.
3. Display size controlled by CSS (`width: 118pt`, `max-height: 44pt`).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Static decorative SVG only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
The SVG is a **simplified** interpretation of the printed trucks, not a traced scan. To match print **exactly**, replace the path data with an exported asset from design (still inline or data-URL). **Superseded by FEATURE-263:** the truck SVG was removed; **`/dema-logo.png`** was restored.

---

## FEATURE-263: Kunden field brief PDF — remove truck SVG, restore dashboard logo
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **inline truck SVG** (FEATURE-262) was **removed** from the field-brief PDF letterhead. The **right-hand slot** again shows the **dashboard raster logo** **`/dema-logo.png`** via `<img>` with an **absolute** `src` (`fieldBriefLetterheadLogoSrc`) for **blob iframe** compatibility with html2canvas.

### Where It Was Added
- `frontend/src/features/customers/utils/customerFieldBriefPrint.ts` — deleted `buildDemaFieldBriefLetterheadTrucksSvg`; restored `fieldBriefLetterheadLogoSrc`; markup/CSS back to `.dema-letterhead-logo-wrap` / `.dema-letterhead-logo`.

### What It Does (Technical)
1. `buildCustomerFieldBriefHtml` sets `src` to `new URL("/dema-logo.png", window.location.origin).href` when possible.
2. Letterhead flex row unchanged: left DE/MA + tagline, right logo image.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Static public asset path. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Logo must exist at **`frontend/public/dema-logo.png`** (or equivalent deploy). Missing file → broken image in PDF.

---

## FEATURE-261: Purchase timetable table — hide Arte and buyer columns
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
On the **Einkaufs-Terminplan** main grid, two columns are no longer shown: the **KA / A (Arte)** subtype column and the **buyer / processing person** column (often **DEMO** in demo sessions). Row data and the contact drawer still store `legacy_arte` and `buyer_name`; only the list view is simplified.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableTable.tsx` — removed header and body cells for Arte and buyer; dropped `kaArtFromRow`; `COL_COUNT` 14→12; slightly reduced table `min-w`.

### What It Does (Technical)
1. Table header row no longer renders `timetableColArte` or `timetableColVp`.
2. Each body row no longer renders the second badge column (`legacy_arte` / derived KA|A) or the `row.buyer_name` cell.
3. Empty-state row `colSpan` matches the new column count (12).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Display-only change; `TimetableEntry` shape unchanged. |

### Database
- **Engine:** Browser `localStorage` (timetable store) / None for this UI-only change
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No change to persistence

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Buyer and Arte values remain editable in the **contact drawer** (`TimetableContactDrawerFormBody` still shows buyer). If the product later needs Arte visible in the grid again, restore one `<th>`/`<td>` pair and `kaArtFromRow` (or read `legacy_arte` only).

---

## FEATURE-264: Kunden field brief PDF — omit appointments (TERMINE) section
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **customer field handout PDF** no longer includes an **appointments** block (**TERMINE** / `customersFieldBriefSectionAppointments`). Identity, addresses, and contacts sections are unchanged. `buildCustomerFieldBriefSections` no longer accepts a `termine` argument.

### Where It Was Added
- `frontend/src/features/customers/utils/customerFieldBriefPrint.ts` — `buildCustomerFieldBriefSections` drops appointment row building and the fourth section; removed `KundenTermin` import.
- `frontend/src/pages/CustomersPage.tsx` — `handleDownloadCustomerFieldBriefPdf` calls `buildCustomerFieldBriefSections(draftKunde, t)`; dependency array no longer lists `termineForCustomer`.

### What It Does (Technical)
1. PDF HTML is still built from `CustomerBriefSection[]`; the appointments slice is never pushed.
2. Customer appointments remain available elsewhere in the app; only this PDF export scope changed.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `termine` | — | — | Removed from `buildCustomerFieldBriefSections` signature (no longer emitted in PDF). |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None (`customersFieldBriefSectionAppointments` / `customersApptDone` remain in app for other UI.)

### Dependencies Added
None

### Notes / Known Limitations
To show appointments on the handout again, reintroduce a `termine` parameter and the previous `apptRows` / section block.

---

## FEATURE-265: Purchase timetable — quick callback reminders (10 / 30 / 60 min)
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Added 

### What Was Added
On the **Einkaufs-Terminplan** daily grid, the **Wiedervorlage / Follow-up** column includes **one-tap presets** (**10 min**, **30 min**, **1 h**) so buyers capture “call me back in …” without opening the call modal. Each tap sets **`follow_up_at`** from the current wall clock, sets **`outcome`** to **`follow_up`**, clears **done**, stamps **`last_called_at`**, persists via **`upsertTimetableEntry`**, shows a **toast**, and when the **Notification API** is allowed schedules a **desktop notification** at due time (replacing any prior pending reminder for that row).

### Where It Was Added
- `frontend/src/store/timetableStore.ts` — **`followUpAtFromNowMinutes`**
- `frontend/src/pages/timetable/timetableReminderNotify.ts` — desktop reminder scheduling
- `frontend/src/pages/timetable/TimetableTable.tsx` — follow-up column quick actions
- `frontend/src/pages/timetable/TimetablePage.tsx` — **`applyQuickFollowUp`**, toolbar hint
- `frontend/src/contexts/LanguageContext.tsx` — **`timetableQuick*`**, **`timetableNotif*`**, **`timetableQueueToolbarHint`**

### What It Does (Technical)
1. User clicks **10 / 30 / 60** in the follow-up column → `applyQuickFollowUp(row, minutes)`.
2. `follow_up_at = followUpAtFromNowMinutes(minutes)`; row updated with **`outcome: 'follow_up'`**, **`is_completed: false`**, **`last_called_at: nowIsoDateTime()`**; state saved through existing persistence.
3. Toast shows company + localized **`toLocaleString`** for the slot.
4. **`Notification.requestPermission()`** on first use if needed; if **`granted`**, **`setTimeout`** + **`new Notification`** at **`follow_up_at`**.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `minutes` | `10 \| 30 \| 60` | Yes | Offset from now for `follow_up_at`. |
| `follow_up_at` | `string` (local ISO) | Yes | Written on **`TimetableEntry`**. |

### Database
- **Engine:** Browser `localStorage` (`dema-purchase-timetable-v6`)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts`
- **Actions/Selectors added:** **`followUpAtFromNowMinutes`**
- **Persisted:** Yes

### i18n Keys Added
`timetableQuickRemindLegend`, `timetableQuickRemind10`, `timetableQuickRemind30`, `timetableQuickRemind60`, `timetableQuickRemindAria`, `timetableQuickRemindToast`, `timetableQuickRemindToastNotifOn`, `timetableNotifReminderTitle`, `timetableNotifReminderBody`, `timetableQueueToolbarHint` — **de / en / fr** + **es, it, pt, tr, ru, hi, ar, zh, ja** overrides.

### Dependencies Added
None

### Notes / Known Limitations
Desktop reminders need the **browser session** until due time; background tab timer throttling may delay notifications slightly.

---

## FEATURE-266: Purchase timetable — reminder popover with custom minutes
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Extended (see FEATURE-265)

### What Was Added
The follow-up **quick reminder** UX was upgraded: a single **Reminder** control opens a **fixed-position dialog** (portal to `document.body`) with **nine presets** (5, 10, 15, 20, 30, 45, 60, 90, 120 minutes) plus a **custom minutes** field (**1–1440**, whole numbers) with **Apply** and **Enter** to submit. **Click-outside**, **Escape**, and **resize/scroll** repositioning preserve a professional workflow. Same persistence and optional **desktop notification** behaviour as FEATURE-265.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableFollowUpReminderCell.tsx` — new component (popover, presets, validation, portal).
- `frontend/src/pages/timetable/TimetableTable.tsx` — follow-up cell uses the new component.
- `frontend/src/contexts/LanguageContext.tsx` — **`timetableQuickRemindOpen`**, **`OpenAria`**, **`PanelTitle`**, **`PanelHint`**, **`PresetsSection`**, **`CustomSection`**, **`NMin`**, **`MinutesPh`**, **`Apply`**, **`MaxHint`**, **`Invalid`**; updated **`Legend`** / **`QueueToolbarHint`** in **de / en / fr** and **LANGUAGE_OVERRIDES**.

### What It Does (Technical)
1. User opens **Reminder** → `useLayoutEffect` positions the panel from **`getBoundingClientRect`**; **`createPortal`** renders above table scroll clipping.
2. Preset or custom minutes → existing **`onQuickFollowUp(row, minutes)`** path.
3. Custom path validates **`/^\d+$/`** and range **1–1440** before save.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `minutes` | `number` (integer) | Yes | **1–1440**; presets or custom input. |

### Database
- **Engine:** Browser `localStorage` (unchanged)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A (reuses FEATURE-265 handlers)
- **Actions/Selectors added:** None
- **Persisted:** Yes (unchanged)

### i18n Keys Added
`timetableQuickRemindOpen`, `timetableQuickRemindOpenAria`, `timetableQuickRemindPanelTitle`, `timetableQuickRemindPanelHint`, `timetableQuickRemindPresetsSection`, `timetableQuickRemindCustomSection`, `timetableQuickRemindNMin`, `timetableQuickRemindMinutesPh`, `timetableQuickRemindApply`, `timetableQuickRemindMaxHint`, `timetableQuickRemindInvalid`; updates to **`timetableQuickRemindLegend`**, **`timetableQueueToolbarHint`** — **de / en / fr** + **es, it, pt, tr, ru, hi, ar, zh, ja**.

### Dependencies Added
None

### Notes / Known Limitations
Same as FEATURE-265 for **Notification** timing in background tabs.

---

## FEATURE-267: Purchase timetable follow-up — poll-based desktop reminders
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
**Desktop follow-up alerts** no longer rely on a single `setTimeout` (unreliable when the tab is in the background). A global **`TimetableFollowUpDueWatcher`** now **polls `loadTimetableDb()` every 5 seconds** (and on **tab visibility**) and calls **`processDueTimetableFollowUpReminders`**, which fires **`Notification`** when `follow_up_at` is due, with **sessionStorage deduplication** per `(row id, follow_up_at)` and a **6-hour lateness cap** to avoid stale spam. **`scheduleTimetableDesktopReminder`** is retained as a no-op for compatibility.

### Where It Was Added
- `frontend/src/pages/timetable/timetableReminderNotify.ts` — polling delivery + notified keys
- `frontend/src/components/TimetableFollowUpDueWatcher.tsx` — interval + `visibilitychange`
- `frontend/src/App.tsx` — mount watcher for authenticated shell
- `frontend/src/pages/timetable/TimetablePage.tsx` — removed timer-based schedule call; toast copy uses updated **`timetableQuickRemindToastNotifOn`**
- `frontend/src/contexts/LanguageContext.tsx` — updated **`timetableQuickRemindToastNotifOn`** (de, en, fr + overrides)

### What It Does (Technical)
1. Every **5s**, watcher loads timetable DB from **localStorage**, scans rows with **`follow_up_at`**, **`!is_completed`**, **`now >= due`**, **`now - due <= 6h`**, and not yet in **`dema-tt-followup-notified-v1`** → **`new Notification`** → persist key.
2. When the user returns to the tab, an **immediate** scan runs.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `dema-tt-followup-notified-v1` | `string[]` (sessionStorage) | No | Dedup keys `id|iso`. |

### Database
- **Engine:** Browser `localStorage` + **sessionStorage** (dedup only)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts` (read-only via `loadTimetableDb`)
- **Actions/Selectors added:** None
- **Persisted:** sessionStorage for notified keys only

### i18n Keys Added
None (updated **`timetableQuickRemindToastNotifOn`** copy only).

### Dependencies Added
None

### Notes / Known Limitations
Alerts still require the **browser session** (app open) and **notification permission**. Very old due times (>6h) do not auto-popup after reopen.

---

## FEATURE-268: Dashboard strip for purchase timetable follow-ups (due / soon)
**Date:** 2026-04-15  
**Author/Agent:** Cursor AI  
**Status:** Added  

### What Was Added
The **dynamic dashboard** now shows an **amber summary card** listing **your** purchase-timetable rows with a **follow-up time** that is **overdue** or **due within the next 24 hours** (same buyer-code rules as the calendar; widened in FEATURE-269). Users can **open the purchase calendar** (`#/purchase/kalender`) or tap **Call** when a phone number is present. **Desktop notifications** from FEATURE-267 remain unchanged.

### Where It Was Added
- `frontend/src/components/TimetableDashboardReminders.tsx` — new card: poll `loadTimetableDb` every 5s + on visibility; filter mine + `follow_up_at` + `!is_completed`.
- `frontend/src/pages/DynamicDashboard.tsx` — mount the strip at the top of `.dema-canvas-content`.
- `frontend/src/store/timetableStore.ts` — **`viewerBuyerCodeFromSessionName`** (shared buyer code from session name).
- `frontend/src/pages/timetable/TimetablePage.tsx` — uses **`viewerBuyerCodeFromSessionName`** instead of a local duplicate.
- `frontend/src/contexts/LanguageContext.tsx` — keys **`dashboardFollowUpTitle`**, **`Hint`**, **`OpenKalender`**, **`BadgeDue`**, **`BadgeSoon`**, **`Call`** in **de / en / fr** and **LANGUAGE_OVERRIDES** (es, it, pt, tr, ru, hi, ar, zh, ja).

### What It Does (Technical)
1. On mount, **`refresh`** reads **`loadTimetableDb()`**, keeps entries where **`timetableRowIsMine(e, viewerCode)`** and **`follow_up_at`** set and **`!is_completed`**.
2. Splits into **overdue** (`due <= now`) and **upcoming** (`due <= now + 24h`), sorts by due time, renders one list (overdue first).
3. **`viewerBuyerCodeFromSessionName(user?.name)`** matches timetable “mine” semantics.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `follow_up_at` | `string` (ISO) | Yes | Shown rows must have this set. |
| `viewerCode` | `string` | Yes | Derived from `user.name` for mine filter. |

### Database
- **Engine:** Browser `localStorage` (timetable DB)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** In-memory filter over `TimetableDbState.entries`

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts` — **`viewerBuyerCodeFromSessionName`** export only (read path unchanged).
- **Actions/Selectors added:** **`viewerBuyerCodeFromSessionName`**
- **Persisted:** No new persistence (reads existing timetable store)

### i18n Keys Added
`dashboardFollowUpTitle`, `dashboardFollowUpHint`, `dashboardFollowUpOpenKalender`, `dashboardFollowUpBadgeDue`, `dashboardFollowUpBadgeSoon`, `dashboardFollowUpCall` — **de / en / fr** + **es, it, pt, tr, ru, hi, ar, zh, ja**.

### Dependencies Added
None

### Notes / Known Limitations
Only shows while the **dashboard** route is mounted; does not replace desktop notifications. Upcoming window **24 hours** (FEATURE-269).

---

## FEATURE-269: Purchase follow-up — in-app due alerts + robust local datetime parse
**Date:** 2026-04-15
**Author/Agent:** Cursor AI
**Status:** Extended

### What Was Added
**Follow-up delivery** no longer depends solely on **`Notification.permission === 'granted'`**. The same poll now raises **fixed in-app cards** (title + body + open calendar) for due rows, using the **same sessionStorage dedupe** as desktop. **Desktop** notifications still fire when permission is granted and the due time is within **6 hours** of “late after reopen”. **In-app** accepts follow-ups up to **48 hours** late after reopen. **`parseTimetableFollowUpDueMs`** parses wall-clock `YYYY-MM-DDTHH:mm:ss` as **local** time to avoid `Date.parse` ambiguity. The dashboard strip uses the same parser and a **24-hour** “soon” horizon. **Extended:** under **`data-dema-theme="dark"`**, global CSS remaps `text-slate-*` and `bg-amber-50` for navy surfaces, which made reminder title/body almost invisible on amber toasts. **`dema-paper-surface`** and related classes in **`index.css`** force a consistent light “paper” panel (and explicit title/body colours) on the **floating follow-up toast** and the **dashboard follow-up strip**.

### Where It Was Added
- `frontend/src/pages/timetable/timetableReminderNotify.ts` — `parseTimetableFollowUpDueMs`, optional **`onInAppDue`**, **`MAX_IN_APP_LATE_MS`**, relaxed entry condition for processing when notifications are off.
- `frontend/src/components/TimetableFollowUpDueWatcher.tsx` — stacked in-app alert UI + wiring to **`onInAppDue`**; **`dema-paper-surface`** layout classes.
- `frontend/src/components/TimetableDashboardReminders.tsx` — **`parseTimetableFollowUpDueMs`**, **`UPCOMING_HOURS = 24`**; **`dema-paper-surface`** layout classes.
- `frontend/src/index.css` — **`.dema-paper-surface`** (+ icon, title, body, list, badges, call link) for readable contrast in dark theme.

### What It Does (Technical)
1. Each poll: for each eligible row, if not deduped → optionally **`new Notification`** (within 6h late) → always **`onInAppDue`** when callback provided → persist dedupe key if anything delivered.
2. **`parseTimetableFollowUpDueMs`**: manual `Date(y, m-1, d, h, mi, s)` when string has no `Z`/offset.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `dema-tt-followup-notified-v1` | `string[]` (sessionStorage) | No | Same dedupe keys `id|iso` for desktop + in-app. |

### Database
- **Engine:** Browser `localStorage` + **sessionStorage**
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** sessionStorage (unchanged key)

### i18n Keys Added
None (reuses **`timetableNotifReminderTitle`**, **`timetableNotifReminderBody`**, **`dashboardFollowUpOpenKalender`**, **`commonClose`**).

### Dependencies Added
None

### Notes / Known Limitations
Dedupe is **per browser tab session** (sessionStorage). Rows older than **48h** past due do not trigger in-app or desktop. Reminder surfaces intentionally **opt out** of global dark-theme slate/amber remaps so copy stays legible.

---

## FEATURE-270: Purchase timetable reminder — notification permission gate before scheduling
**Date:** 2026-04-15  
**Author/Agent:** Cursor AI  
**Status:** Added  

### What Was Added
Opening the **Reminder** popover now runs a **notification readiness check** (`getTimetableNotificationReadiness`). **Preset and custom duration controls stay disabled** until the user either **allows browser notifications** (calls **`ensureTimetableNotificationPermission`**) or explicitly chooses **Continue without desktop alerts** / (when blocked) **confirms via checkbox** that desktop alerts are unavailable. Copy explains scope (purchase follow-up only), persistence of follow-up without permission, and in-app fallback. **Saving a follow-up** no longer auto-triggers `requestPermission` on every save; the toast uses **`timetableQuickRemindToastNotifOn`** vs **`timetableQuickRemindToastNotifOff`** based on current **`Notification.permission`**. The dialog uses **`dema-timetable-reminder-dialog`** for readable text in dark theme.

### Where It Was Added
- `frontend/src/pages/timetable/timetableReminderNotify.ts` — **`getTimetableNotificationReadiness`**, **`TimetableNotificationReadiness`** export.
- `frontend/src/pages/timetable/components/TimetableFollowUpReminderCell.tsx` — permission compliance block, gated presets/custom, **`onAllowNotifications`**.
- `frontend/src/pages/timetable/TimetablePage.tsx` — **`applyQuickFollowUp`** sync permission read + **`timetableQuickRemindToastNotifOff`** line; removed post-save **`ensureTimetableNotificationPermission`** call.
- `frontend/src/contexts/LanguageContext.tsx` — **`timetableRemindPerm*`** keys, **`timetableQuickRemindToastNotifOff`** in **de / en / fr** and **LANGUAGE_OVERRIDES** (es, it, pt, tr, ru, hi, ar, zh, ja).
- `frontend/src/index.css` — **`.dema-timetable-reminder-dialog`** dark-theme text overrides.

### What It Does (Technical)
1. On panel open: `setNotifReadiness(getTimetableNotificationReadiness())`, reset **`noDesktopPath`**.
2. **`canSchedule`** = `granted` | `unsupported` | `noDesktopPath` (after Continue or denied checkbox).
3. **`onAllowNotifications`** → `ensureTimetableNotificationPermission()` → re-read readiness.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `Notification.permission` | browser API | Yes | Drives readiness and CTA visibility. |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** **`getTimetableNotificationReadiness`**
- **Persisted:** No

### i18n Keys Added
`timetableRemindPermTitle`, `BodyDefault`, `BodyDenied`, `BodyUnsupported`, `BodyGranted`, `Allow`, `ContinueWithout`, `DeniedAckLabel`, `LockedHint`, `AllowBusy`; **`timetableQuickRemindToastNotifOff`** — **de / en / fr** + **es, it, pt, tr, ru, hi, ar, zh, ja**.

### Dependencies Added
None

### Notes / Known Limitations
If the user dismisses the browser prompt without choosing, they remain on **`default`** until they retry **Allow** or pick **Continue without desktop alerts**.

---

## FEATURE-271: Follow-up alerts — “Go to customer” deep-link opens timetable contact drawer
**Date:** 2026-04-15  
**Author/Agent:** Cursor AI  
**Status:** Added  

### What Was Added
In-app follow-up toasts and the dashboard follow-up strip no longer use a generic **Open calendar** action for opening the purchase workspace. They use **Go to customer** (`timetableFollowUpGoToCustomer`) and navigate to **`#/purchase/kalender?ttEntry=<rowId>`**. **`TimetablePage`** reads the query on load / **`hashchange`**, switches **`selectedDate`** to that row’s appointment day, opens **`TimetableContactDrawer`** via **`setContactDrawerEntryId`**, then strips the query with **`history.replaceState`** so refresh does not re-trigger.

### Where It Was Added
- `frontend/src/pages/timetable/timetableContactDeepLink.ts` — **`TT_HASH_ENTRY_PARAM`**, **`timetableHashOpenContact`**, parse/strip helpers.
- `frontend/src/pages/timetable/TimetablePage.tsx` — hash listener effect.
- `frontend/src/components/TimetableFollowUpDueWatcher.tsx` — alert payload includes **`entryId`**; primary CTA uses deep link + **`UserRound`** icon.
- `frontend/src/components/TimetableDashboardReminders.tsx` — per-row **Go to customer** button; removed header-only calendar button.
- `frontend/src/contexts/LanguageContext.tsx` — **`timetableFollowUpGoToCustomer`** in **de / en / fr** + **LANGUAGE_OVERRIDES**.

### What It Does (Technical)
1. `timetableHashOpenContact(id)` builds hash with **`ttEntry`** query.
2. `parseTimetableEntryIdFromLocationHash` → find entry → `setSelectedDate` + `setContactDrawerEntryId` → **`stripTimetableEntryFromLocationHash`**.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `ttEntry` | number (query) | Yes | Timetable row `id`. |

### Database
- **Engine:** N/A (URL only)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** N/A
- **Persisted:** No

### i18n Keys Added
**`timetableFollowUpGoToCustomer`** — **de / en / fr** + **es, it, pt, tr, ru, hi, ar, zh, ja**.

### Dependencies Added
None

### Notes / Known Limitations
If the row id is missing from **`localStorage`** (cleared data), the hash query is stripped and the drawer does not open.

---

## FEATURE-272: Follow-up in-app alert — manual dismiss only + faster due polling
**Date:** 2026-04-15  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
Callback reminder cards in **`TimetableFollowUpDueWatcher`** no longer disappear after a fixed timeout. They stay on screen until the user clicks the **close (X)** control. **Go to customer** navigates to the timetable contact deep link but does **not** remove the card. The watcher polls **`localStorage`** every **1 second** (was 5s) while the tab is active so due times surface within about one second instead of up to five.

### Where It Was Added
- `frontend/src/components/TimetableFollowUpDueWatcher.tsx` — removed **`FollowUpAlertCard`** auto-dismiss **`setTimeout(20000)`**; **`POLL_MS`** **5000 → 1000**; **`goToCustomer`** no longer calls **`onDismissKey`**; file header comment updated.

### What It Does (Technical)
1. **`tick()`** runs on interval **`POLL_MS`** and on **`visibilitychange`** when visible.
2. **`processDueTimetableFollowUpReminders`** returns due keys; new alerts merge into **`inAppAlerts`** state.
3. **`FollowUpAlertCard`** renders until **`dismissInAppKey`** removes that **`item.key`** from state (X button only).
4. **`goToCustomer`** sets **`location.hash`** only.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | No new persisted fields; same **`InAppAlert`** shape as FEATURE-271. |

### Database
- **Engine:** Browser **`localStorage`** (timetable rows unchanged)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** N/A
- **Persisted:** No (in-app stack is React state only)

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Polling every 1s is negligible for **`localStorage`** reads but runs only while the component is mounted and the document is visible (existing visibility handling). Multiple tabs each run their own interval.

---

## FEATURE-273: Dashboard purchase follow-ups — always show card with “nothing due” copy
**Date:** 2026-04-15  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The dashboard **`TimetableDashboardReminders`** block no longer disappears when the user has no overdue or “soon” (24h) follow-ups. The same card stays visible; the subtitle switches to **`dashboardFollowUpEmpty`** explaining there is nothing due in the next 24 hours, and the list is omitted until items exist.

### Where It Was Added
- `frontend/src/components/TimetableDashboardReminders.tsx` — removed **`if (total === 0) return null`**; conditional hint vs empty copy; list wrapped in **`total > 0`**.
- `frontend/src/contexts/LanguageContext.tsx` — **`dashboardFollowUpEmpty`** in **MESSAGES.de / en / fr** and **LANGUAGE_OVERRIDES** (**es, it, pt, tr, ru, hi, ar, zh, ja**).

### What It Does (Technical)
1. **`refresh()`** still partitions **`mine`** rows into **`overdue`** / **`upcoming`** by **`parseTimetableFollowUpDueMs`** and **`UPCOMING_HOURS`**.
2. Section always renders; **`total === 0`** shows **`t('dashboardFollowUpEmpty', …)`** instead of **`dashboardFollowUpHint`**.
3. **`ul`** renders only when **`total > 0`**.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Same **`TimetableEntry`** filter rules as before. |

### Database
- **Engine:** Browser **`localStorage`** via **`loadTimetableDb`**
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** N/A
- **Persisted:** No

### i18n Keys Added
**`dashboardFollowUpEmpty`** — **de / en / fr** + **es, it, pt, tr, ru, hi, ar, zh, ja**.

### Dependencies Added
None

### Notes / Known Limitations
“Nothing due” refers only to **my** rows with **`follow_up_at`** in the overdue or 24h-upcoming window; rows beyond 24h or without **`follow_up_at`** do not change this message.

---

## FEATURE-274: “Go to customer” label — drop calendar parenthetical
**Date:** 2026-04-15  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
The **`timetableFollowUpGoToCustomer`** string no longer appends “(Kalender)” / “(calendrier)” / equivalent in any locale, so the toast and dashboard CTA read e.g. **“Zum Kunden”** instead of **“Zum Kunden (Kalender)”**. Behavior (deep link to purchase timetable contact) is unchanged.

### Where It Was Added
- `frontend/src/contexts/LanguageContext.tsx` — **`timetableFollowUpGoToCustomer`** value updates in **MESSAGES.de / fr** and **LANGUAGE_OVERRIDES** (**es, it, pt, tr, ru, hi, ar, zh, ja**). **MESSAGES.en** was already **“Go to customer”**.

### What It Does (Technical)
1. **`t('timetableFollowUpGoToCustomer', 'Go to customer')`** in **`TimetableFollowUpDueWatcher`** and **`TimetableDashboardReminders`** resolves to shorter copy per language.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Same **`t()`** key, new display strings only. |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** N/A
- **Persisted:** No

### i18n Keys Added
None (modified **`timetableFollowUpGoToCustomer`** copy only).

### Dependencies Added
None

### Notes / Known Limitations
None

---

## FEATURE-275: Kunden field brief PDF — hide empty kontakte slots (dial code only)
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
**Außendienst-PDF** contact rows no longer list **`kontakte`** cards that only carry a **default country dial code** (e.g. **+49**) with an **empty subscriber number**. Previously `joinTelCodeAndNumber` returned the code alone when `telefon` / `handy` were blank, so `formatKontaktCard` produced **“Telefonnummer: +49”** lines and **`buildPeopleRows`** treated the card as non-empty → **Kontakt 2…6** noise. The join helper now returns **`""`** when the number part is empty.

### Where It Was Added
- `frontend/src/features/customers/utils/customerFieldBriefPrint.ts` — `joinTelCodeAndNumber` (`if (!n) return ""`).

### What It Does (Technical)
1. `formatKontaktPhoneLines` → `tel` / `mob` are empty unless there is trimmed text in **`telefon`** / **`handy`** (or code-only path removed).
2. `formatKontaktCard` often becomes **empty** for placeholder cards → **`buildPeopleRows`** skips them (existing **`!block.trim()`** guard).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `telefon` / `handy` | `string` | No | Empty + code-only no longer emits display lines in the PDF. |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
If a product rule should **show** “preferred dial code” without a number, that line will no longer appear in this PDF (only in this join path used for field brief).

---

## FEATURE-276: Kunden PDF button label — “Data druken” (de) + i18n refresh
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The customer modal **PDF / field handout** action label **`customersFieldBriefPrintBtn`** is renamed to **`Data druken`** in **German** (exact product wording requested). **English** and **French** primaries use natural equivalents (**Print data** / **Imprimer les données**). All **nine** `LANGUAGE_OVERRIDES` locales were updated with idiomatic “print data” style labels (no “PDF / …” prefix). **`customersFieldBriefPrintAria`** was aligned in **de / en / fr**; **`CustomersPage`** `t()` English fallbacks for the button and aria were updated.

### Where It Was Added
- `frontend/src/contexts/LanguageContext.tsx` — `customersFieldBriefPrintBtn`, `customersFieldBriefPrintAria` in **MESSAGES.de / en / fr** and **LANGUAGE_OVERRIDES** (es, it, pt, tr, ru, hi, ar, zh, ja).
- `frontend/src/pages/CustomersPage.tsx` — `t("customersFieldBriefPrintBtn", …)` and `t("customersFieldBriefPrintAria", …)` English fallbacks.

### What It Does (Technical)
1. Visible button text resolves from **`MESSAGES[locale]`** or overrides as before; German default shows **Data druken**.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Copy-only change. |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None (values updated for **`customersFieldBriefPrintBtn`**, **`customersFieldBriefPrintAria`** only.)

### Dependencies Added
None

### Notes / Known Limitations
Secondary locales’ **`customersFieldBriefPrintAria`** strings were not all rewritten; they still describe downloading a field summary where translators had not mirrored the new DE/EN/FR aria phrasing.

---

## FEATURE-277: Kunden field brief PDF — restore print logo on letterhead
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **customer field handout PDF** letterhead again includes the **same raster logo** as the app shell (**`/dema-logo.png`**), **top-right** next to the DE/MA wordmark and tagline. **`fieldBriefLetterheadLogoSrc()`** builds an **absolute** `src` so **html2pdf/html2canvas** still loads the asset in the **blob iframe** fallback.

### Where It Was Added
- `frontend/src/features/customers/utils/customerFieldBriefPrint.ts` — `fieldBriefLetterheadLogoSrc`, flex `.dema-letterhead`, `.dema-brand-col`, `.dema-letterhead-logo-wrap` / `.dema-letterhead-logo`, `<img>` in `buildCustomerFieldBriefHtml`.

### What It Does (Technical)
1. Letterhead is a horizontal flex: left **brand + tagline**, right **logo** (`max-height` / `max-width` constrained for A4).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Static public asset path. |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Asset must exist at **`frontend/public/dema-logo.png`** (or deploy static root). Missing file → broken image in PDF. **Superseded by FEATURE-278:** raster logo removed from PDF letterhead again per product request.

---

## FEATURE-278: Kunden field brief PDF — remove right-hand logo (wordmark only)
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **field handout PDF** letterhead **no longer** shows the **`/dema-logo.png`** image (DEMA wordmark + truck motif on the right). The header is **text-only**: **DE/MA** + **GmbH &amp; Co. KG** + **tagline**, then the existing horizontal rule. **`fieldBriefLetterheadLogoSrc`** and related flex/logo CSS were removed.

### Where It Was Added
- `frontend/src/features/customers/utils/customerFieldBriefPrint.ts` — `buildCustomerFieldBriefHtml` letterhead markup and styles.

### What It Does (Technical)
1. `.dema-letterhead` is **`display: block`** again; no `<img>`, no absolute logo URL.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Layout-only change. |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
The **in-app sidebar** still uses **`/dema-logo.png`**; only the **PDF export** letterhead omits it.

---

## FEATURE-279: Kunden “Data druken” button — printer icon (not download)
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The customer edit header **field-brief PDF** action (**`customersFieldBriefPrintBtn`**) now shows Lucide **`Printer`** instead of **`FileDown`**, matching “print data” semantics while behavior remains **browser PDF download**.

### Where It Was Added
- `frontend/src/pages/CustomersPage.tsx` — `lucide-react` import; `editHeaderDocumentsAction` button icon.

### What It Does (Technical)
1. Same `onClick` → **`handleDownloadCustomerFieldBriefPdf`**; only the leading icon component changed.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Visual only. |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
The control still generates a **PDF file download**, not `window.print()`; tooltip/aria copy unchanged in this entry.

---

## FEATURE-280: Settings Typography — dark mode surface fix
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
**Display → Typografie** sub-panels no longer use a **light-only gradient** (`from-slate-50/90 to-white`) that **skipped** the global dark-theme surface remaps in `index.css`, which caused **light grey cards** with **light-on-light** slate text and **inverted** chip contrast. Panels now use **`bg-slate-50/70`**, which is remapped in dark mode like the rest of Settings. **Selected font tile** `bg-blue-50/60` is included in the dark **`bg-blue-50/*`** remap group so the active state is not a bright paper highlight.

### Where It Was Added
- `frontend/src/pages/SettingsPage.tsx` — font family and text size section wrappers: `bg-slate-50/70` instead of gradient.
- `frontend/src/index.css` — dark theme: add `.bg-blue-50\/60` next to existing `.bg-blue-50` interactive remaps.

### What It Does (Technical)
1. Typography outer cards use a **solid utility** (`bg-slate-50/70`) that matches existing `html[data-dema-theme="dark"] .bg-slate-50\/70` rules → **dark glass** background.
2. Global `text-slate-*` remaps then align with the **dark** surface (readable light grey body copy).
3. `bg-blue-50/60` on the selected typeface tile now receives the same **tinted navy** treatment as `bg-blue-50` / `bg-blue-50/70`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Visual/CSS only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Other pages still use **`to-white` gradients**; only Settings Typografie was changed in this pass. A broader gradient token strategy could be a follow-up.

---

## FEATURE-281: Timetable contact drawer — remove footer Zuständige Person
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **Kontakt** drawer footer no longer shows the **“Zuständige Person”** label and **assignee `<select>`** (demo names / “nicht zugeordnet”). The strip still shows **unsaved changes** when dirty and **Verwerfen** / **Kontakt speichern**, right-aligned.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — removed `ZUSTAENDIGE_OPTIONS` and the footer left column; outer footer row uses `justify-end` for actions only.

### What It Does (Technical)
1. Footer layout: single **`flex flex-wrap justify-end gap-2`** group for discard + save.
2. **`contact_profile.zustaendige_person`** is no longer edited from this drawer (field may still exist on persisted types / data from other flows).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | UI removal only; `onPersist` payload unchanged except field is not updated here. |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No change to persist shape; assignee not set from this UI anymore.

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
If product later needs assignee on timetable rows, reintroduce via a dedicated control or sync from **Kunden** master data.

---

## FEATURE-282: Timetable queue — remove toolbar hint paragraph
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
Removed the long **queue toolbar hint** (“Zeile anklicken für den Kontaktbereich…”) above the timetable table and deleted the **`timetableQueueToolbarHint`** i18n key from **de / en / fr** and all **LANGUAGE_OVERRIDES** locales so no orphan key remains.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — removed the `<p>` that called `t('timetableQueueToolbarHint', …)`; queue badges row simplified to a single flex wrapper.
- `frontend/src/contexts/LanguageContext.tsx` — removed `timetableQueueToolbarHint` from every bundle.

### What It Does (Technical)
1. UI: only **“Ihre Liste”** (daily queue) and **row count** chip remain in that toolbar row.
2. i18n: key removed everywhere; no remaining `t('timetableQueueToolbarHint')` references.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Copy/UI only. |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None (key **removed**: `timetableQueueToolbarHint`)

### Dependencies Added
None

### Notes / Known Limitations
Follow-up / reminder behaviour is unchanged; only the instructional paragraph was removed.

---

## FEATURE-283: Timetable quick filters — labels aligned with Ergebnis
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
**Schnellfilter → Zeilenfilter** chip labels now match the **Ergebnis** column vocabulary where filters map to outcomes (e.g. **LKW verfügbar** instead of “Angebote”), clarify **Wiedervorlage** vs. outcome (**Mit Wiedervorlage**), tighten **Erledigt** wording, add proper **DE** string for **other-buyer date range** (previously English fallback), and **reorder** chips: Alle → pipeline → trucks → follow-up date → closed → buyer-specific → geparkt.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — `filterChipItems` order and `t()` English fallbacks.
- `frontend/src/contexts/LanguageContext.tsx` — `timetableFilter*` strings in **de / en / fr**; new **`timetableFilterOtherBuyerRange`** in primaries; matching overrides in **es, it, pt, tr, ru, hi, ar, zh, ja**.

### What It Does (Technical)
1. Filter logic (`filterMode` predicates) is **unchanged**; only **display labels** and chip **order** changed.
2. **`timetableFilterOtherBuyerRange`** is now a first-class key in all three primaries (no orphan `t(..., 'Other buyers (range)')` only).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | i18n + UI order only. |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
**`timetableFilterOtherBuyerRange`** (de/en/fr + 9 overrides). Updated: **`timetableFilterUnfinished`**, **`timetableFilterFollowUp`**, **`timetableFilterOffers`**, **`timetableFilterCompleted`**, **`timetableFilterDoneNoFollowUp`**, **`timetableFilterUnfinishedOther`** across primaries and listed overrides.

### Dependencies Added
None

### Notes / Known Limitations
**`timetableFilterOpen`** / hero stats strings unchanged; only quick-filter row used the `timetableFilterUnfinished` key.

---

## FEATURE-284: Follow-up due watcher — only current buyer’s rows (fix “random” alerts)
**Date:** 2026-04-15  
**Author/Agent:** Cursor AI  
**Status:** Modified  

### What Was Added
**`TimetableFollowUpDueWatcher`** previously passed **all** timetable **`db.entries`** into **`processDueTimetableFollowUpReminders`**, so any due **`follow_up_at`** on **another buyer’s** rows could fire toasts and desktop notifications for the signed-in user. It now filters with **`timetableRowIsMine(row, viewerCode)`** using **`viewerBuyerCodeFromSessionName(user?.name)`**, matching **`TimetableDashboardReminders`**. In-app alerts clear when **`viewerCode`** changes (account switch).

### Where It Was Added
- `frontend/src/components/TimetableFollowUpDueWatcher.tsx` — **`useAuth`**, **`useMemo`** for **`viewerCode`**, **`entries.filter(timetableRowIsMine)`**, effect dependency **`[viewerCode]`**, **`setAlerts([])`** on viewer change.

### What It Does (Technical)
1. **`run()`** loads DB → **`mine = entries.filter(e => timetableRowIsMine(e, viewerCode))`** → **`processDueTimetableFollowUpReminders(mine, …)`**.
2. **`useEffect`** depends on **`viewerCode`**; on change, clears **`alerts`** and restarts the interval.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `user.name` | `string \| undefined` | No | Drives **`viewerCode`** (same heuristic as timetable quick-create). |

### Database
- **Engine:** Browser **`localStorage`** (unchanged)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** N/A
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Rows with **`buyer_name === 'DEMO'`** remain **“mine”** for every user (**`timetableRowIsMine`**); demo seed data can still surface multiple names. **`sessionStorage`** dedupe keys are not cleared on logout (existing behaviour).

---

## FEATURE-285: Purchase Kalender — chrono lattice page background
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Added 

### What Was Added
The **Purchase call timetable / Kalender** route now uses a **dedicated ambient background** that feels **precision-scheduling oriented**: soft slate–mint base, **slowly drifting horizontal “slot” bands**, a **very slow conic wash** (emerald / violet / teal, blurred — not the Customers sapphire orb stack), **vertical column rhythm** that creeps horizontally, a **subtle diagonal trace** pulse, film **noise**, and edge **vignette**. It replaces the previous flat gradient + dot grid + two blob layers so the calendar page reads as its own **industry-grade** surface while staying calm behind the dark header and white cards.

### Where It Was Added
- `frontend/src/pages/timetable/TimetablePage.tsx` — root uses **`dema-canvas-root`** + **`timetable-page-chrono`**; new **`timetable-chrono-ambient`** stack; page chrome wrapped in **`dema-canvas-content`** (toast and modals stay in the same stacking context).
- `frontend/src/index.css` — **`timetable-chrono-*`** layer styles, **`@keyframes`** for bands / cyclic / rhythm / trace, **`prefers-reduced-motion`** overrides for those layers.

### What It Does (Technical)
1. **`timetable-page-chrono`** sits on **`dema-canvas-root`** (`position: relative`, `isolation: isolate`) like Customers, but **does not reuse** **`dema-canvas-base`** orbs/shine/grid.
2. **`timetable-chrono-base`** paints stacked **radials + linear** (mint floor, violet corner, teal corner) with **dark theme** variants keyed off **`html[data-dema-theme="dark"]`**.
3. **`timetable-chrono-bands`** uses **`repeating-linear-gradient`** (180deg) + **`timetable-chrono-bands-drift`** (`translateY` by one band period) under a vertical **mask** so edges fade.
4. **`timetable-chrono-cyclic-shell`** centers **`timetable-chrono-cyclic`**: **`conic-gradient`** + blur, **`timetable-chrono-cyclic-spin`** (~400s rotation).
5. **`timetable-chrono-rhythm`** repeats **72px** vertical hairlines, **`background-position`** animation **`timetable-chrono-rhythm-shift`**, radial **mask** for focus.
6. **`timetable-chrono-trace`** mimics a **single soft audit beam** via **`background-position`** on oversized **`linear-gradient`** (**`timetable-chrono-trace-slide`**); dark theme tints green/violet instead of white.
7. **`timetable-chrono-noise`** / **`timetable-chrono-vignette`** match the dashboard noise/vignette pattern at lower strength.
8. **`prefers-reduced-motion: reduce`** disables motion on bands, cyclic, rhythm, trace and **fixes trace** `background-position` / opacity.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Pure presentation; no props or API. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** N/A
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Motion is **CSS-only** (no canvas). **`timetable-chrono-cyclic`** uses **`will-change: transform`** for the long spin; if profiling shows cost on low-end GPUs, reduce blur radius or spin duration. Header card still uses its **own** internal blob accents (unchanged).

---

## FEATURE-286: Dark theme — missing Tailwind surface remaps (slate / white / neutral)
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
**Black (dark) theme** had **light gray “patches”** where components used Tailwind classes that were **not covered** by the global `html[data-dema-theme="dark"]` overrides — notably **`bg-slate-50/60`** on customer modal **address** and **contact** card headers (and the same pattern in timetable drawer, date picker footers, assignee picker, etc.). This change **extends the central dark-theme CSS** so those utilities map to the same **navy glass** surfaces as the rest of the dashboard, and adds **fractional `bg-white/*`**, **`bg-neutral-*`**, **`border-neutral-*`**, extra **hover** utilities, **`focus:bg-white`**, and **`open:bg-white/70`** so **chat, timetable, customers**, and **modals** stay visually consistent.

### Where It Was Added
- `frontend/src/index.css` — expanded **Surface hierarchy** and **Interactive states** under `html[data-dema-theme="dark"]` (no component file churn).

### What It Does (Technical)
1. **`bg-slate-50/60`**, **`/30`**, **`/40`**, **`/90`**, **`/95`**, and **`bg-slate-100/80`** now receive the same dark **panel** background as existing **`bg-slate-50`** remaps.
2. **`bg-white/40` … `bg-white/92`** (glass panels) map to a dark translucent **slate** fill with a subtle inset highlight so timetable/chat sidebars and cards no longer read as **paper white**.
3. **`bg-neutral-50`**, **`bg-neutral-100`**, **`bg-neutral-200`**, **`border-neutral-200`**, **`border-neutral-300`**, and **`hover:bg-neutral-50`** align neutral “chrome” with the dark palette; **`bg-neutral-100`** stays slightly **elevated** vs **`bg-neutral-50`** for selected controls.
4. **`hover:bg-slate-50/90`**, **`hover:bg-slate-100/80`**, **`focus:bg-white`**, and **`open:bg-white/70`** avoid **hover/focus flashes** to light gray or white in dark mode.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | CSS-only; `data-dema-theme="dark"` on `html` drives selectors. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** N/A
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
**Arbitrary Tailwind variants** (e.g. `[&::-webkit-scrollbar-track]:bg-slate-100/80`) are **not** matched by these class-based rules; if a scrollbar track stays light, fix that **arbitrary** class in a follow-up. **`bg-white/10`** on **colored** headers is intentionally unchanged (meant as highlight on dark bars).

---

## FEATURE-287: Dark theme — teal tints, slate-100 opacities, paper gradients, ring offset
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
Follow-up dark polish for surfaces that still read **light gray** because they used **Tailwind gradient stops** (`from-white` / `via-white` / `to-slate-50/*`) or **teal wash** utilities (`bg-teal-50/25` … `/40`, tab strip `bg-teal-50/40`, Angebot body `from-teal-50/25 to-slate-50/40`) that **do not** inherit the flat `bg-white` / `bg-slate-50` remaps. Also covers **mini-calendar** weekday strip gradient, **Bestand** drawer header `from-slate-50 to-white`, **slate-100** fractional backgrounds, **teal borders**, **hover** on teal borders / `teal-100/80`, and **`ring-offset-white`** so selected day rings sit on a **navy** offset in dark mode.

### Where It Was Added
- `frontend/src/index.css` — extended `html[data-dema-theme="dark"]` rules only.

### What It Does (Technical)
1. Adds **`bg-slate-100/60`**, **`/90`**, **`/95`** to the shared slate panel remap list.
2. Extends **teal** panel remaps with **`/25`–`/70`**, **`hover:bg-teal-100/80`**, **`hover:border-teal-200`**, and **`border-teal-100`**, **`border-teal-200/70`**, **`/80`**.
3. Adds **compound gradient overrides** matching Tailwind’s multi-class gradients (e.g. **`bg-gradient-to-b.from-white.via-white.to-slate-50/95`** for `TimetableMiniCalendar`, **`from-teal-50/25.to-slate-50/40`** for `NewAngebotModal` scroll body, **`from-slate-50.to-white`** for `BestandPage` drawer header, plus slate-only vertical gradients used on timetable shells).
4. **`ring-offset-white`** → **`--tw-ring-offset-color`** navy so focus/selection rings on dark panels do not show a **white halo**.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | CSS-only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** N/A
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Gradients that mix **`from-blue-50` + `via-white`** (marketing cards, timetable offer blocks) are **not** fully enumerated here; add another compound rule if a specific screen still shows a bright band. **`bg-clip-text`** gradients intentionally not remapped.

---

## FEATURE-288: New Angebot modal — price negotiation as third column on Preise tab
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Added 

### What Was Added
On **Angebot (Neu)** → tab **Preise & Extras**, the **Price negotiation** experience from the timetable offer drawer is shown as a **third column** next to **Verknüpfte Datensätze** and **Extras**. Users record rounds from **Preisvorstellung Kunde** (seller asking) and **Preisvorstellung DEMA** (DEMA side) on the **Technik & Termine** tab; rounds are stored on the new offer payload and persisted with the local **Angebote** store row.

### Where It Was Added
- `frontend/src/components/NewAngebotModal.tsx` — `negotiation_rounds` on form state; `formToPayload`; synthetic `TimetableTruckOffer` bridge + `setNegotiationOfferField`; third grid column with `TimetableOfferNegotiationHistory`.
- `frontend/src/pages/timetable/components/TimetableOfferNegotiationHistory.tsx` — optional **`description`** (React node) and **`compactTop`** for reuse outside the drawer.
- `frontend/src/types/angebote.ts` — optional **`negotiation_rounds`** on **`AngebotStamm`** (typed from **`TimetableNegotiationPriceRound`**).
- `frontend/src/contexts/LanguageContext.tsx` — **`angebotNegotiationHint`** in **de / en / fr** and all **`LANGUAGE_OVERRIDES`** locales.

### What It Does (Technical)
1. **`FormState.negotiation_rounds`** defaults to **`[]`**; **`formToPayload`** sends **`negotiation_rounds`** only when non-empty (otherwise **`undefined`**).
2. **`useMemo`** builds **`negotiationBridgeOffer`**: stable offer **`id`** via **`useRef(newTruckOfferId())`**; **`expected_price_eur`** / **`purchase_bid_eur`** from **`parseOptionalEuro`** on **`preisvorstellung_kunde`** / **`preisvorstellung_dema`** (aligned with timetable memory: seller vs DEMA).
3. **`setNegotiationOfferField`** applies only **`negotiation_rounds`** patches back into React form state.
4. **`TimetableOfferNegotiationHistory`** receives **`compactTop`**, **`description={t('angebotNegotiationHint', …)}`**, and shared **`t` / `localeTag`** from **`localeTagForLanguage(language)`**.
5. **`createAngebot`** continues to spread **`payload`** into **`AngebotStamm`**, so rounds persist in memory when present.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `negotiation_rounds` | `TimetableNegotiationPriceRound[]` | No | Timestamped rounds with optional note and author; stored on offer row when saving. |
| `preisvorstellung_kunde` / `preisvorstellung_dema` | `string` | No | Parsed as EUR numbers for **`canRecord`** and for values copied into each new round. |

### Database
- **Engine:** Browser in-memory **`angeboteStore`** (demo persistence)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** `frontend/src/store/angeboteStore.ts` (unchanged; **`createAngebot`** spreads payload)
- **Actions/Selectors added:** None
- **Persisted:** Yes (within **`AngebotStamm`** objects carried in app state / local patterns for Angebote)

### i18n Keys Added
- **`angebotNegotiationHint`** — de, en, fr primaries + es, it, pt, tr, ru, hi, ar, zh, ja overrides

### Dependencies Added
None

### Notes / Known Limitations
Editing an **existing** saved Angebot from a list is **out of scope** for this modal path (still “Neu” reset-on-open); **`negotiation_rounds`** would need hydration when an edit flow exists. Other **`TimetableOfferNegotiationHistory`** keys (title, table columns, button) still rely on **`t()`** fallbacks until added to **`MESSAGES`** if product wants full parity.

---

## FEATURE-289: Timetable contact drawer — offer tab three columns (Preisvergleich)
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **Fahrzeug / Angebot** workspace in the **Kundenkontakt** drawer now uses **three parallel columns** on large screens, aligned with **Angebots-Generator** and **Angebot**: a dedicated **Preisvergleich** column holds **Offer memory** (historical DEMA/seller prices) and **Price negotiation** (recorded rounds). The middle column keeps **Angebot** as the structured vehicle/price form only. A minimal **`TimetableOfferCompactPreview`** module was added so the existing **Pro Test** tab import type-checks.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — offer panel layout: **`lg:basis-1/3`** × three sections; vertical dividers between columns; **`TimetableOfferMemoryPanel`** + **`TimetableOfferNegotiationHistory`** (`compactTop`) moved under **`timetableOfferPriceComparisonTitle`**.
- `frontend/src/contexts/LanguageContext.tsx` — **`timetableOfferPriceComparisonTitle`** in **de / en / fr** and **LANGUAGE_OVERRIDES** (es, it, pt, tr, ru, hi, ar, zh, ja).
- `frontend/src/pages/timetable/components/TimetableOfferCompactPreview.tsx` — lightweight tab panel body (reuses existing **`t()`** keys).

### What It Does (Technical)
1. Parent row remains **`flex-col` / `lg:flex-row`** with **`lg:gap-0`**; each column uses **`lg:pr-6`**, **`lg:px-6`**, **`lg:pl-6`** for gutters beside gradient **`w-px`** separators (same pattern as the former two-column split).
2. **Column 1:** unchanged **`TimetableOfferGeneratorBlock`**.
3. **Column 2:** **`TimetableOfferMinimalBlock`** only (no memory / no negotiation).
4. **Column 3:** section title from **`t('timetableOfferPriceComparisonTitle', …)`**, then **`TimetableOfferMemoryPanel`**, then **`TimetableOfferNegotiationHistory`** with **`compactTop`** to avoid double top margin under the section title.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Layout-only; data paths unchanged (`offer`, `offerMemoryItems`, `setOfferField`, etc.). |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
- **`timetableOfferPriceComparisonTitle`** — de, en, fr + es, it, pt, tr, ru, hi, ar, zh, ja overrides

### Dependencies Added
None

### Notes / Known Limitations
**`TimetableOfferCompactPreview`** is intentionally minimal until a full compact layout ships; the tab remains a **demo** surface. On **`lg`** and below, columns stack with the same order as DOM (Generator → Angebot → Preisvergleich).

---

## FEATURE-290: Offer memory — dedupe “Add vehicle” + jump to matched vehicle
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
**Offer memory** now supports **opening the matched vehicle** without duplicating slots: **Add vehicle** skips creating a new offer when the **currently selected** offer already matches another slot at the **same similarity threshold** as offer memory (score ≥ 45 via **`offerVehicleSameEnough`**). **Open this vehicle** and **Use these prices** on a memory card **select that offer chip** on the same calendar row (scroll strip into view) or **jump the contact drawer** to the other row with optional **price merge**; merged jumps mark the draft dirty when prices are applied from another row.

### Where It Was Added
- `frontend/src/pages/timetable/timetableOfferMemory.ts` — export **`offerVehicleSameEnough`** (shared ≥45 rule with **`collectTimetableOfferMemory`**).
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — **`addVehicleOffer`** dedupe; **`navigateToMemoryOffer`**; **`scrollOfferVehicleStripIntoView`** + **`#tt-offer-vehicle-strip`** wrapper; props **`contactEntryOfferFocus`**, **`onContactEntryOfferFocusConsumed`**, **`onRequestJumpToEntryOffer`**; **`useLayoutEffect`** initial focus + price merge + dirty flag; removed tab-reset **`useEffect`** on **`entry.id`** (tab now set in layout reset).
- `frontend/src/pages/timetable/TimetablePage.tsx` — **`contactDrawerOfferJump`** state, **`handleJumpToEntryOffer`**, **`clearContactDrawerOfferJump`**, wired drawer props; clear jump on table open / hash open / close.
- `frontend/src/pages/timetable/components/TimetableOfferMemoryPanel.tsx` — optional **`onOpenOffer`**, **Open this vehicle** button ( **`stopPropagation`** removed from Apply after article click removed).
- `frontend/src/contexts/LanguageContext.tsx` — **`timetableOfferMemoryOpenVehicle`** in **de / en / fr** and all **LANGUAGE_OVERRIDES** locales.

### What It Does (Technical)
1. **`addVehicleOffer`**: **`ensureOfferIdsAndSelection`** → if **`offerHasVehicleIdentity(current)`** and another offer **`offerVehicleSameEnough`**, set **`selected_offer_id`** to the duplicate instead of appending **`createEmptyTruckOffer`**; else append. Calls **`scrollOfferVehicleStripIntoView`**.
2. **`navigateToMemoryOffer(item, mode)`**: same **`entry.id`** → **`patchDraft`** select **`item.offerId`** and optionally merge seller/DEMA EUR fields when **`mode === 'apply'`**; switch to **offer** tab; scroll strip. Otherwise **`onRequestJumpToEntryOffer`** with **`mergeMemoryPrices`** from mode.
3. **Parent** stores **`contactDrawerOfferJump`**; drawer **`useLayoutEffect`** applies **`contactEntryOfferFocus`** when resetting draft for that entry: select offer, optional merge, **`setDirty(true)`** if merge, consume callback.

### Data It Accepts / Emits
| Field / callback | Type | Required | Description |
|----|---|----|----|
| `contactEntryOfferFocus` | object / null | No | One-shot `{ offerId, workspaceTab?, applyPrices? }` for drawer init. |
| `onRequestJumpToEntryOffer` | function | No | Opens another **`entryId`** with optional price merge payload. |

### Database
- **Engine:** N/A (timetable entries in client store)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts` (unchanged; persist still via existing **`onPersist`**)
- **Actions/Selectors added:** None
- **Persisted:** Only after user **Save contact** as before

### i18n Keys Added
- **`timetableOfferMemoryOpenVehicle`** — de, en, fr + es, it, pt, tr, ru, hi, ar, zh, ja

### Dependencies Added
None

### Notes / Known Limitations
Cross-row jump requires **`TimetablePage`** wiring (implemented). **`TimetableOfferModal`** still omits **`onOpenOffer`** (no vehicle strip there). Dedup compares the **selected** offer only, not “would this new empty slot duplicate” (empty offers rarely score ≥45 against each other).

---

## FEATURE-291: Offer generator — Format preview only; Save applies to form
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
In the timetable contact drawer **Angebots-Generator**, **Formatieren** now only **parses free text** and shows the **structured preview** plus a status hint — it **does not** call **`setOfferField`** / **`setVehicleExtra`** or mark the drawer dirty. **Speichern** remains the sole action that **applies** parsed patches to the minimal offer form, triggers **`onGeneratorApplied`** (dirty), and **clears** the generator textarea.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferGeneratorBlock.tsx` — **`handleFormat`** no longer calls **`applyParsed`** or **`onGeneratorApplied`**; added **`setFormatPreviewMessage`** using new i18n key; renamed internal **`setAppliedMessage`** to **`setSaveAppliedMessage`** for save path; updated default **`timetableOfferGenHint`** / **`timetableOfferGenFormatTitle`** fallbacks in JSX.
- `frontend/src/contexts/LanguageContext.tsx` — revised **`timetableOfferGenHint`** and **`timetableOfferGenFormatTitle`** in **de, en, fr** and all **LANGUAGE_OVERRIDES** (es, it, pt, tr, ru, hi, ar, zh, ja); added **`timetableOfferGenFormatPreviewCount`** (`{count}`) in all twelve bundles.

### What It Does (Technical)
1. User pastes text → **`parseOfferFreeText`** on **Format** → **`setLastParsed`**, **`setFormatPreviewMessage(extractedCount(parsed))`** — no **`setOfferField`** / **`setVehicleExtra`**.
2. User clicks **Save** → **`applyParsed`** from **`lastParsed`** or fresh parse of trimmed text → **`setSaveAppliedMessage`**, **`onGeneratorApplied`** if count > 0, clear text and **`lastParsed`**.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `setOfferField` | function | Yes | Partial patch to offer — **only** from **Save** |
| `setVehicleExtra` | function | Yes | Partial patch to vehicle display extra — **only** from **Save** |
| `onGeneratorApplied` | function | No | Fires on **Save** when at least one field was applied |

### Database
- **Engine:** N/A
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A (drawer draft via parent)
- **Actions/Selectors added:** None
- **Persisted:** Unchanged — user still saves contact to persist

### i18n Keys Added
- **`timetableOfferGenFormatPreviewCount`** — all 12 locales; **`timetableOfferGenHint`** / **`timetableOfferGenFormatTitle`** text updated (not new keys for the latter two)

### Dependencies Added
None

### Notes / Known Limitations
If the user clicks **Save** without **Format**, parsing still runs from the raw textarea (unchanged). Preview lines match the last **Format** or the in-memory parse used on **Save** until the textarea is edited ( **`lastParsed`** cleared on change).

---

## FEATURE-292: New offer modal — mutual-exclusive Gekauft / Verkauft stock ledger
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **Neues Angebot** form replaces two independent **Gekauft** / **Verkauft** checkboxes with a **three-option stock ledger** control: **Gekauft** (inbound purchase — DEMA acquires), **Verkauft** (outbound disposal to another company), and **Not classified** (both flags false). **Gekauft** and **Verkauft** are **mutually exclusive** at the data layer (`gekauft` / `verkauft` on `AngebotStamm`). The dialog header shows a compact **badge** when purchase or third-party sale is selected.

### Where It Was Added
- `frontend/src/components/NewAngebotModal.tsx` — `StockLedgerMode`, `stockLedgerModeFromFlags`, `applyStockLedgerMode`, segmented tile UI (icons `ArrowDownLeft` / `ArrowUpRight` / `CircleDashed`), header badges; removed duplicate checkboxes for `gekauft`/`verkauft`.
- `frontend/src/contexts/LanguageContext.tsx` — keys `angebotStockLedgerLabel`, `angebotStockLedgerHint`, `angebotStockLedgerPurchase`, `angebotStockLedgerPurchaseSub`, `angebotStockLedgerDisposal`, `angebotStockLedgerDisposalSub`, `angebotStockLedgerNeutral`, `angebotStockLedgerNeutralSub` in **de, en, fr** and **LANGUAGE_OVERRIDES** (es, it, pt, tr, ru, hi, ar, zh, ja).

### What It Does (Technical)
1. `stockLedgerMode` is derived from `form.gekauft` and `form.verkauft` (`purchase` if gekauft only, `disposal` if verkauft only, else `neutral`).
2. User selects a tile → `applyStockLedgerMode` sets **`gekauft`** / **`verkauft`** so the pair is never both true for normal operation (ambiguous legacy rows show as **neutral** until the user picks again).
3. **`formToPayload`** unchanged — still sends `gekauft` and `verkauft` to **`NewAngebotPayload`** / **`angeboteStore`**.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `gekauft` | boolean | Yes | True when **Gekauft** (purchase) tile selected |
| `verkauft` | boolean | Yes | True when **Verkauft** (third-party sale) tile selected |

### Database
- **Engine:** Browser `localStorage` (existing angebote demo store)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** `frontend/src/store/angeboteStore.ts` (unchanged)
- **Actions/Selectors added:** None
- **Persisted:** Yes — via existing offer save flow

### i18n Keys Added
`angebotStockLedgerLabel`, `angebotStockLedgerHint`, `angebotStockLedgerPurchase`, `angebotStockLedgerPurchaseSub`, `angebotStockLedgerDisposal`, `angebotStockLedgerDisposalSub`, `angebotStockLedgerNeutral`, `angebotStockLedgerNeutralSub` — **de, en, fr** + overrides **es, it, pt, tr, ru, hi, ar, zh, ja**

### Dependencies Added
None

### Notes / Known Limitations
**Fahrzeug anbieten** and **Abgemeldet** remain separate checkboxes (unchanged). If legacy data had both `gekauft` and `verkauft` true, the UI shows **neutral** until the user selects a lane.

---

## FEATURE-293: Angebote list tab — stock-flow badges, lane filter, row accents
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The **Angebote** (offers) table now **visualises the same stock ledger** as the new-offer form: **pill badges** per row (inbound purchase, outbound third-party sale, unclassified, or **review** when both flags are set), a **five-lane filter** (all / purchase / disposal / neutral / conflict) replacing ambiguous separate **gekauft** / **verkauft** checkboxes, **left accent + soft row tint** for purchase/disposal/conflict, a **footer mix line** with counts per lane, and a **drawer callout** for stock flow. Shared **`angebotLedgerKind`** logic lives in **`frontend/src/lib/angebotLedger.ts`**. Demo seed rows set **`gekauft: true`** on two offers so inbound lanes appear in the list.

### Where It Was Added
- `frontend/src/pages/AngebotePage.tsx` — filter pills, `ledgerMix`, `renderLedgerBadge`, table column, row styling, drawer stock card, i18n `t()` for new copy.
- `frontend/src/lib/angebotLedger.ts` — **`AngebotLedgerKind`**, **`angebotLedgerKind`**.
- `frontend/src/store/angeboteStore.ts` — seed **`gekauft: true`** on offers **A-24001**, **A-24004**.
- `frontend/src/contexts/LanguageContext.tsx` — **`angeboteStockFlow*`** / **`angeboteDrawer*`** / **`angeboteSummaryLedgerMix`** etc. in **de, en, fr** and **LANGUAGE_OVERRIDES**.

### What It Does (Technical)
1. **`angebotLedgerKind`** maps `gekauft`/`verkauft` to `purchase` | `disposal` | `neutral` | `conflict`.
2. **`stockFlowLane`** filter keeps one active lane; **`filtered`** includes only rows whose kind matches (or all).
3. Table **`Verkauft`** column replaced by **stock flow** column rendering **`renderLedgerBadge`**; selected row uses translucent badge styles on dark background.
4. **`ledgerMix`** counts kinds over the **current filtered** list for the footer line.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `gekauft` / `verkauft` | boolean on `AngebotStamm` | Yes | Same as before; UI derives display |

### Database
- **Engine:** Browser `localStorage` (`dema-angebote-db`)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** `frontend/src/store/angeboteStore.ts` (seed only)
- **Actions/Selectors added:** None
- **Persisted:** Unchanged

### i18n Keys Added
`angeboteStockFlowFilterTitle`, `angeboteStockLaneAll`, `angeboteStockLaneConflict`, `angeboteStockLaneConflictHint`, `angeboteStockFlowColHeader`, `angeboteSummaryLedgerMix`, `angeboteFooterRowCount`, `angeboteEmptyFiltered`, `angeboteFilterOfferVehicle`, `angeboteDrawerLabelStockFlow`, `angeboteDrawerStockFlowHint` — **de, en, fr** + **LANGUAGE_OVERRIDES**

### Dependencies Added
None

### Notes / Known Limitations
Other Angebote page strings (column headers, sort labels) remain legacy German until a full page i18n pass. **`focusAngebotFromSearch`** still calls **`showAll()`** which resets **`stockFlowLane`** to **all**.

---

## FEATURE-295: Timetable contact drawer offer — Gekauft/Verkauft ledger + persistence
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
The timetable **contact drawer → Angebot** column (**`TimetableOfferMinimalBlock`**) now includes the same **mutually exclusive stock ledger** as Angebote / **Neues Angebot**: **Gekauft** (inbound purchase), **Verkauft** (outbound to third party), **Unclassified**, placed **beside** seller asking and purchasing bid (three-column row on `sm+`). Flags persist on **`TimetableTruckOffer`** as **`gekauft`** / **`verkauft`**. The full **TimetableOfferModal** and **`mergeTimetableOfferInputIntoEntry`** / **`normalizeOffer`** / **`offerHasContent`** / **`createEmptyTruckOffer`** were updated; **`angebotLedgerKind`** now accepts **`OfferStockFlags`**. **NewAngebotModal** negotiation bridge offer passes **`gekauft`/`verkauft`** into the negotiation preview offer.

### Where It Was Added
- `frontend/src/types/timetable.ts` — **`gekauft`?, `verkauft`?** on **`TimetableTruckOffer`**; optional on **`TimetableOfferInput`**.
- `frontend/src/lib/angebotLedger.ts` — **`OfferStockFlags`**; **`angebotLedgerKind`** parameter widened.
- `frontend/src/pages/timetable/components/TimetableOfferMinimalBlock.tsx` — price row grid + ledger buttons + conflict hint.
- `frontend/src/pages/timetable/TimetableOfferModal.tsx` — **`stock_ledger`** form state, UI strip, save payload.
- `frontend/src/pages/timetable/contactDrawerFormUtils.ts` — defaults, **`offerHasContent`**, **`mergeTimetableOfferInputIntoEntry`**.
- `frontend/src/store/timetableStore.ts` — **`normalizeOffer`** reads flags.
- `frontend/src/components/NewAngebotModal.tsx` — negotiation bridge **`TimetableTruckOffer`** includes ledger flags.

### What It Does (Technical)
1. User selects a ledger button → **`setOfferField({ gekauft, verkauft })`** with exclusive booleans (same as Angebote form).
2. **`angebotLedgerKind`** classifies row for UI; **conflict** shows **`angeboteStockLaneConflictHint`** until resolved.
3. Persisted offers in **`timetableStore`** JSON include **`gekauft`/`verkauft`**; legacy rows without keys normalize to **`false`/`false`**.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `gekauft` | boolean (optional on offer) | No | **true** = inbound purchase lane |
| `verkauft` | boolean (optional on offer) | No | **true** = outbound third-party sale lane |

### Database
- **Engine:** Browser persistence for timetable (existing store)
- **Tables Affected:** N/A
- **Schema Changes:** None (JSON shape extension on offer objects)
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** `frontend/src/store/timetableStore.ts` — **`normalizeOffer`**
- **Actions/Selectors added:** None
- **Persisted:** Yes — with timetable entries

### i18n Keys Added
None (reuses **`angebotStockLedger*`** and **`angeboteStockLaneConflictHint`**)

### Dependencies Added
None

### Notes / Known Limitations
Conflict copy references resolving in the **offer form** generically; text is shared with Angebote. Offer memory **apply prices** does not change ledger flags.

---

## FEATURE-296: New customer modal — mandatory Kunde & Adresse fields + save validation
**Date:** 2026-04-15 
**Author/Agent:** Cursor AI 
**Status:** Modified 

### What Was Added
On **Kunden** → **Neuer Kunde / Kundendaten** (**Kunde & Adresse** tab), saving now **requires** the same core fields as the business form: **Kundentyp**, **Branche**, **FZG-Händler (Ja/Nein)**, **Gesellschaftsform**, **Firmenname**, **Straße / PLZ / Ort / Land** on the **primary address** (`adressen[0]`), and the **first contact** **Name** and **Telefon**. **Status** is marked required in the UI (dropdown always has a value). Red asterisks were added to labels; the footer shows translated validation messages and focuses the first invalid field (switching to the main address or primary contact tab when needed).

### Where It Was Added
- `frontend/src/features/customers/validators/customerValidation.ts` — **`NEW_CUSTOMER_KUNDE_FOCUS`**, **`collectNewCustomerKundeRequiredIssues`**.
- `frontend/src/features/customers/validators/customerValidation.test.ts` — tests for the collector.
- `frontend/src/features/customers/index.ts` — re-exports **`collectNewCustomerKundeRequiredIssues`**, **`NEW_CUSTOMER_KUNDE_FOCUS`**.
- `frontend/src/components/NewCustomerModal.tsx` — **`handleSave`** uses the collector; **`id`** / asterisks on inputs; primary-address-only focus ids when **`safeAdresseIdx === 0`** for strasse / PLZ / Ort / Land / USt.
- `frontend/src/contexts/LanguageContext.tsx` — **`newCustomerRequired*`** keys in **de, en, fr** and **LANGUAGE_OVERRIDES** (es, it, pt, tr, ru, hi, ar, zh, ja).

### What It Does (Technical)
1. **`collectNewCustomerKundeRequiredIssues`** walks **`CustomerFormDraft`** in a fixed order and returns **`messageKey`**, **`fieldId`**, optional **`preFocus`** (`mainAdresse` | `primaryKontakt`).
2. **`handleSave`** maps issues to footer messages, switches to the **kunde** tab, runs **`preFocus`** (e.g. **`setActiveAdresseIdx(0)`**), then **`focusModalCustomerField`** after 60ms.
3. **Primary address** validation always uses **`form.adressen[0]`**, matching **`formToPayload`** flattening.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `customer_type` | `"" \| legal_entity \| natural_person` | Yes (non-empty) | Kundentyp |
| `branche` | string | Yes (trim) | Industry |
| `fzgHandel` | `"" \| ja \| nein` | Yes | Vehicle trader |
| `gesellschaftsform` | string | Yes (trim) | Legal form |
| `firmenname` | string | Yes (trim) | Company name |
| `adressen[0].*` | address slice | strasse, plz, ort, land required | Main address |
| `kontakte[0].name` / `.telefon` | string | Yes (trim / phone) | Primary contact |

### Database
- **Engine:** N/A (client-side validation only before existing persist path)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
`newCustomerRequiredCustomerType`, `newCustomerRequiredBranche`, `newCustomerRequiredFzgHandel`, `newCustomerRequiredGesellschaftsform`, `newCustomerRequiredStrasse`, `newCustomerRequiredPlz`, `newCustomerRequiredOrt`, `newCustomerRequiredLand`, `newCustomerRequiredContactName`, `newCustomerRequiredContactPhone` — **de, en, fr** + **LANGUAGE_OVERRIDES**

### Dependencies Added
None

### Notes / Known Limitations
**Status** is not validated as empty because the select has no empty option. Secondary addresses are not individually validated beyond the primary row used for **`formToPayload`**.

---
