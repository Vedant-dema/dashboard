# Feature Log — 2026-04-17

> Features added or modified on this date.

---

## FEATURE-325: Timetable offer tab — Angebotsarchiv as column beside Preisvergleich
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
On the calendar (timetable) contact drawer **Angebot** tab, **Angebotsarchiv** (prior matching offers) is now its own column beside **Preisvergleich** (current price negotiation / rounds), instead of stacking both under one heading. Layout uses a responsive grid: two-by-two from large screens and four columns on extra-wide screens.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — split offer workspace into four grid areas; Preisvergleich column shows only negotiation history; new column for archive with section title.
- `frontend/src/pages/timetable/components/TimetableOfferMemoryPanel.tsx` — `layout` prop (`card` | `column`) for embedded column styling and empty state with icon + text.
- `frontend/src/pages/timetable/components/TimetableOfferNegotiationHistory.tsx` — optional `hideCardTitle` when the parent column supplies the main title.

### What It Does (Technical)
1. The offer tab body uses `grid` with `lg:grid-cols-2` and `xl:grid-cols-4` so generator, offer form, Preisvergleich, and Angebotsarchiv each occupy a distinct cell with borders on larger breakpoints.
2. `TimetableOfferNegotiationHistory` renders with `hideCardTitle` so the column heading `timetableOfferPriceComparisonTitle` is the single primary label for that column.
3. `TimetableOfferMemoryPanel` with `layout="column"` omits the duplicate in-card title row, uses `aria-label` on the region, and shows an icon + empty copy row aligned for the archive column.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `layout` | `'card' \| 'column'` | No | Controls archive panel chrome; default `card` for modal/standalone. |
| `hideCardTitle` | `boolean` | No | Hides inner negotiation card title when parent shows Preisvergleich. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None (reuses `timetableOfferPriceComparisonTitle`, `timetableOfferMemoryTitle`, and existing memory strings).

### Dependencies Added
None

### Notes / Known Limitations
Below `lg`, sections stack in a single column. Between `lg` and `xl`, layout is 2×2 (generator | offer / Preisvergleich | Angebotsarchiv).

---

## FEATURE-326: Timetable contact drawer — grid parity with customer modal (width/height)
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The calendar (timetable) **customer contact** modal now uses the same **multi-column grid rhythm** as **Kunde & Adresse** in `NewCustomerModal`: three master columns use `md:grid-cols-3` and `lg:grid-cols-12` with **4+5+3** spans; the **Angebot** workspace uses **2×2 until 1420px** then **four equal columns** (`min-[1420px]:grid-cols-12` with `col-span-3` each), matching the customer edit wide layout. Column cards use **equal row stretch** (`items-stretch`, `h-full`, `min-h-0`) without a forced `dvh` minimum on remarks, so bottoms align more cleanly like the customers page.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — outer call-tab grid, column span classes, remarks column height behaviour, offer-tab inner grid gaps and breakpoints.

### What It Does (Technical)
1. **Appointment tab:** `grid-cols-1 md:grid-cols-3 lg:grid-cols-12` with columns `lg:col-span-4`, `lg:col-span-5`, `lg:col-span-3`; each column wrapper is `flex h-full min-h-0` so grid row height is shared evenly.
2. **Remarks column:** Removed `min-h-[min(70dvh,22rem)]` / `lg:min-h-[min(78dvh,28rem)]` so the column no longer forces an asymmetric strip taller than siblings.
3. **Offer tab:** Inner grid uses `gap-5 md:gap-6`, `lg:grid-cols-2 min-[1420px]:grid-cols-12`, four sections `lg:col-span-1 min-[1420px]:col-span-3` with border rules adjusted for 2×2 vs single row (same breakpoint as customer modal’s `min-[1420px]` wide grid).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Layout-only change; no props or API changes. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Body remains scrollable when content exceeds viewport; equal heights apply **per grid row** within the scroll region, matching how the customer modal behaves.

---

## FEATURE-328: Timetable offer — Abholauftrag CTA layout and alignment
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **Abholauftrag** action in the timetable offer column is no longer squeezed inside the narrow seller-price grid cell. It sits in a **full-width row** under the price / stock-type grid when “Gekauft” is active, with adjusted padding, font scale (`text-[11px]` → `sm:text-xs`), `leading-snug`, and flex alignment so the label, icon, and chevron align cleanly.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferMinimalBlock.tsx` — moved CTA below the three-column block; updated button classes and `aria-label` English fallback for `t()`.

### What It Does (Technical)
1. Removes nested Abholauftrag button from the first grid column under seller price.
2. Renders a conditional block after the `lg:grid-cols-3` price/ledger grid with top border and full-width button using `items-center`, `px-4 py-2.5`, and responsive `justify-center` / `sm:justify-start`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Presentational layout only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
CTA only appears when the offer is in the purchase (`gekauft`) lane, unchanged from prior behaviour.

---

## FEATURE-329: Timetable Angebot tab — equal-width offer columns
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The four offer-tab columns (Angebots-Generator, Angebot, Preisvergleich, Angebotsarchiv) now share the **same track width** at wide breakpoints: the inner grid uses **`min-[1420px]:grid-cols-4`** with uniform `gap-x` instead of a 12-column layout with mixed spans and asymmetric `pl`/`pr` gutters.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — offer workspace grid container and section wrappers (removed unequal `col-span` and border/padding offsets).

### What It Does (Technical)
1. Replaces `min-[1420px]:grid-cols-12` + per-section `col-span-3` with `min-[1420px]:grid-cols-4` so each column is one equal `1fr` track.
2. Uses `lg:grid-cols-2` with explicit `lg:gap-x-6 lg:gap-y-8` for an even 2×2 layout below 1420px.
3. Drops left borders and extra left padding on middle/right sections at the wide layout so inner content widths match across columns; row separation on `lg` remains via `border-t` on the second row only.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Layout only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Breakpoint for four-across remains `min-[1420px]` to align with the wider customer-modal grid.

---

## FEATURE-330: Timetable Angebot column — surface matches Angebotsarchiv
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **Angebot** form column in the timetable contact drawer uses the **same blue-tinted gradient panel** as the **Angebotsarchiv** `TimetableOfferMemoryPanel` (column layout): `border-blue-100/90`, `from-blue-50/70 via-white to-white`, matching ring/shadow.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferMinimalBlock.tsx` — optional `archiveColumnSurface`; outer form wrapper class switches between default white card and archive-matched surface.
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — passes `archiveColumnSurface` for the offer tab minimal block.

### What It Does (Technical)
1. `archiveColumnSurface` selects the same Tailwind surface string used by `TimetableOfferMemoryPanel` column layout (padding `p-3.5 sm:p-4` aligned with archive panel).
2. Default surface (no prop) unchanged for any future reuse.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `archiveColumnSurface` | `boolean` | No | When true, apply archive-matched panel styling. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Only the timetable drawer offer column passes the flag; other call sites would need to opt in explicitly.

---

## FEATURE-331: Timetable offer — Abholauftrag CTA under price fields
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **Abholauftrag** control is placed **directly under** the seller price and purchase-bid inputs and **above** the **Bestandsfluss** (stock lane) buttons, with a light separator and full-width layout; the price + CTA + ledger block uses a vertical stack instead of a three-column grid.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferMinimalBlock.tsx` — reordered price section markup; ledger tiles use `sm:flex-row` with `sm:w-auto sm:flex-1` for a single compact row on wider viewports.

### What It Does (Technical)
1. `space-y-5` wraps: (a) `sm:grid-cols-2` for the two EUR fields, (b) optional Abholauftrag row when `activeLedgerMode === 'purchase'`, (c) full-width Bestandsfluss group with top border.
2. Removes the previous full-width Abholauftrag row below the combined three-column grid.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | — | — | Layout only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Abholauftrag still only shows in the **Gekauft** (purchase) lane; unchanged logic.

---

## FEATURE-332: Timetable drawer — sold vehicle control before Kurzfassung
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **sold vehicle** quick control (red “Verkauft” affordance) is moved from the **FAHRZEUGE / ANGEBOTE** strip header into the **workspace tab bar**, immediately **before** **Kurzfassung** (smart summary). With a single sold line, the second line shows **trailing model digits only** (e.g. `818` from “MB Atego 818”); multi-sold menu rows use the same rule with **full label on `title`** for hover. Choosing a sold line switches to the **Angebot** tab and updates the selected offer.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — export `SoldVehicleQuickAccess`, `timetableOfferVehicleChipLabel`, `vehicleModelNumberOnly`; optional `sublineNumberOnly`; remove duplicate sold control from strip title row.
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — render `SoldVehicleQuickAccess` before the Kurzfassung control; wire `selectOfferId` + tab switch.

### What It Does (Technical)
1. `vehicleModelNumberOnly` parses the last `\d+` run from `brand`+`model`, else `vehicle_type`.
2. `SoldVehicleQuickAccess` `sublineNumberOnly` drives `soldSubline` for the main chip and dropdown labels.
3. Drawer `onSelect` calls `selectOfferId` then `setDrawerWorkspaceTab('offer')`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `sublineNumberOnly` | `boolean` | No | Prefer numeric model suffix on second line. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
If no trailing digits exist in make/model/type, the second line falls back to the full chip label for that offer.

---

## FEATURE-333: Timetable offer — remove generator close and negotiation notes UI
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
Removed the **close (X)** control and **collapse** behaviour from the **Angebots-Generator** (panel always open). In **Preisvergleich** / price negotiation, removed the **Notiz** table column, the **optional note** field above “Preisrunde erfassen”, and new rounds no longer capture a note from the UI.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferGeneratorBlock.tsx` — dropped `genOpen`, collapsed chip reopen, and X button; “Detected” chip row only when relevant.
- `frontend/src/pages/timetable/components/TimetableOfferNegotiationHistory.tsx` — table and composer simplified; unused imports removed.

### What It Does (Technical)
1. Generator `useEffect` on `rowKey` resets text/status/parsed only.
2. `appendRound` builds rounds without `note`; existing persisted `note` values on old rows remain in data but are not shown.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `negotiation_rounds[].note` | `string` (optional) | No | Still allowed in type; UI no longer edits or displays it. |

### Database
- **Engine:** None (client draft)
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No change to persistence shape beyond omitted `note` on newly appended rounds.

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Historical `note` text on saved rounds is hidden until a future UI restores it.

---

## FEATURE-334: Timetable offer — scalable vehicle strip (many trucks)
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Added

### What Was Added
When a contact has **many vehicle offer lines** (e.g. dozens of trucks), the **Vehicles / offers** row no longer tries to show every chip inline. Above a configurable threshold (see **FEATURE-336**: **more than six** lines), the UI shows a **compact trigger** (current selection, total count, chevron) that opens a **searchable, scrollable list** to switch or remove lines. At or below that count, chips stay visible in a **single horizontal scroll row** so the layout stays predictable without wrapping clutter.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — `VEHICLE_STRIP_PICKER_THRESHOLD`, `VehicleOfferOverflowPicker`, conditional layout for strip vs picker; inline chips use `overflow-x-auto` and `truncate` caps.
- `frontend/src/contexts/LanguageContext.tsx` — `timetableOfferVehiclePicker*` keys in `MESSAGES.de` / `en` / `fr` and all nine `LANGUAGE_OVERRIDES` blocks.

### What It Does (Technical)
1. If `offers.length > VEHICLE_STRIP_PICKER_THRESHOLD` (see **FEATURE-336**, currently `6`), render `VehicleOfferOverflowPicker`: button shows ledger-colored shell for the active offer, truncated label, numeric badge, chevron; click toggles panel.
2. Panel: filter `input` (type `search`) filters by concatenated chip label, brand, model, `vehicle_type`, and trailing model digits; list rows call `onSelect` / `onRemove`; outside click and Escape close; query resets when closed.
3. Otherwise render chip UX inside `flex-nowrap overflow-x-auto`; add button remains `shrink-0` beside the scroller on `sm+`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `VEHICLE_STRIP_PICKER_THRESHOLD` | `6` (constant, FEATURE-336) | N/A | Count above which the overflow picker is used instead of inline chips. |
| Picker props | same as strip | Yes | Reuses `offers`, `selectedId`, `onSelect`, `onRemove`, `t`. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
- `timetableOfferVehiclePickerAria` — button `aria-label` with `{active}` and `{n}` placeholders.
- `timetableOfferVehiclePickerMenuAria` — listbox region label.
- `timetableOfferVehiclePickerSearchPh` — filter field placeholder.
- `timetableOfferVehiclePickerEmpty` — no filter matches.

### Dependencies Added
None

### Notes / Known Limitations
Initial threshold was 10; **FEATURE-336** lowers it to 6 (picker when more than 6 lines). Picker is positioned `absolute` under the trigger (z-index 30); very narrow viewports use full-width panel.

---

## FEATURE-336: Timetable vehicle strip — earlier overflow + pill-style selector
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Extended

### What Was Added
Large offer lists switch to the **searchable overflow picker** sooner (**more than six** vehicle lines instead of more than ten), so busy deals avoid a long chip strip earlier. The overflow **trigger** uses **rounded-full** styling and slightly wider max width so it reads like the existing pill tags. Chip mode keeps **horizontal scroll** with `overscroll-x-contain` for touch.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — `VEHICLE_STRIP_PICKER_THRESHOLD` set to `6`; overflow button classes (`rounded-full`, padding, `sm:max-w`).

### What It Does (Technical)
1. `useOverflowPicker` when `offers.length > 6` (seven or more lines).
2. No API or store changes.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `VEHICLE_STRIP_PICKER_THRESHOLD` | `number` | N/A | Constant `6` in source. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
References **FEATURE-334** for picker behaviour and keys. Further tuning is a one-line constant change.

---

## FEATURE-335: Timetable offer minimal block — three-column price / stock layout
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **Verkäuferpreis**, **Einkaufsgebot**, and **Bestandsfluss** section is laid out as **three aligned columns** (from `sm` breakpoints): seller price with **Abholauftrag** directly underneath in the **first** column only when **Gekauft** is selected; purchase bid alone in the **second** column; **Bestandsfluss** as a **vertical stack** of lane buttons in the **third** column. Narrow screens keep a **single-column** stack in the same order. Abholauftrag styling was tightened (emerald panel, truck icon tile, serif label) and the chevron affordance was removed to match the reference.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferMinimalBlock.tsx` — replaced two-column price grid + full-width Abholauftrag + separate ledger row with `sm:grid-cols-3`; ledger uses `flex-col` only; third-column title uses existing `angeboteStockFlowColHeader`; removed `ChevronRight` import.

### What It Does (Technical)
1. `grid gap-5 sm:grid-cols-3 sm:items-start` wraps three cells: (A) seller field + conditional Abholauftrag, (B) purchase field, (C) ledger label + three `ledgerTileStyles` buttons with `w-full` stacked.
2. Abholauftrag still gates on `activeLedgerMode === 'purchase'`.
3. No new translation keys; column header reuses `angeboteStockFlowColHeader` with English fallback `"Stock flow"`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Layout-only; same `setOfferField` / `onOpenAbholauftrag` as before. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None (reused `angeboteStockFlowColHeader` for third-column label).

### Dependencies Added
None

### Notes / Known Limitations
`angeboteStockFlowColHeader` may differ slightly from `angebotStockLedgerLabel` in some locales (shorter table-style header vs longer hint label); both remain in the bundle for other screens.

---

## FEATURE-337: Timetable offer tab — slim vehicle bar + denser workspace
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
**Fahrzeuge / Angebote** is redesigned as a **single slim toolbar row**: short column label (from `timetableOfferVehicleStripLabelShort`), one **compact lane combobox** (searchable list for any fleet size — **no horizontal chip strip**), and a **square +** control with full `aria-label` / `title`. **Empty** offer lists show a one-line hint (`timetableOfferVehicleStripNoLines`) plus **+**. The **offer tab** panel and four-column grid use **tighter gaps and padding**; **Angebots-Generator** no longer forces tall `min-h` viewport fractions and uses a **capped max-height**, smaller default **textarea** size, and tighter inner spacing; **Angebot** minimal form (archive surface) uses **smaller radii/padding**, tighter field grids, and a **shorter equipment textarea** default height. Goal: **less vertical and horizontal noise**; the drawer body may still scroll on very small viewports when content exceeds `92vh`.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — removed inline chips and `VEHICLE_STRIP_PICKER_THRESHOLD`; always `VehicleOfferOverflowPicker`; slim trigger + list chrome; icon-only add; empty state row; removed unused `unselectedChipShellClass`.
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — offer tab panel `gap`/`padding`; grid gaps; section vertical gaps; vehicle strip wrapper `shrink-0`.
- `frontend/src/pages/timetable/components/TimetableOfferGeneratorBlock.tsx` — `max-h` cap, reduced `rows`, smaller `min-h` / typography / radii.
- `frontend/src/pages/timetable/components/TimetableOfferMinimalBlock.tsx` — denser spacing when `archiveColumnSurface`; equipment textarea rows / min-height.
- `frontend/src/contexts/LanguageContext.tsx` — `timetableOfferVehicleStripLabelShort`, `timetableOfferVehicleStripNoLines` in **de, en, fr** and all **nine** `LANGUAGE_OVERRIDES` blocks.

### What It Does (Technical)
1. `TimetableOfferVehicleStrip` renders `role="region"` with `aria-label` from `timetableOfferVehicleStripTitle`; non-empty path is `flex` row: optional short label (`sm+`), `VehicleOfferOverflowPicker`, add button.
2. Picker button uses `h-8`, `rounded-lg`, smaller dot and count badge; dropdown `max-h` tightened.
3. Generator `section` uses `max-h-[min(52vh,26rem)]` (lg slightly smaller) instead of large `min-h` vh locks.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Same props to `TimetableOfferVehicleStrip`; new strings are i18n-only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
- `timetableOfferVehicleStripLabelShort` — compact column label beside selector.
- `timetableOfferVehicleStripNoLines` — empty list hint beside + control.

### Dependencies Added
None

### Notes / Known Limitations
**FEATURE-334** / **FEATURE-336** chip threshold behaviour is **removed**; history in those entries is partially superseded. Guaranteeing **zero** vertical scroll for the full contact drawer on all screen sizes would require further structural changes (e.g. sub-tabs or a dedicated full-screen offer layout).

---

## FEATURE-338: Timetable vehicle lane selector — narrow trigger width
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The vehicle lane **combobox trigger** is no longer `flex-1` full-width: it uses a **fixed max width** (`max-w-[min(100%,10.5rem)]`, `sm:max-w-[12.5rem]`) with **`rounded-full`** so the control stays a **compact pill** beside **FAHRZEUGE** and **+**. The **dropdown list** remains comfortably wide (`w-[min(20rem,…)]`) for search and many lines.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — `VehicleOfferOverflowPicker` wrapper and button classes; listbox panel width classes.

### What It Does (Technical)
1. Picker root `div` is `w-auto shrink-0` with max-width caps instead of `flex-1`.
2. Listbox panel uses explicit `w-[min(20rem,calc(100vw-1.5rem))]` (no `left-0 right-0` stretch).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Presentation-only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Long make/model labels **truncate** inside the narrow trigger; full text remains in the opened list and in `aria-label`.

---

## FEATURE-339: Timetable vehicle picker — z-index above offer generator
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The vehicle lane **dropdown** was painting **under** the **Angebots-Generator** column because the generator sits **later in the DOM** as a flex sibling and won the default paint order. The vehicle strip wrapper now uses **`relative z-[60]`**, the main offer **grid** uses **`relative z-0`**, the generator **section** uses **`relative z-0`**, and the listbox uses **`z-[100]`** so the menu reliably stacks **above** the generator panel.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `#tt-offer-vehicle-strip` classes; offer workspace `grid` + first `section` stacking.
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — listbox `z-[100]` (was `z-30`).

### What It Does (Technical)
1. Explicit stacking: strip subtree `z-[60]` vs workspace grid `z-0`.
2. Higher `z-index` on the popover inside the strip’s stacking context.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | CSS only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Stacking-only fix; **FEATURE-340** adds a **body portal** so the list is not clipped by panel `overflow-hidden`.

---

## FEATURE-340: Timetable vehicle picker — portal + fixed position to escape overflow
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The vehicle lane listbox renders via **`createPortal(..., document.body)`** with **`position: fixed`**, **`z-index: 6000`**, and placement from **`getBoundingClientRect()`** on the trigger so it is no longer clipped or covered by the offer column’s **`overflow-hidden`** or sibling columns (**Angebots-Generator**). **`useLayoutEffect`** updates position on open, **window resize**, and **capture-phase scroll**. **Click-outside** closes when the target is outside both the trigger wrapper and the portaled menu. Listbox **`id`** supports **`aria-controls`** on the trigger.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — `VehicleOfferOverflowPicker` only.

### What It Does (Technical)
1. `placement` state holds `top`, `left`, `width`, `maxHeight` in viewport coordinates.
2. `mousedown` / `Escape` unchanged except outside-click includes `menuRef`.
3. Menu uses `flex` column + scrollable list region with `min-h-0 flex-1 overflow-y-auto`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | DOM only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Picker `z-index` is inline **6000** so it stays above the contact drawer shell; adjust if a higher global overlay is introduced.

---

## FEATURE-341: Timetable offer — vehicle arrows + spiral transition on workspace
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Extended

### What Was Added
When the user moves between vehicle lines with the **previous / next** controls on the offer tab, the **generator + offer form + price comparison + offer memory** grid plays a short **3D spiral-style** transition (CSS classes `tt-offer-spiral-next` / `tt-offer-spiral-prev` from `index.css`). The vehicle strip shows a compact **`current/total`** index in the pill while keeping the full translated phrase on **`title`** and **`aria-label`**. Navigation ref tracking was corrected so the animation does not re-fire on every render.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — `offerSpiralIndex` / `prevOfferSpiralIdxRef` / `offerSpiralDir` state; `useEffect` detects index change on the offer tab and applies animation class for ~580ms; perspective wrapper around the offer workspace grid; proper closing of nested wrappers; dropped `z-[60]` on the vehicle strip container.
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — position badge displays `index+1/total` with `timetableOfferVehicleIndexOf` used for accessible name and tooltip.
- `frontend/src/index.css` — existing `@keyframes` and `.tt-offer-spiral-next` / `.tt-offer-spiral-prev` (no change in this entry if already present).

### What It Does (Technical)
1. `ensureOfferIdsAndSelection(draft)` drives `offerSpiralSelectedId` and `offerSpiralIndex` while the drawer is open.
2. On the **offer** workspace tab, when `offerSpiralIndex` changes and the previous ref is non-null and different, set `offerSpiralDir` to `'next'` or `'prev'`, immediately set `prevOfferSpiralIdxRef` to the new index, and clear `offerSpiralDir` after a timeout matching the CSS animation duration.
3. The grid wrapper uses `[perspective:1400px]`, `[transform-style:preserve-3d]`, and the spiral class names while `offerSpiralDir` is set.
4. When the user is not on the offer tab, the ref is kept in sync with the current index so the first switch after returning to offer does not animate from a stale index incorrectly.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `offerSpiralDir` | `'next' \| 'prev' \| null` | N/A | Internal UI state for CSS animation class. |
| `prevOfferSpiralIdxRef` | `number \| null` | N/A | Previous offer index for direction detection. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None (reuses `timetableOfferVehicleIndexOf` and related keys from the vehicle strip work).

### Dependencies Added
None

### Notes / Known Limitations
Respects **`prefers-reduced-motion`** via existing CSS. Spiral runs on the whole offer workspace, not per-field.

---

## FEATURE-342: Timetable offer — uniform vehicle transition + refined motion
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Extended

### What Was Added
The **vehicle strip (Fahrzeuge)** and the **full offer column grid** (generator, offer form, price comparison, offer memory) now sit inside **one** animated surface, so every vehicle-linked control moves together when the selected line changes. The motion was retuned to a **direction-aware 3D panel settle** (translateZ + rotateY/rotateX, no blur), with **`transform-origin`**, **`will-change`**, and **row-change** handling so opening another calendar row does not play a bogus transition. Effect dependencies were tightened so the animation logic does not re-run on unrelated `draft` reference churn.

### Where It Was Added
- `frontend/src/pages/timetable/TimetableContactDrawer.tsx` — nested layout: perspective shell → single inner node with `tt-offer-spiral-*` wrapping `#tt-offer-vehicle-strip` plus the grid; `offerSpiralRowIdRef` resets spiral state when `draft.id` changes; `OFFER_SPIRAL_MS` aligned to CSS duration; `offerSpiralIndex` memo keyed by `draft.id`, `selected_offer_id`, and an offer-id fingerprint.
- `frontend/src/index.css` — replaced prior rotate3d/blur keyframes with `translate3d` + `rotateY` / `rotateX` + light opacity ramp; **0.52s** `cubic-bezier(0.16, 1, 0.3, 1)`; reduced-motion clears `will-change`.

### What It Does (Technical)
1. One **`[perspective:2000px]`** wrapper contains the animated flex column that holds the strip and the four-column grid.
2. On timetable **row** change, refs reset and `offerSpiralDir` clears before index comparisons run.
3. `offerSpiralIndex` derives from `ensureOfferIdsAndSelection(draft)` only when stable id keys change.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `offerSpiralRowIdRef` | `number \| null` | N/A | Last `draft.id` used to suppress cross-row animation. |
| `OFFER_SPIRAL_MS` | `number` | N/A | Clears direction class after animation (~540ms). |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Extends **FEATURE-341**. Very fast successive vehicle switches may restart the class before the prior timeout fires; acceptable for normal use.

---

## FEATURE-343: Timetable offer — vehicle strip centered in form
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **Fahrzeuge** label and the **prev / vehicle pill / next / remove / add** control row are **horizontally centered** within the offer panel: a stacked layout (`flex-col` + `items-center`) with the short vehicles label on top and a `justify-center` wrap row for controls. The empty state (no vehicle lines) uses the same centered column pattern.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — layout classes only; removed left-aligned sidebar label and `flex-1` stretch on the pill so the cluster sizes to content and stays centered.

### What It Does (Technical)
1. Outer region: `flex flex-col items-center gap-2`.
2. Inner row: `flex w-full flex-wrap items-center justify-center gap-1.5 sm:gap-2`.
3. Chip container no longer uses `flex-1`; `max-w` caps width on small screens.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Presentation only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
On very narrow widths the control row may wrap while remaining centered.

---

## FEATURE-344: Timetable offer — vehicle strip meta + card layout
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The timetable **Fahrzeuge** strip is a **centered card** with prev/next on the sides of the vehicle panel, **remove** and **add** on a **secondary row** under a divider. The selected vehicle shows a **second line of metadata** (stock lane label, optional year, mileage, truncated standort, negotiation round count, attachment count, customer asking price in EUR) joined with middots, plus a **full summary** on the card `title`. Visual lane state uses a **soft tinted surface** and **3px left accent** instead of a full saturated pill.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — `buildStripMetaLine`, `ledgerAccentBorderClass`, `ledgerSoftSurfaceClass`, numeric/location helpers; redesigned JSX; larger `h-9`/`rounded-xl` controls.
- `frontend/src/contexts/LanguageContext.tsx` — new keys in **de / en / fr** and **es, it, pt, tr, ru, hi, ar, zh, ja** for ledger short labels, year/mileage/rounds/files/asking string templates.

### What It Does (Technical)
1. `buildStripMetaLine` reads `TimetableTruckOffer` + `angebotLedgerKind` and appends optional segments when data exists.
2. `Intl.NumberFormat` formats mileage (integer) and `expected_price_eur` (EUR currency, 0 decimals).
3. Card `title` concatenates make/model label, meta line, and index-of string for hover/tooltip discovery.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `negotiation_rounds` | `array` | No | Drives optional “price rounds” segment when length > 0. |
| `vehicle_unterlagen` | `array` | No | Drives optional file count segment. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
`timetableOfferVehicleStripLedgerPurchase`, `timetableOfferVehicleStripLedgerDisposal`, `timetableOfferVehicleStripLedgerConflict`, `timetableOfferVehicleStripLedgerNeutral`, `timetableOfferVehicleStripYearFmt`, `timetableOfferVehicleStripMileageFmt`, `timetableOfferVehicleStripRoundsFmt`, `timetableOfferVehicleStripFilesFmt`, `timetableOfferVehicleStripAskFmt` — in **MESSAGES.de/en/fr** and all **nine** `LANGUAGE_OVERRIDES` bundles.

### Dependencies Added
None

### Notes / Known Limitations
EUR formatting uses the runtime default `Intl` locale (not the app language); acceptable for a compact strip until `localeTag` is threaded into this component.

---

## FEATURE-345: Timetable offer — customer vs DEMA price labels
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The two short offer price field labels (`timetableOfferSellerAskingShort` for `expected_price_eur`, `timetableOfferPurchaseBidShort` for `purchase_bid_eur`) were renamed for clarity: **customer price** vs **DEMA price**, with German copy **Kundenpreis (EUR)** and **DEMA-Preis (EUR)**. All **12** language bundles were updated or given explicit override strings; component `t()` fallbacks use English **Customer price (EUR)** and **DEMA price (EUR)**.

### Where It Was Added
- `frontend/src/contexts/LanguageContext.tsx` — `MESSAGES.de/en/fr` and **es, it, pt, tr, ru, hi, ar, zh, ja** blocks.
- `frontend/src/pages/timetable/components/TimetableOfferMinimalBlock.tsx` — fallback strings for `t()`.
- `frontend/src/pages/timetable/TimetableOfferModal.tsx` — fallback strings for `t()`.

### What It Does (Technical)
1. Same keys drive labels in the minimal offer block and the offer modal; only display strings changed.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | i18n only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None — existing keys **modified** across locales.

### Dependencies Added
None

### Notes / Known Limitations
None

---

## FEATURE-346: Timetable vehicle strip — remove inside card, smaller
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
When more than one vehicle line exists, the **remove (X)** control is **inside** the lane-colored vehicle summary card, **top-right** (`absolute`), with a **smaller** hit target (**6×6**, icon **2.5**). Content gains **right padding** so title/meta do not run under the icon.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — layout and classes only.

### What It Does (Technical)
1. Inner panel `relative`; remove button `absolute right-1 top-1` (slightly larger offset on `sm`).
2. Inner flex row gets `pr-7 sm:pr-8` when `offers.length > 1`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Presentation only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Add (+) placement updated again in **FEATURE-347** (beside next arrow; outer footer removed).

---

## FEATURE-347: Timetable vehicle strip — minimal row, + beside next
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The vehicle strip no longer uses the **outer rounded shell**, the **“Fahrzeuge” heading line**, or a **footer row** for add. The control is a **single horizontal row**: **previous**, **vehicle summary card** (with small remove when multiple lines), **next**, and **add (+)** immediately to the right of next. The region keeps `aria-label` for screen readers.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — layout only (non-empty state).

### What It Does (Technical)
1. `flex` row `justify-center` with `max-w-xl`; next and add share a shrink-0 `flex-row` group with `gap-1.5`.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Presentation only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
Empty state (zero vehicles) still shows the short heading and copy plus add.

---

## FEATURE-348: Sold-vehicle quick access — professional module chrome
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
The **`SoldVehicleQuickAccess`** chip (drawer header — sold third-party lines) was visually redesigned: **idle** state uses a white pill with subtle ring, **car icon in a rounded tile** (soft red gradient), uppercase **Sold** micro-label with letter-spacing, and **serif** primary line; **selected** state uses a restrained **red gradient** shell with frosted icon tile and white type; **empty** state uses a dashed slate capsule. The **dropdown** for multiple sold lines uses rounded-2xl panel, blur, and **menu rows** with small car icon tiles instead of only a dot. **Behavior unchanged** (disabled when none, single click jumps, multi toggles menu, same `aria-*` and `title` sources).

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — `SoldVehicleQuickAccess` JSX/classes only.

### What It Does (Technical)
1. Same `handleMainClick`, `menuOpen`, `soldOffers.map`, and `onSelect` paths as before.
2. Menu `z-[80]` retained above dense drawer UI.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Presentation only. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
None

---

## FEATURE-349: Sold-vehicle menu rows — label + price meta
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
When several sold (third-party) lines exist, each **dropdown row** shows the **full vehicle title** (`chipLabel`: make/model or type) on the first line and, when data exists, a **second line** with **customer price** (`expected_price_eur`) and **DEMA price** (`purchase_bid_eur`), formatted in **EUR** via `Intl`, joined with a middot. New i18n keys **`timetableOfferSoldVehicleMenuMetaCustomer`** and **`timetableOfferSoldVehicleMenuMetaDema`** (with `{amount}`) exist in **all 12** language bundles. Menu min-width increased slightly for the extra text; `title` / `aria-label` on each menu item summarize vehicle + prices.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — `formatSoldMenuEurAmount`, `soldOfferMenuMetaLine`, and sold-vehicle menu item JSX.
- `frontend/src/contexts/LanguageContext.tsx` — new keys in **de, en, fr** and **es, it, pt, tr, ru, hi, ar, zh, ja**.

### What It Does (Technical)
1. `soldOfferMenuMetaLine` appends customer segment when `expected_price_eur > 0`, DEMA segment when `purchase_bid_eur > 0`.
2. Rows use `items-start` and a two-line text block; icon column `mt-0.5` for optical alignment.

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| `expected_price_eur` | `number \| null` | No | Shown as customer price when > 0. |
| `purchase_bid_eur` | `number \| null \| undefined` | No | Shown as DEMA price when > 0. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
`timetableOfferSoldVehicleMenuMetaCustomer`, `timetableOfferSoldVehicleMenuMetaDema` — in **MESSAGES.de/en/fr** and all **nine** `LANGUAGE_OVERRIDES` bundles.

### Dependencies Added
None

### Notes / Known Limitations
EUR formatting uses the browser default `Intl` locale (not necessarily the app language).

---

## FEATURE-350: Sold-vehicle menu — same strip detail line as main card
**Date:** 2026-04-17
**Author/Agent:** Cursor AI
**Status:** Modified

### What Was Added
Each sold-vehicle **dropdown row** now includes the same **middot-separated detail string** as the main vehicle strip (`buildStripMetaLine`): stock lane (e.g. Verkauft (Dritt)), optional **year**, **mileage**, **location**, **negotiation rounds**, **files**, and **asking** segment when present. It appears **below** the vehicle title and **above** the Kunden-/DEMA-Preis line. Menu max-width widened slightly for longer strings; `title` / `aria-label` concatenate title, detail, and price line.

### Where It Was Added
- `frontend/src/pages/timetable/components/TimetableOfferVehicleStrip.tsx` — `SoldVehicleQuickAccess` menu map only.

### What It Does (Technical)
1. Reuses existing `buildStripMetaLine(offer, t)` for each `soldOffers` entry (no duplicate logic).

### Data It Accepts / Emits
| Field | Type | Required | Description |
|----|---|----|----|
| N/A | N/A | N/A | Uses existing offer fields via `buildStripMetaLine`. |

### Database
- **Engine:** None
- **Tables Affected:** N/A
- **Schema Changes:** None
- **Key Queries:** None

### API Endpoints (if applicable)
None

### State / Store (if applicable)
- **Store file:** N/A
- **Actions/Selectors added:** None
- **Persisted:** No

### i18n Keys Added
None

### Dependencies Added
None

### Notes / Known Limitations
If both `buildStripMetaLine` (asking segment) and the Kunden/DEMA price line show amounts, wording may overlap conceptually; acceptable until a dedicated “menu-only” meta builder is introduced.

---

